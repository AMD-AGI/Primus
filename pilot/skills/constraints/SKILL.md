# Constraints

**Status**: Stub
**Read by**: Re-Plan / EnvSweep / Execute Workers
**Domain**: safety constraints

Sub-Skills:
- `oom.md` — memory estimation rules
- `config.md` — config validity (tp divides hidden, etc.)
- `validation.md` — validation methods
- `env.md` — env incompatibility matrix (mutually exclusive / dangerous combos)

Each Skill is consumed by `tools/constraint.py`.
