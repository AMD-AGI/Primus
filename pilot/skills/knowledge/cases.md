# Cases (Historical Best-Configs)

**Status**: Stub

Case library indexed by `(cluster_class, model_family)`. Each entry is a frozen `KnowledgeEntry` with full `evidence_refs` to `state/archive/drafts/`.

<!-- CASE ENTRIES BELOW (curator-managed) -->
