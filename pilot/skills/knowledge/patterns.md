# Patterns

**Status**: Stub

General regularities (e.g. "MoE > 16 nodes always benefits from alltoall overlap"). Each entry follows `KnowledgeEntry` schema (§S4.4).

<!-- PATTERN ENTRIES BELOW (curator-managed) -->
