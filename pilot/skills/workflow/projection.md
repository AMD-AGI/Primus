# Projection (Modeling)

**Status**: Stub

Build initial Plans from Model Spec + ClusterProfile via single-node profiling and the Execution Model (§6).

## TODO

- [ ] Single-node profiling protocol (delegate to `tools/profiler.py`)
- [ ] Initial-plan generation rules (parallelism / mbs / recompute / env.diff scale-aware)
- [ ] When to STRUCTURAL_INVALIDATION reentry → re-PROJECT
