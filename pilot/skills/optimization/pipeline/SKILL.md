# Pipeline Optimization

**Status**: Stub

Trigger: Diagnose returns PIPELINE_BOUND (`bubble_ratio` > threshold).

Primary moves:
1. VPP (interleaved) — see `vpp.md`
2. Microbatch (mbs / gas) — see `microbatch.md`
3. Stage rebalance — see `balance.md`
