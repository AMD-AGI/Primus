work_group: ${PRIMUS_TEAM:amd}
user_name: ${PRIMUS_USER:root}
exp_name: ${PRIMUS_EXP_NAME:gemma4_31B-fp8-pretrain}
workspace: ./output

# STATUS (MI300X / gfx942): DOES NOT RUN.
# Compiles ~10 min, then aborts with kAllGather rendezvous timeout (40s default).
# Fix: raise first_collective_call timeouts in XLA_FLAGS (see flux_dev MI300X config).
# bf16 gemma4_31B trains normally.

modules:
  pre_trainer:
    framework: maxtext
    config: pre_trainer.yaml

    # model to run
    model: gemma4_31B.yaml
    overrides:
      run_name: "gemma4_31b_training"
      base_output_directory: "./output"
      steps: 50
      profiler: ""

      # data
      dataset_type: "synthetic"
      hf_access_token: ${HF_TOKEN:""}

      # checkpoint
      enable_checkpointing: false
      async_checkpointing: false

      # inter-node parallelism strategy
      dcn_data_parallelism: -1
      dcn_fsdp_parallelism: 1

      # intra-node parallelism strategy
      ici_fsdp_parallelism: 8
      ici_data_parallelism: 1
      ici_tensor_sequence_parallelism: -1

      remat_policy: "full"
      attention: "cudnn_flash_te"
      max_target_length: 8192
      # MI300X has less HBM (192GB) than MI355X; conservative default, untuned on gfx942.
      per_device_batch_size: 1

      # nanoo_fp8 for AMD MI300X/MI325X (uses e4m3fnuz/e5m2fnuz types)
      quantization: "nanoo_fp8"
