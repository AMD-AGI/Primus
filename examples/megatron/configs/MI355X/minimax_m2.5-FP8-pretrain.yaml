# Generated: same overrides as minimax_m2.5-BF16-pretrain-new.yaml + FP8 preset from minimax_m2.5-FP8-pretrain.yaml.
# Training args mirror start_training_minimax_m2.5.sh (PRETRAIN_TYPE=FP8 → point EXP to this file).
# Original presets: minimax_m2.5-FP8-pretrain.yaml (unchanged).

work_group: ${PRIMUS_TEAM:amd}
user_name: ${PRIMUS_USER:root}
exp_name: ${PRIMUS_EXP_NAME:minimax_m2.5-pretrain}
workspace: ${PRIMUS_WORKSPACE:./output}

modules:
  pre_trainer:
    framework: megatron
    config: pre_trainer.yaml

    model: ${PRIMUS_MODEL:minimax_m2.5}.yaml
    overrides:
      # log
      wandb_project: "Primus_MiniMax_M2.5_Pretrain"
      stderr_sink_level: DEBUG

      # debug
      moe_router_force_load_balancing: true
      log_avg_skip_iterations: 2
      log_avg_reset_interval: 50

      # --- start_training_minimax_m2.5.sh → train pretrain CLI (resolved defaults) ---
      num_layers: 62
      train_iters: 10
      micro_batch_size: 2
      global_batch_size: 1024
      seq_length: ${PRIMUS_SEQ_LENGTH:4096}
      max_position_embeddings: ${PRIMUS_MAX_POSITION_EMBEDDINGS:4096}
      lr: 2.2e-4
      min_lr: 2.2e-5
      lr_warmup_iters: 200
      lr_decay_iters: 5000
      lr_decay_style: cosine
      weight_decay: 0.1
      adam_beta1: 0.9
      adam_beta2: 0.95
      eod_mask_loss: true
      init_method_std: 0.02
      norm_epsilon: 1.0e-6

      # parallel (PP=4, VPP=2, EP=8; layout for 62 layers / 8 stages)
      tensor_model_parallel_size: ${PRIMUS_TP:1}
      pipeline_model_parallel_size: 4
      virtual_pipeline_model_parallel_size: 2
      expert_model_parallel_size: 8
      pipeline_model_parallel_layout: "Et*7|t*8|t*8|t*8|t*8|t*8|t*8|t*7,L"
      overlap_grad_reduce: true
      overlap_param_gather: true
      gradient_accumulation_fusion: true

      # data
      mock_data: true
      train_data_path: ${PRIMUS_TOKENIZED_DATA_PATH:/shared_aig/c4/tokenized/c4_en_train_text_document}
      valid_data_path: null
      test_data_path: null

      moe_use_legacy_grouped_gemm: true

      # rope fusion (script: --enable_experimental --apply_rope_fusion from APPLY_ROPE_FUSION=True)
      enable_experimental: true
      apply_rope_fusion: true

      # ckpt
      finetune: false
      auto_continue_train: false
      load: null
      no_load_optim: null
      no_load_rng: null
      save: null
      save_interval: 20000
      no_save_optim: null
      no_save_rng: null
      disable_last_saving: true
      ckpt_format: torch
      eval_iters: 0

      # Turbo
      enable_primus_turbo: true
      use_turbo_attention: false
      use_turbo_grouped_mlp: false
      use_turbo_rms_norm: true

      # deepep
      use_turbo_deepep: true
      moe_shared_expert_overlap: false
      moe_router_dtype: fp32
      turbo_deepep_num_cu: 80
      turbo_sync_free_moe_stage: 1

      moe_use_fused_router_with_aux_score: true
      moe_permute_fusion: true

      moe_layer_freq: 1

      # recompute
      recompute_num_layers: 2
      recompute_granularity: full
      recompute_method: block

      # Cross entropy
      cross_entropy_fusion_impl: "te"
      cross_entropy_loss_fusion: true

      # precision-aware optimizer (script)
      use_precision_aware_optimizer: true
      main_grads_dtype: bf16
      exp_avg_dtype: bf16
      exp_avg_sq_dtype: bf16

      # GC / PP warmup / MTP
      manual_gc: true
      manual_gc_interval: 1
      pp_warmup: true
      mtp_num_layers: 0

      # profile
      profile: false
      use_pytorch_profiler: false
      profile_step_end: 7
      profile_step_start: 6

      disable_wandb: true
      disable_tensorboard: true

      # FP8 (from minimax_m2.5-FP8-pretrain.yaml)
      fp8: hybrid
