work_group: ${PRIMUS_TEAM:amd}
user_name: ${PRIMUS_USER:root}
exp_name: ${PRIMUS_EXP_NAME:zebra_llama_1B_kda_pure-pretrain}
workspace: ${PRIMUS_WORKSPACE:./output}

modules:
  pre_trainer:
    framework: megatron
    config: pre_trainer.yaml

    model: zebra_llama_1B_kda_pure.yaml
    overrides:
      wandb_project: "Primus_Zebra_Llama_1B_KDA_Pure_Pretrain"
      stderr_sink_level: DEBUG

      eval_iters: 0

      num_workers: 0
      create_attention_mask_in_dataloader: false

      profile: false
      use_pytorch_profiler: false
      log_avg_skip_iterations: 2
      log_avg_reset_interval: 50

      # Training schedule — matched to FLA:
      #   steps=38147, batch=16/gpu, update=1, gpus=8, context=2048
      #   global_batch = 16 * 8 = 128, tokens/step = 128 * 2048 = 262,144
      train_iters: 38147
      micro_batch_size: 16
      global_batch_size: 128

      seq_length: 2048
      max_position_embeddings: 2048
      original_max_position_embeddings: 2048

      # Optimizer — matched to FLA:
      #   lr=2e-4, cosine_with_min_lr (min_lr_rate=0.1 → min_lr=2e-5)
      #   weight_decay=0.01, beta1=0.9, beta2=0.95
      lr: 2.0e-4
      min_lr: 2.0e-5
      lr_warmup_iters: 200
      lr_decay_iters: 38147
      lr_decay_style: cosine
      weight_decay: 0.01
      adam_beta1: 0.9
      adam_beta2: 0.95
      eod_mask_loss: true

      # Use KDA hybrid spec with hybrid_attention_ratio=0.0 (pure KDA)
      spec: ['primus.backends.megatron.core.models.hybrid.hybrid_mamba_mla_layer_specs', 'kda_hybrid_stack_spec']
      use_fla_triton_kda: true

      # Tokenizer
      tokenizer_type: HuggingFaceTokenizer
      tokenizer_model: meta-llama/Llama-3.2-1B

      # Parallelism
      tensor_model_parallel_size: 1
      pipeline_model_parallel_size: 1
      expert_model_parallel_size: 1
      overlap_grad_reduce: true
      overlap_param_gather: true
      gradient_accumulation_fusion: true
      use_torch_fsdp2: false
      use_distributed_optimizer: true

      # Data
      mock_data: true
      train_data_path: >
        /home/vanbhati@amd.com/Primus/data/fineweb-edu-10BT/HuggingFaceTokenizer/fineweb_edu_10BT_0_text_sentence
        /home/vanbhati@amd.com/Primus/data/fineweb-edu-10BT/HuggingFaceTokenizer/fineweb_edu_10BT_1_text_sentence
        /home/vanbhati@amd.com/Primus/data/fineweb-edu-10BT/HuggingFaceTokenizer/fineweb_edu_10BT_2_text_sentence
        /home/vanbhati@amd.com/Primus/data/fineweb-edu-10BT/HuggingFaceTokenizer/fineweb_edu_10BT_3_text_sentence
      valid_data_path: null
      test_data_path: null

      # Checkpoints
      finetune: false
      auto_continue_train: true
      load: null
      save: ./output/zebra_llama_1B_kda_pure-pretrain
      save_interval: 1000
      disable_last_saving: false
      ckpt_format: torch

      # Turbo
      enable_primus_turbo: false
      use_turbo_attention: false

      # Context parallel
      context_parallel_size: 1
