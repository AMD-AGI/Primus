# Primus Weekly Engineering Report — 2026-W20

## 1. Time Window

- Start: Monday 2026-05-11 00:00:00 Asia/Shanghai (GMT+8)
- End: Friday 2026-05-15 09:01 Asia/Shanghai (GMT+8) (report generation time)
- Branch observed: `origin/main`

## 2. Executive Summary

- **7 PRs merged to `main`** in the weekly window (Mon 2026-05-11 00:00 GMT+8 → Fri 2026-05-15 09:01 GMT+8).
- Category breakdown: **Performance Optimization: 1**, **Refactor: 1**, **Bug Fix: 1**, **Other: 4**; Turbo/Dependency Version Update, CI/Infra, Docs: 0.
- **No backend dependency pin change this week.** `PRIMUS_TURBO_COMMIT`, `PRIMUS_TURBO_AITER_COMMIT`, and `TRITON_COMMIT` in both `.github/workflows/ci.yaml` and `.github/workflows/benchmark.yaml` are byte-identical to the W19 values (`ef5b58ea` / `857f4d15` / `88b227e2`); no commit in this window touches the workflow files, the docker build args, or `.gitmodules`.
- **Submodule SHAs unchanged.** `third_party/Megatron-LM` is still pinned at `d3528a21` and `third_party/torchtitan` at `5fb7cc2e` on `origin/main`.
- **Megatron-LM upstream drift: `plan sync` (unchanged).** Pin is `d3528a21` (2026-03-06); upstream `main` HEAD is `266562f3` (2026-05-14) — **505 commits ahead** (+47 since the W19 snapshot `7cdf652c`). Notable new upstream activity in this window: `fully remove legacy code` (#4759), `Update transformer-engine dependency to version 2.15.0` (#4682), `build: upgrade mamba-ssm to 2.3.2.post1, causal-conv1d to 1.6.2.post1` (#4712), `Support recomputing in HybridModel` (#4496), `[training migration] Migrate mamba builder` (#4550), `Integrate LayerWiseDistributedOptimizer with DDP buffer infrastructure` (#4509), `NCCL UB fix: reduce memory cost and correctly deregister NCCL mem pool` (#4492), `One single flag that determines if we are in inference` (#4617), and `Add periodic GPU sniff tests to detect hardware stragglers` (#4662).
- **torchtitan upstream drift: `urgent sync` (unchanged).** Pin is `5fb7cc2e` (2025-10-15); upstream `main` HEAD is `a2a0d99e` (2026-05-14) — **644 commits ahead** (+34 since the W19 snapshot `4ebb0895`). Major new upstream activity in this window: `[GraphTrainer] Add Context Parallel support` (#3305), `[graph_trainer] AutoParallel AOT FX Trace Integration` (#2725), `[graph_trainer] Refactor passes.py into focused modules` (#3319), `[graph_trainer] Support multiple FSDP PGs in overlap_fsdp_ag_rs_pass` (#3351), `[graph_trainer] Generalize minimal_fx_tracer to module + optimizer roots` (#3164), `[graph_trainer] Refactor selective activation remat to in-place` (#3270), `Remove MoE expert for-loop fallback` (#3308), `[Quantization] Use class factory for MXFP8/Float8 GroupedExperts to fix _owner` (#3251), `[LoRA] Add LoRA converter and rename quantization to converters` (#3264), `[rl] switch to vllm v2 engine` (#3330), and `RL: observability spans across trainer/generator/controller` (#3234).
- **Primus-Turbo monthly drift: `monitor` (no further movement this week).** May month-start CI pin is `333b68d7` (2026-03-27); current CI pin is `ef5b58ea` (2026-04-29) — **+32 commits**. May month-start benchmark pin is `a4488f6c` (2026-03-19); current benchmark pin is `ef5b58ea` (2026-04-29) — **+37 commits**. The Turbo bump itself landed in W19 (PR #694); no further Turbo/AITER/Triton movement in W20.
- **Turbo gating refactor on the Megatron backend (#722):** unifies all Primus-Turbo patch gating behind a single `enable_primus_turbo` eligibility helper, moves `fp4_patches.py` and `fp8_patches.py` under `primus/backends/megatron/patches/turbo/` so they are activated by the same Turbo conditions as the rest of the Turbo patch set, adds a TE FP8 blockwise-scaling support override that lets HIP/ROCm environments use the Primus implementation path (`primus/backends/transformer_engine/pytorch/fp8.py`), and rewires the existing Turbo TE-spec-provider, GPT output-layer, MoE dispatcher, and RMSNorm patches to consume the shared helper.
- **Pipeline-parallel projection + zero-bubble GEMM rewrite (#644):** large change to the projection / pipeline / TE-GEMM stack — adds a `te_wgrad_store` Megatron extension, expands `zbpp_gemm.py` (~+700 lines) for zero-bubble PP, threads a wgrad split through the Primus pipeline launcher / handlers and `primuspipe`, refreshes the projection memory/perf models (`primus/core/projection/**`), and adds `legacy_grouped_mlp_wgrad_patches.py` plus updated `general_gemm_workspace_patches.py` and `te_wgrad_split_patches.py` to align with the current TE general-gemm API.
- **Hybrid-model TFLOPS patch (#594):** adds `primus/backends/megatron/patches/zebra_llama_flops_patches.py` (~+174 lines) to correct hybrid (Mamba-style) TFLOPS accounting, ships an MI355X `mamba_370M-pretrain.yaml`, and updates the existing MI300X `mamba_370M-pretrain.yaml`.
- **MaxText multi-node dependency installer fix (#725):** rewrites `runner/helpers/hooks/train/pretrain/maxtext/prepare.py` so the RDMA / IB / verbs / netlink apt install only runs on multi-node JAX/MaxText launches (`NNODES > 1`), uses `dpkg-query` to skip already-installed packages, gates the package list on Ubuntu 22.04 vs 24.04 (`libibnetdisc5` vs `libibnetdisc5t64`), and downgrades apt failures from fatal exits to warnings so single-node runs no longer break on minimal images without apt connectivity.
- **TraceLens + MLflow integration (#439):** adds `primus/backends/megatron/training/mlflow_artifacts.py` (~+1.2k lines) and `mlflow_setup.py` plus `primus/core/utils/rocm_mem_info.py`, wires `mlflow_upload_tracelens_report` / `mlflow_tracelens_output_format` config knobs through `primus_megatron_module.yaml` and the Megatron trainer, auto-installs TraceLens from GitHub on demand, and uploads generated XLSX/CSV reports to MLflow `artifacts/trace_analysis/`. Adds `tests/backends/megatron/test_mlflow_artifacts.py` (~+660 lines) and `test_mlflow_setup.py`.
- **New model coverage:** PR #709 lands `qwen3_5_35B_A3B` on Megatron with MI300X/MI355X BF16 + FP8 example pretrain configs (4 yamls) and a Primus model config (`primus/configs/models/megatron/qwen3_5_35B_A3B.yaml`), bumps `runner/helpers/hooks/train/pretrain/megatron/requirements-megatron.txt`, and extends `tests/trainer/test_megatron_trainer.py`. PR #662 refreshes the MI355X `glm5`, `minimax_m2.5` BF16/FP8 pretrain configs and adds `examples/moe_package/run_glm5_4layers_proxy.sh` plus a docs update for the unified Primus CLI ROCm tech blog.
- **No backend-gap report regenerated this week.** No submodule SHA changed (`third_party/Megatron-LM`, `third_party/torchtitan`, `third_party/Megatron-Bridge`, `third_party/Emerging-Optimizers`, `third_party/HummingbirdXT`, `third_party/maxtext` are all unchanged on `origin/main`); no `PRIMUS_TURBO_*` or `TRITON_COMMIT` value moved; no other tracked backend-version source moved in the weekly window. The existing torchtitan baseline report (`5fb7cc2e` vs upstream `main`) is therefore unaffected.

## 3. Weekly PR Update Table

| PR | Merged Time (GMT+8) | Category | Key Update |
| --- | --- | --- | --- |
| [#662](https://github.com/AMD-AGI/Primus/pull/662) `update glm5_fp8,minimax_m2.5_fp8,minimax_m2.5_bf16, parameter optim config` (author: `botaohu001`) | 2026-05-11 11:32 | Other | Refreshes MI355X pretrain example configs for `glm5` (BF16, FP8), `minimax_m2.5` (BF16, FP8) with updated parallelism / optimizer parameters; adds `examples/moe_package/run_glm5_4layers_proxy.sh` (4-layer GLM5 proxy launch script); updates `docs/tech_blogs/primus_cli_unified_entry_rocm.md`. No submodule, no Turbo pin, no Megatron-LM/torchtitan SHA touched. |
| [#709](https://github.com/AMD-AGI/Primus/pull/709) `add qwen3.5-35b-a3b` (author: `Rongzhang Zheng`) | 2026-05-11 13:32 | Other | Lands initial Qwen3.5 35B A3B Megatron support: `primus/configs/models/megatron/qwen3_5_35B_A3B.yaml` plus four new MI300X/MI355X BF16/FP8 example pretrain configs (`examples/megatron/configs/{MI300X,MI355X}/qwen3_5_35B_A3B-{BF16,FP8}-pretrain.yaml`). Bumps `runner/helpers/hooks/train/pretrain/megatron/requirements-megatron.txt` and extends `tests/trainer/test_megatron_trainer.py` with the new model entry. |
| [#644](https://github.com/AMD-AGI/Primus/pull/644) `Projection Accuracy, communication model core changes and more optimizations added` (author: `Anshu Raina`) | 2026-05-11 16:32 | Performance Optimization | Large pipeline-parallel + projection + TE-GEMM rewrite. Adds `primus/backends/megatron/core/extensions/te_wgrad_store.py`, expands `zbpp_gemm.py` (~+700 lines) for zero-bubble PP, threads a wgrad split through Primus pipeline launcher / `primuspipe` handlers (`bwd_handler`, `combined_fwd_bkwd_handler`, `communication_handler`, `wgrad_handler`), refreshes Megatron `tensor_parallel/layers.py`, `parallel_state.py`, `fp8_utils.py`, and `transformer_engine_spec_provider.py`. Adds `legacy_grouped_mlp_wgrad_patches.py` and updates `general_gemm_workspace_patches.py`, `te_wgrad_split_patches.py`, `v_schedule_patches.py`. Refreshes the projection memory + performance models under `primus/core/projection/**` (`memory_projection`, `performance_projection`, `module_profilers/{collective_args,collective_model,language_model,loss}.py`) and the `primus projection` CLI subcommand. Updates `.skills/primus-projection.md`. |
| [#594](https://github.com/AMD-AGI/Primus/pull/594) `[Megatron-LM] Create patch to update hybrid models TFLOPS calculation` (author: `clairesonglee`) | 2026-05-12 11:06 | Other | Adds `primus/backends/megatron/patches/zebra_llama_flops_patches.py` (~+174 lines) to fix the hybrid (Mamba-style) TFLOPS accounting on top of upstream Megatron, lands a new MI355X `mamba_370M-pretrain.yaml`, and updates the existing MI300X `mamba_370M-pretrain.yaml`. Single-target Megatron patch; no submodule bump. |
| [#722](https://github.com/AMD-AGI/Primus/pull/722) `[Megatron-LM] feat: unify enable_primus_turbo flag, add blockwise scaling patch` (author: `RuibinCheung`) | 2026-05-14 09:12 | Refactor | Unifies Primus-Turbo patch gating: relocates `primus/backends/megatron/patches/{fp4,fp8}_patches.py` under the `turbo/` subpackage so they are activated by the same Primus-Turbo conditions as the rest of the Turbo patch set; adds `primus/backends/megatron/patches/turbo/utils.py` with shared eligibility helpers for `enable_primus_turbo`, `primus_turbo` availability, and unsupported TP configurations; adds `primus/backends/transformer_engine/pytorch/fp8.py` providing a TE FP8 blockwise-scaling support override that lets HIP/ROCm environments take the Primus implementation path; rewires existing `gpt_output_layer_patches.py`, `moe_dispatcher_patches.py`, `rms_norm_patches.py`, `te_spec_provider_patches.py` to consume the shared helper. |
| [#439](https://github.com/AMD-AGI/Primus/pull/439) `feat: Add TraceLens integration for trace analysis with MLflow upload` (author: `GP Huang`) | 2026-05-14 16:02 | Other | Adds `primus/backends/megatron/training/mlflow_artifacts.py` (~+1.2k lines) and `primus/backends/megatron/training/mlflow_setup.py` (+111 lines) plus `primus/core/utils/rocm_mem_info.py` (+48 lines). Wires `mlflow_upload_tracelens_report` and `mlflow_tracelens_output_format: all\|xlsx\|csv` config knobs through `primus/configs/modules/megatron/primus_megatron_module.yaml` and `primus/modules/trainer/megatron/trainer.py` (~+150 lines) plus `primus/backends/megatron/megatron_base_trainer.py` (~+50 lines). Auto-installs TraceLens from GitHub if missing, generates XLSX/CSV trace-analysis reports from PyTorch profiler output, and uploads them to MLflow `artifacts/trace_analysis/`. Adds `tests/backends/megatron/test_mlflow_artifacts.py` (~+660 lines) and `test_mlflow_setup.py` (~+51 lines). |
| [#725](https://github.com/AMD-AGI/Primus/pull/725) `fix(maxtext): process dependency install when using multi node` (author: `llying-001`) | 2026-05-14 17:29 | Bug Fix | Single-file rewrite of `runner/helpers/hooks/train/pretrain/maxtext/prepare.py` (`install_maxtext_dependencies`). Skips the RDMA / IB / verbs / netlink apt install entirely when `NNODES <= 1` (single-node runs no longer require apt connectivity); on multi-node runs uses `dpkg-query` to install only actually-missing packages; gates `libibnetdisc5` vs `libibnetdisc5t64` on Ubuntu 22.04 vs 24.04 (time_t transition); downgrades apt failures from fatal `log_error_and_exit` to warnings so a missing optional system library no longer brings down the whole training pipeline. |

## 4. Megatron-LM Drift Overview

- Upstream: `https://github.com/NVIDIA/Megatron-LM.git` (`main`)
- Pinned in Primus `main` (`third_party/Megatron-LM`): `d3528a21301db2d12e92912b3ec025dc8a2ed4d6` — *fix(moe): fix TE general_gemm API change (#3582)*, 2026-03-06
- Upstream `main` HEAD: `266562f32cc3fcee90fd706f2d2732791b8ba022` — *Update owners (#4794)* (2026-05-14)
- Last upstream functional change in this window: `98031e11` — *Bump nvidia-modelopt>=0.44.0 (#4803)* (2026-05-14)
- Commit gap: **upstream is 505 commits ahead of Primus pin** (+47 since the W19 snapshot `7cdf652c`).
- Submodule SHA on Primus side: unchanged in W20; last submodule SHA bump on `main` was `3bec9aa9` → `d3528a21` inside PR #654 (merged 2026-04-10).
- Recommendation: **plan sync** (unchanged from W17/W18/W19). Upstream this week landed the `fully remove legacy code` finalizer (#4759), bumped TE to 2.15.0 (#4682), and bumped `mamba-ssm`/`causal-conv1d` minor versions (#4712), all of which expand the structural delta but do not yet change Primus's overall sync risk profile this week.

### Notable upstream areas that have moved since the pin

- **Legacy code removal (finalizer)**: `fully remove legacy code` (#4759) and `remove dead manual_release_grads code path in 1F1B overlap schedule` (#4511) finalize the multi-week legacy purge that started with #4207 / #4322 in W19; the next Primus pin bump must drop any remaining references to the removed legacy paths.
- **Dependency / build pins**: TE 2.15.0 (#4682); `mamba-ssm` → `2.3.2.post1`, `causal-conv1d` → `1.6.2.post1` (#4712); widen `flashinfer-python<0.7.0` (#4700); bump `nvidia-modelopt>=0.44.0` (#4803, plus #4723 to 0.43); upgrade Gitlab CI base image to 26.04 pytorch (#4688, carried from W19).
- **Hybrid / Mamba / SSM**: `Support recomputing in HybridModel` (#4496); `[training migration] Migrate mamba builder` (#4550); `fix(fsdp): recognize legacy GDN TP metadata` (#4664); `[main] feat(moe): Support packed sequence for gated delta net (GDN)` (#2645); on top of W19's checkpoint conversion between `GPT_model` and `Hybrid_model` (#4482).
- **Inference / CUDA graphs / RL**: `One single flag that determines if we are in inference` (#4617); `Increase CG cover from max_requests to max_tokens` (#4214); `Fix recompute checkpointing + training CGs` (#3919); `Wire --rl-inference-parsers into MRL` (#4768); `Support transfomers 5.x.x for text generation server` (#4732); `Add LLaVA audio (sound) model support` (#4402).
- **Distributed / DDP / FSDP**: `Integrate LayerWiseDistributedOptimizer with DDP buffer infrastructure` (#4509); `NCCL UB fix: reduce memory cost and correctly deregister NCCL mem pool` (#4492); `Make weight and optimizer memory estimation take into account expert parallelism correctly` (#4687); `Add Python-side guardrail for HybridEP InfiniBand limit and rename seq_len` (#4094, then reverted #4718, then re-landed as `Add Python-side guardrail for DeepEP IB limits` #4719); `fix legacy torch save when tensor_model_parallel_size > expert_model_parallel_size * expert_tensor_parallel_size` (#4678).
- **Resiliency / observability**: `Add periodic GPU sniff tests to detect hardware stragglers` (#4662); `Add bump-base-image skill and update golden value comparison` (#4733); golden-value refreshes (#4703, #4706).
- **Tokenizer / data / experiment**: `fix tokenizers in respect to newer transformers` (#4608); `Use Protocols to type-check linear_proj submodules of Attention` (#3434); `Create a Protocol for the MLP layer of TransformerLayer` (#3435); `feat(gpt): add output postprocess hook` (#4686); `refit clean up and refactoring` (#4762); `Guard omegaconf imports` (#4685); `fix: use no_mask in local ViT layer spec` (#4395).

### Megatron-LM upstream feature delta table

| Area | New Upstream Capability | Evidence (PR/Commit) | Potential Impact to Primus |
| --- | --- | --- | --- |
| Legacy code removal (finalizer) | `fully remove legacy code` (closes the multi-week legacy purge);<br>`remove dead manual_release_grads code path in 1F1B overlap schedule` | NVIDIA/Megatron-LM #4759, #4511 | Must audit Primus patches under `primus/backends/megatron/**` that still reference any legacy `transformer`/`modules`/legacy GPT path before the next pin bump. |
| Dependency / build pins | TE → 2.15.0;<br>`mamba-ssm` → 2.3.2.post1;<br>`causal-conv1d` → 1.6.2.post1;<br>`flashinfer-python<0.7.0`;<br>`nvidia-modelopt>=0.44.0`;<br>Gitlab CI base image → 26.04 pytorch | NVIDIA/Megatron-LM #4682, #4712, #4700, #4803, #4688 | Upstream TE 2.15 is now the floor; Primus's TE-spec / FP8 paths (including this week's #722 blockwise-scaling override) must be re-validated against TE 2.15 before the next Megatron pin bump. The mamba-ssm / causal-conv1d minor bumps interact with this week's Primus hybrid TFLOPS patch (#594). |
| Hybrid / Mamba / SSM | `Support recomputing in HybridModel`;<br>`[training migration] Migrate mamba builder`;<br>`fix(fsdp): recognize legacy GDN TP metadata`;<br>`[main] feat(moe): Support packed sequence for gated delta net (GDN)` | NVIDIA/Megatron-LM #4496, #4550, #4664, #2645 | Reinforces the Hybrid/Mamba sync risk previously called out in W19; once Primus syncs, the new HybridModel recompute path interacts with the just-landed Primus hybrid TFLOPS patch (#594) and the W19 `[ssm]` extras split. |
| Inference / CUDA graphs / RL | `One single flag that determines if we are in inference`;<br>`Increase CG cover from max_requests to max_tokens`;<br>`Fix recompute checkpointing + training CGs`;<br>`Wire --rl-inference-parsers into MRL`;<br>`Support transfomers 5.x.x for text generation server`;<br>`Add LLaVA audio (sound) model support` | NVIDIA/Megatron-LM #4617, #4214, #3919, #4768, #4732, #4402 | Inference / post-train / RL paths for Primus DSV3/MoE configs continue to accumulate; the single inference flag (#4617) is a behavioral change Primus's Megatron trainer wrapper must consume on sync. |
| Distributed / DDP / FSDP | `Integrate LayerWiseDistributedOptimizer with DDP buffer infrastructure`;<br>`NCCL UB fix: reduce memory cost and correctly deregister NCCL mem pool`;<br>EP-aware optimizer-memory estimation;<br>`Add Python-side guardrail for DeepEP IB limits`;<br>FSDP/EP legacy torch-save fix | NVIDIA/Megatron-LM #4509, #4492, #4687, #4719, #4678 | Aligns conceptually with this week's Primus PP / projection / wgrad-store rewrite (#644) and with the Turbo gating refactor (#722); Primus's DDP/HybridEP coupling must be re-validated when Megatron is bumped. |
| Resiliency / observability | `Add periodic GPU sniff tests to detect hardware stragglers`;<br>`Add bump-base-image skill and update golden value comparison`;<br>multiple golden-value refreshes | NVIDIA/Megatron-LM #4662, #4733, #4703, #4706 | Reference patterns Primus can adopt for MI300X/MI355X cluster validation; Primus already invests in preflight (W19 #668) and could fold the upstream sniff tests into preflight. |
| Tokenizer / data / experiment | `fix tokenizers in respect to newer transformers`;<br>Protocol type-check for `linear_proj` and MLP submodules;<br>`feat(gpt): add output postprocess hook`;<br>`refit clean up and refactoring` | NVIDIA/Megatron-LM #4608, #3434, #3435, #4686, #4762 | Builds on the W19 Primus tokenizer/data alignment work (#675); the new tokenizer fix is the next upstream candidate Primus must re-validate at sync time. |

## 5. torchtitan Drift Overview

- Upstream: `https://github.com/pytorch/torchtitan.git` (`main`)
- Pinned in Primus `main` (`third_party/torchtitan`): `5fb7cc2e3bbb9b9dc0ab7af34ed5cc58b5f32021` — *Deepseek-V3 toml file minor fix (#1894)*, 2025-10-15
- Upstream `main` HEAD: `a2a0d99e333b27fe314359ff9c9d918a3c5363f6` — *RL: observability spans across trainer/generator/controller (#3234) (#3234)* (2026-05-14)
- Commit gap: **upstream is 644 commits ahead of Primus pin** (+34 since the W19 snapshot `4ebb0895`).
- Submodule SHA on Primus side: unchanged in W20. No Primus PR in this window touches `primus/backends/torchtitan/**`.
- Recommendation: **urgent sync** (unchanged from W17/W18/W19). The pin is now ~7 months stale; another wave of GraphTrainer / precompile / RL landings this week further widens the gap.

### Notable upstream areas that have moved since the pin

- **GraphTrainer / precompile**: `[GraphTrainer] Add Context Parallel support` (#3305); `[graph_trainer] AutoParallel AOT FX Trace Integration` (#2725); `[graph_trainer] Refactor passes.py into focused modules` (#3319); `[graph_trainer] Support multiple FSDP PGs in overlap_fsdp_ag_rs_pass` (#3351); `[graph_trainer] Generalize minimal_fx_tracer to module + optimizer roots` (#3164); `[graph_trainer] Refactor selective activation remat to in-place` (#3270); `[graph_trainer] Improve SAC tagging and CPU offload pass metadata` (#3321); `[graph_trainer] Remove deprecated AOT compile mode` (#3327); `[graph_trainer] Add log_timer utility for tracing step timing` (#3311); `[graph_trainer] Fix AutoParallel input_fn to include positions tensor` (#3315); `[GraphTrainer] Add compile.disable_passes to selectively skip graph passes` (#3273); `[cpu-offloading] Encode last consumer as dep arg in ao.wait` (#3333); on top of W19's GraphTrainer memory-policy registry + precompile bucketing pass (#3215, #3213).
- **MoE / quantization / LoRA**: `Remove MoE expert for-loop fallback` (#3308); `[Quantization] Use class factory for MXFP8/Float8 GroupedExperts to fix _owner` (#3251); `[LoRA] Add LoRA converter and rename quantization to converters` (#3264); `Merge lora and float8 tests` (#3310); on top of W19's MXFP8GroupedExperts swap fix (#3199), float8 GroupedExperts quantization (#3233), and AllToAllTokenDispatcher `sp_size` padding (#3193).
- **RL / vLLM**: `[rl] switch to vllm v2 engine` (#3330); `RL: observability spans across trainer/generator/controller` (#3234); `[rl] Register customized config parser to vllm + less vllm config dependency` (#3242); `[rl] Graceful shutdown GRPO` (#3238); on top of W19's 50s→15s vLLM compile-time win (#3145).
- **Loss / numerics**: `Make ChunkedCELoss support torch.autograd.grad` (#3249); `Remove unnecessary clone from ChunkedCELoss` (#3272); `Avoid repeat_interleave output-size sync` (#3274) (and a same-week revert #3335 + re-land); `[graph_trainer] Pass global_valid_tokens into loss_fn instead of dividing externally` (#3244).
- **Codex / weight tying / tokens**: `[codex] Fix weight tying parameter accounting` (#3225); `[codex] Fix shared BOS and EOS token inference` (#3279); `Add AGENTS.md symlinks for Codex usage` (#3326); on top of W19's Qwen3 MoE bitwise-deterministic tests + weight-tying gradient bug fix (#3174).
- **CI / observability**: `Enhance Lychee Link Checker (Resiliency & Performance)` (#3203); `Fix precompile tests` (#3316); `[graph_trainer] Disable flexattn integration test due to upstream nightly regression` (#3312); `profiler: drop duplicate dataclass fields` (#3227, carried from W19); structured-logging observability work continues (#3176, carried from W19).

### torchtitan upstream feature delta table

| Area | New Upstream Capability | Evidence (PR/Commit) | Potential Impact to Primus |
| --- | --- | --- | --- |
| GraphTrainer / precompile | Context-parallel support;<br>AutoParallel AOT FX trace integration;<br>passes.py refactor into focused modules;<br>multi-FSDP-PG `overlap_fsdp_ag_rs_pass`;<br>minimal_fx_tracer generalized to module + optimizer roots;<br>selective activation remat refactored in-place | pytorch/torchtitan #3305, #2725, #3319, #3351, #3164, #3270 | Major continuing GraphTrainer infrastructure; remains unavailable behind the stale pin. Will require coordinated patches in `primus/backends/torchtitan/**` when Primus syncs; the precompile + remat refactor is structurally invasive. |
| MoE / quantization / LoRA | Removed MoE expert for-loop fallback;<br>class factory for MXFP8/Float8 GroupedExperts (`_owner` fix);<br>LoRA converter + quantization → converters rename;<br>merged LoRA + float8 tests | pytorch/torchtitan #3308, #3251, #3264, #3310 | Useful for Primus MoE configs (DSV3, GPT-OSS, Qwen3) on the torchtitan backend; the LoRA → converters rename will require renaming any Primus-side imports targeting `quantization`. |
| RL / vLLM | vLLM v2 engine switch;<br>RL observability spans across trainer/generator/controller;<br>`expandable_segments:True` for monarch RDMA (carried);<br>vLLM config-parser registration + dependency reduction;<br>graceful GRPO shutdown | pytorch/torchtitan #3330, #3234, #3242, #3238 | Primus does not currently route RL through torchtitan, but the vLLM v2 engine + observability changes set the upstream RL baseline that any future Primus RL/torchtitan integration will need to match. |
| Loss / numerics | ChunkedCELoss `torch.autograd.grad` support;<br>ChunkedCELoss clone removal;<br>repeat_interleave output-size sync removal (with one revert + re-land);<br>`global_valid_tokens` plumbed into loss_fn | pytorch/torchtitan #3249, #3272, #3274/#3335, #3244 | Direct training-loop numerics changes; Primus loss/CE wrappers must be re-validated against the new ChunkedCELoss behavior on sync. |
| Codex / weight tying / tokens | `Fix weight tying parameter accounting`;<br>`Fix shared BOS and EOS token inference`;<br>AGENTS.md symlinks for Codex | pytorch/torchtitan #3225, #3279, #3326 | Touches the same code paths as the W19 Qwen3 weight-tying gradient bug fix (#3174); Primus Qwen3 patches need to be re-validated against the param-accounting fix. |
| CI / observability | Lychee link checker enhanced;<br>precompile tests fix;<br>flexattn integration test disabled for nightly cuDNN regression;<br>profiler duplicate dataclass fields removed | pytorch/torchtitan #3203, #3316, #3312, #3227 | Reference patterns for Primus torchtitan CI; the disabled flexattn integration test signals an upstream nightly regression Primus should track when planning the torchtitan sync. |

## 6. Primus-Turbo Monthly Drift Overview

- Drift type: **in-repo**, not upstream — compares the Turbo version/SHA referenced on Primus `main` now vs the latest commit at or before `month_start_ts = 2026-05-01 00:00 Asia/Shanghai` (`2026-04-30 16:00 UTC`).
- Turbo is **not a submodule** in Primus. Canonical version source:
  - `.github/workflows/ci.yaml` → `PRIMUS_TURBO_COMMIT`, `PRIMUS_TURBO_AITER_COMMIT`, `TRITON_COMMIT` (also wired through `.github/workflows/docker/Dockerfile`)
  - `.github/workflows/benchmark.yaml` → `PRIMUS_TURBO_COMMIT`
- Reference Primus commit at month start (May) on `main`: `0a25e20030b38cb46f840f0971c8ed97746eae4c` (*fix: keep legacy groupedgemm on megatron backend (#693)*, 2026-04-28 15:27 GMT+8). This is the latest `main` commit at or before 2026-05-01 00:00 GMT+8 — the Turbo bump in W19 (#694, merged 2026-05-07) had not yet landed.
- Current state on `origin/main` (W20):
  - `ci.yaml` `PRIMUS_TURBO_COMMIT`: `ef5b58ea3de0a2956d57dba518be466b7a092442` — *feat(grouped_gemm): switch default backend to Triton for BF16 and FP8 (#320)*, 2026-04-29
  - `ci.yaml` `PRIMUS_TURBO_AITER_COMMIT`: `857f4d15775a29af153a2c68a2f8e8a8d696c986`
  - `ci.yaml` `TRITON_COMMIT`: `88b227e23f0445f3f695bad05bbf1a363b4f50e0`
  - `benchmark.yaml` `PRIMUS_TURBO_COMMIT`: `ef5b58ea3de0a2956d57dba518be466b7a092442`
- Month-start (2026-05-01) state on `main`:
  - `ci.yaml` `PRIMUS_TURBO_COMMIT`: `333b68d7c81b722b21b4aad10cd250c45f15027c` — *fix sm_scale none bug (#263)*, 2026-03-27
  - `ci.yaml` `PRIMUS_TURBO_AITER_COMMIT`: `e83f9903c07001a0ec29e85d223f6e6cdbe00859`
  - `benchmark.yaml` `PRIMUS_TURBO_COMMIT`: `a4488f6cdb15cfff4383c61af7922bb50803f0ea` — *feat: update triton impl for mi300 & mi355 (#252)*, 2026-03-19
  - `ci.yaml` `TRITON_COMMIT`: not present (introduced by W19 PR #694).
- **Primus-Turbo pin did not move further this week.** CI pin remains 32 commits ahead of May month-start; benchmark pin remains 37 commits ahead of May month-start; AITER and Triton pins remain at the W19 values. The two YAML pins are still in sync (CI and benchmark both `ef5b58ea`).
- Recommendation: **monitor**. Track the next Turbo upstream bump and continue validating the Triton-backed grouped-gemm default path on Primus MoE configs; this week's Primus-side Turbo gating refactor (#722) consolidates eligibility checks but does not move the Turbo pin itself.

### Notable areas changed since month start

- **Grouped GEMM default backend (carry-over)**: Primus-Turbo #320 switches the default grouped-gemm backend to Triton for BF16 and FP8; combined with `TRITON_COMMIT=88b227e` source build, this remains the kernel path used by Primus-Turbo's `TEGroupedMLP` integration on the Megatron backend. This week's Primus #722 unifies all Turbo patch gating behind `enable_primus_turbo` so the same Turbo conditions cover the moved `fp4_patches.py` / `fp8_patches.py` paths.
- **Attention `torch.compile` wrappers (carry-over)**: Primus-Turbo #310 adds `torch.compile` custom wrappers for the attention path; relevant for both the Megatron backend and the W19 torchtitan Qwen3 attention adapter (Primus #694).
- **MoE EPBackend Protocol + `EPBufferConfig` (carry-over)**: Primus-Turbo #297 refactors the dispatcher around an extensible `EPBackend` Protocol with an explicit `EPBufferConfig`; Primus's `PrimusTurboDeepEPTokenDispatcher` (`primus/backends/megatron/core/transformer/moe/`) consumes this surface.
- **Attention layouts (carry-over)**: Primus-Turbo #275 (sbhd) and #304 (bhsd) extend attention layout coverage; aligns with the W19 Primus Qwen3 attention adapter.
- **Symmetric Memory rewrite (carry-over)**: Primus-Turbo #276 replaces `hip-python` and `AMDSymmetricMemory` with a new `SymmetricMemory`; relevant when running DeepEP/MoE paths on ROCm.
- **MXFP8/FP8 quantization fixes (carry-over)**: Primus-Turbo #298, #306, #307, #308 tighten correctness on FP8 paths used by Primus MoE configs; this week's Primus #722 also adds a TE FP8 blockwise-scaling support override (`primus/backends/transformer_engine/pytorch/fp8.py`) that complements the Turbo FP8 path on HIP/ROCm.
- **AITER pin (carry-over)**: `PRIMUS_TURBO_AITER_COMMIT` remains at `857f4d15` (no movement in W20).
- **Triton built from source (carry-over)**: `TRITON_COMMIT=88b227e` remains wired through `ci.yaml`; the Dockerfile still clones `triton-lang/triton@release/3.7.x`, checks out `${TRITON_COMMIT}`, and `pip install --no-build-isolation -v .` to compile Triton from source.

### Primus-Turbo monthly drift table

| Component | Current Version/SHA | Month-start Version/SHA | Delta Summary | Key Changes | Evidence |
| --- | --- | --- | --- | --- | --- |
| `PRIMUS_TURBO_COMMIT` (CI build) | `ef5b58ea3de0a2956d57dba518be466b7a092442` (*feat(grouped_gemm): switch default backend to Triton for BF16 and FP8 (#320)*, 2026-04-29) | `333b68d7c81b722b21b4aad10cd250c45f15027c` (*fix sm_scale none bug (#263)*, 2026-03-27) | **+32 commits** (no further movement in W20) | Grouped-GEMM default → Triton (#320);<br>`Torch.compile` attention wrappers (#310);<br>EPBackend Protocol + EPBufferConfig refactor (#297);<br>SymmetricMemory rewrite (#276);<br>MXFP8/FP8 quant fixes (#306, #307, #308);<br>`deepep_use_comm_stream` re-introduction (#314). | [`.github/workflows/ci.yaml` L17](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/ci.yaml#L17), [Primus-Turbo compare](https://github.com/AMD-AGI/Primus-Turbo/compare/333b68d7c81b722b21b4aad10cd250c45f15027c...ef5b58ea3de0a2956d57dba518be466b7a092442) |
| `PRIMUS_TURBO_AITER_COMMIT` (CI build) | `857f4d15775a29af153a2c68a2f8e8a8d696c986` | `e83f9903c07001a0ec29e85d223f6e6cdbe00859` | **Pin advanced in W19** (no movement in W20) | AITER pin advanced by W19 PR #694; build pulls AITER at the new SHA inside the docker image. | [`.github/workflows/ci.yaml` L18](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/ci.yaml#L18) |
| `PRIMUS_TURBO_COMMIT` (benchmark) | `ef5b58ea3de0a2956d57dba518be466b7a092442` (*feat(grouped_gemm): switch default backend to Triton for BF16 and FP8 (#320)*, 2026-04-29) | `a4488f6cdb15cfff4383c61af7922bb50803f0ea` (*feat: update triton impl for mi300 & mi355 (#252)*, 2026-03-19) | **+37 commits** (no further movement in W20) | Same set of upstream changes as the CI pin plus the earlier `a4488f6c`→`333b68d7` interval; benchmark and CI pins remain in sync. | [`.github/workflows/benchmark.yaml` L9](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/benchmark.yaml#L9), [Primus-Turbo compare](https://github.com/AMD-AGI/Primus-Turbo/compare/a4488f6cdb15cfff4383c61af7922bb50803f0ea...ef5b58ea3de0a2956d57dba518be466b7a092442) |
| `TRITON_COMMIT` (CI build) | `88b227e23f0445f3f695bad05bbf1a363b4f50e0` (`triton-lang/triton@release/3.7.x`) | not present | **Introduced in W19** (no movement in W20) | Compiles Triton from source inside the docker image; wired through both the main and JAX docker builds. | [`.github/workflows/ci.yaml` L21](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/ci.yaml#L21), [`Dockerfile`](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/docker/Dockerfile) |

## 7. Source Links

- Primus main branch: https://github.com/AMD-AGI/Primus/tree/main
- Primus weekly PR listing (window): https://github.com/AMD-AGI/Primus/pulls?q=is%3Apr+is%3Amerged+base%3Amain+merged%3A%3E%3D2026-05-10T16%3A00%3A00Z
- PR #725 (fix(maxtext): process dependency install when using multi node): https://github.com/AMD-AGI/Primus/pull/725
- PR #439 (feat: Add TraceLens integration for trace analysis with MLflow upload): https://github.com/AMD-AGI/Primus/pull/439
- PR #722 (`[Megatron-LM] feat: unify enable_primus_turbo flag, add blockwise scaling patch`): https://github.com/AMD-AGI/Primus/pull/722
- PR #594 (`[Megatron-LM] Create patch to update hybrid models TFLOPS calculation`): https://github.com/AMD-AGI/Primus/pull/594
- PR #644 (`Projection Accuracy, communication model core changes and more optimizations added`): https://github.com/AMD-AGI/Primus/pull/644
- PR #709 (`add qwen3.5-35b-a3b`): https://github.com/AMD-AGI/Primus/pull/709
- PR #662 (`update glm5_fp8,minimax_m2.5_fp8,minimax_m2.5_bf16, parameter optim config`): https://github.com/AMD-AGI/Primus/pull/662
- Megatron-LM pin: https://github.com/NVIDIA/Megatron-LM/commit/d3528a21301db2d12e92912b3ec025dc8a2ed4d6
- Megatron-LM upstream HEAD (at report time): https://github.com/NVIDIA/Megatron-LM/commit/266562f32cc3fcee90fd706f2d2732791b8ba022
- Megatron-LM compare: https://github.com/NVIDIA/Megatron-LM/compare/d3528a21301db2d12e92912b3ec025dc8a2ed4d6...main
- torchtitan pin: https://github.com/pytorch/torchtitan/commit/5fb7cc2e3bbb9b9dc0ab7af34ed5cc58b5f32021
- torchtitan upstream HEAD (at report time): https://github.com/pytorch/torchtitan/commit/a2a0d99e333b27fe314359ff9c9d918a3c5363f6
- torchtitan compare: https://github.com/pytorch/torchtitan/compare/5fb7cc2e3bbb9b9dc0ab7af34ed5cc58b5f32021...main
- Primus-Turbo (current shared) pin: https://github.com/AMD-AGI/Primus-Turbo/commit/ef5b58ea3de0a2956d57dba518be466b7a092442
- Primus-Turbo CI pin (month-start → W20) compare: https://github.com/AMD-AGI/Primus-Turbo/compare/333b68d7c81b722b21b4aad10cd250c45f15027c...ef5b58ea3de0a2956d57dba518be466b7a092442
- Primus-Turbo benchmark pin (month-start → W20) compare: https://github.com/AMD-AGI/Primus-Turbo/compare/a4488f6cdb15cfff4383c61af7922bb50803f0ea...ef5b58ea3de0a2956d57dba518be466b7a092442
- Triton release branch (current pin): https://github.com/triton-lang/triton/commit/88b227e23f0445f3f695bad05bbf1a363b4f50e0
- Month-start reference commit on `main` (May): https://github.com/AMD-AGI/Primus/commit/0a25e200
- Last week's report (W19): https://github.com/AMD-AGI/Primus/blob/main/docs/weekly_reports/2026-W19-primus-weekly.md

---

*Generated automatically by the Primus weekly report automation. Factual statements are derived from `git log origin/main`, the pinned submodule SHAs in `third_party/`, and the `PRIMUS_TURBO_COMMIT` / `PRIMUS_TURBO_AITER_COMMIT` / `TRITON_COMMIT` values in `.github/workflows/{ci,benchmark}.yaml` as observed at 2026-05-15 09:01 GMT+8. Upstream-HEAD SHAs and commit counts are snapshots at report generation time.*
