# Primus Weekly Engineering Report — 2026-W22

## 1. Time Window

- Start: Monday 2026-05-25 00:00:00 Asia/Shanghai (GMT+8)
- End: Friday 2026-05-29 09:01 Asia/Shanghai (GMT+8) (report generation time)
- Branch observed: `origin/main`

## 2. Executive Summary

- **4 PRs merged to `main`** in the weekly window (Mon 2026-05-25 00:00 GMT+8 → Fri 2026-05-29 09:01 GMT+8).
- Category breakdown: **Bug Fix: 2**, **CI/Infra: 1**, **Other: 1**; Performance Optimization, Turbo/Dependency Version Update, Refactor, Docs: 0.
- **No backend dependency pin change this week.** `PRIMUS_TURBO_COMMIT`, `PRIMUS_TURBO_AITER_COMMIT`, `TRITON_COMMIT`, `ROCSHMEM_COMMIT`, and `UCCL_COMMIT` in `.github/workflows/ci.yaml` and `.github/workflows/benchmark.yaml` are unchanged from `origin/main` HEAD prior to the window. The last Primus-Turbo pin bump on `main` was [PR #723](https://github.com/AMD-AGI/Primus/pull/723) (merged 2026-05-15, before this window) which moved `PRIMUS_TURBO_COMMIT` to `06b8d3fe` and reverted `PRIMUS_TURBO_AITER_COMMIT` to `b5e03ed1`.
- **Submodule SHAs unchanged.** `third_party/Megatron-LM` is still pinned at `d3528a21` and `third_party/torchtitan` at `5fb7cc2e` on `origin/main`. No `third_party/*` SHA changed in the window.
- **Megatron-LM upstream drift: `plan sync` (unchanged).** Pin is `d3528a21` (2026-03-06); upstream `main` HEAD is `ba9e0eaf` (2026-05-28) — **603 commits ahead** (+145 since the W19 snapshot `7cdf652c`). New upstream activity in this window: switch DSv3 on H100 to HybridEP (#5039), thread `position_ids` through `MimoModel` for multimodal RoPE (#4938), Megatron-FSDP full-iteration CUDA graphability (#4663), `Drain predecessor reduce-scatter at dispatch time` (#4940), `Paged Stashing` (#4247), MTP-stage `word_embeddings` correctness tag (#5034), DDP wrap pg-size fixes (#5006), `Release MTP assertion when EP overlap with PP=1` (#4796), TE 2.15 compatibility fix in cuda_graphs (#4874), `Add TEFusedDenseMLP for Dense+Grouped GEMM fusion` (#4786), MTP recompute crash fix with packed sequences (#4593), `MimoOptimizer` dist-ckpt save/load fix for non-colocated MIMO (#4801), and an inference perf-test harness for GPT-583M / hybrid-2B (#4806).
- **torchtitan upstream drift: `urgent sync` (unchanged).** Pin is `5fb7cc2e` (2025-10-15); upstream `main` HEAD is `92abc88e` (2026-05-28) — **680 commits ahead** (+70 since the W19 snapshot `4ebb0895`). New upstream activity in this window: RoPE refactor using model's `max_sequence_length` as upper bound (#3395, follow-up fix #3448), MoE series `[MoE][5/n]` clean DTensor boundaries (#3386), `[MoE][6/n]` extract local_reorder + split DeepEP/HybridEP dispatchers (#3389), `[MoE][7/n]` keep 3D tensors through MoE (#3423), `[MoE][8/n]` shape suffix for MoE (#3425), `[MoE][SAC]` deterministic ops in MoE routing (#3146), `[graph_trainer]` separate EP process groups for overlap (#3369), `[graph_trainer]` DeepSeek V3 16B SDPA config (#3361), `[graph_trainer]` rework `full_inductor_compilation_pass` via `regional_inductor` (#3346), FSDP symmetric-memory configuration (#3105), `[Module]` replace `from_nn_module` with native Module subclasses (#3398), `[rl]` add Batcher in RL Loop (#3347), and `[loss]` fix `ChunkedCELoss` + TP gradient placement mismatch (#3412).
- **Primus-Turbo monthly drift: `monitor` (no movement this week).** The May month-start pins (2026-05-01 00:00 GMT+8) were `PRIMUS_TURBO_COMMIT=333b68d7` (CI) / `a4488f6c` (benchmark) and `PRIMUS_TURBO_AITER_COMMIT=e83f9903`. The current pins on `origin/main` are `06b8d3fe` (CI and benchmark) — **+39 commits** vs CI month-start, **+44 commits** vs benchmark month-start — and `PRIMUS_TURBO_AITER_COMMIT=b5e03ed1`. The headline upstream changes between month start and the current pin are `Add HYBRID FP8 format support for Triton backend in gemm and grouped_gemm` ([Primus-Turbo #278](https://github.com/AMD-AGI/Primus-Turbo/pull/278)), `fix: remove attn workaround` (#313), `fix: hipblaslt_gemm correct workspace size for MI300X` (#333), `[Attention] Add flash_attn_varlen_func with aiter backend` (#332), `fix(grouped_gemm): bind per-stream hipBLASLt handles to fix bf16 multi-stream stall on gfx942` (#319), `fix: fix deepep combine hang` (#327), and `feat(grouped_gemm): switch default backend to Triton for BF16 and FP8` (#320). The pins did **not** move this week; the +39/+44 in-repo drift is carried over from the W21 bump in PR #723 (2026-05-15) and the prior W19 bump in PR #694 (2026-05-07).
- **Deterministic-mode hardening landed (#731):** `PrimusTurboAttention.__init__` now captures `args.deterministic_mode` and forwards it to the underlying flash-attention call instead of the hard-coded `deterministic=False`; the Megatron-side deterministic-mode pre-flight checker (`validate_args_on_rocm`) is refactored to validate from a single `NON_DETERMINISTIC_ENVS` mapping and now also requires `PRIMUS_TURBO_AUTO_TUNE=0` and `PRIMUS_DETERMINISTIC=1` in addition to the previously checked `TORCH_COMPILE_DISABLE` and `ROCBLAS_DEFAULT_ATOMICS_MODE`; `runner/helpers/envs/base_env.sh` automatically exports `PRIMUS_TURBO_AUTO_TUNE=0` when `PRIMUS_DETERMINISTIC=1`.
- **Modern CLI override parser fix (#732):** the modern CLI override parser used `ast.literal_eval` only for booleans/numerics, so `--profile_ranks '[0]'` was passed downstream as the string `[0]` (breaking Megatron's `int in args.profile_ranks` check). The parser now safely `literal_eval`s values that start with `[`, `(`, `{`, or equal the literal `None`, so they are converted to real `list`/`tuple`/`dict`/`set`/`None` objects. New dedicated test file `tests/unit_tests/core/utils/test_arg_utils.py` covers bool/int/float/string/list/dict/None and `parse_cli_overrides`.
- **Megatron gpt_oss_20B sink-attention trainer test added (#727):** new Megatron trainer functional test for `gpt_oss_20B` with sink attention enabled, using `micro_batch_size=2` (`global_batch_size=16`) so BSHD/SBHD transpose regressions in the Primus Turbo attention path are not masked by `micro_batch_size=1` broadcasting.
- **MLflow artifact upload added (#440):** profiler trace files (`tensorboard_dir`) and training log files (`exp_root_path/logs/`) are now automatically uploaded as MLflow artifacts (`artifacts/traces/`, `artifacts/logs/`) at the end of training when MLflow tracking is enabled. New runner config options `mlflow_upload_traces` and `mlflow_upload_logs`; MLflow environment variables are passed through into the docker container; `tensorboard_dir` uses a unique timestamp-based suffix for multi-node consistency.
- **No backend-gap report regenerated this week.** No submodule SHA changed (`third_party/Megatron-LM`, `third_party/torchtitan`, `third_party/Megatron-Bridge`, `third_party/Emerging-Optimizers`, `third_party/HummingbirdXT`, `third_party/maxtext` are all unchanged on `origin/main`), and no tracked-config backend version pin changed in this window.

## 3. Weekly PR Update Table

| PR | Merged Time (GMT+8) | Category | Key Update |
| --- | --- | --- | --- |
| [#731](https://github.com/AMD-AGI/Primus/pull/731) `feat: add turbo attn deterministic support and refine env checker` (author: `RuibinCheung`) | 2026-05-26 10:41 | Bug Fix | Wires `args.deterministic_mode` end-to-end through `PrimusTurboAttention` so the underlying flash-attention call honors deterministic mode instead of the previous hard-coded `deterministic=False`. Refactors `validate_args_on_rocm` in `primus/modules/trainer/megatron/utils.py` to drive deterministic-mode env validation from a single `NON_DETERMINISTIC_ENVS` mapping and extends the required set from `{TORCH_COMPILE_DISABLE, ROCBLAS_DEFAULT_ATOMICS_MODE}` to also include `PRIMUS_TURBO_AUTO_TUNE=0` and `PRIMUS_DETERMINISTIC=1`, with clearer assertion messages reporting both the expected and the actual value. `runner/helpers/envs/base_env.sh` automatically exports `PRIMUS_TURBO_AUTO_TUNE=0` when `PRIMUS_DETERMINISTIC=1`; `runner/helpers/hooks/05_deterministic.sh` logs the newly added variable so the experiment env dump reflects the actual deterministic configuration. |
| [#727](https://github.com/AMD-AGI/Primus/pull/727) `test(megatron): add gpt_oss_20B sink attention trainer case` (author: `WangLingxun`) | 2026-05-26 09:53 | CI/Infra | Adds a new Megatron trainer functional test for `gpt_oss_20B` with sink attention enabled. Uses `micro_batch_size=2` (`global_batch_size=16`) deliberately so that BSHD/SBHD transpose regressions in the Primus Turbo attention path are not masked by `micro_batch_size=1` broadcasting. |
| [#732](https://github.com/AMD-AGI/Primus/pull/732) `fix(arg_utils): parse list/dict/None literals in modern CLI overrides` (author: `WangLingxun`) | 2026-05-26 09:41 | Bug Fix | Modern CLI override parser previously kept container/None literals as raw strings, so e.g. `--profile_ranks '[0]'` was forwarded as the string `[0]` instead of the Python list `[0]`, breaking downstream consumers such as Megatron's `int in args.profile_ranks` check. The parser now safely uses `ast.literal_eval` on values that start with `[`, `(`, `{`, or equal the exact literal `None`, returning real `list`/`tuple`/`dict`/`set`/`None` objects. Adds a dedicated new test file `tests/unit_tests/core/utils/test_arg_utils.py` covering bool (case-insensitive), int, float, string fallback, list/dict/None literals, and `parse_cli_overrides` end-to-end behavior. |
| [#440](https://github.com/AMD-AGI/Primus/pull/440) `feat: Add MLflow artifact upload for traces and logs` (author: `gphuang`) | 2026-05-26 09:40 | Other | Adds automatic upload of PyTorch profiler trace files (`tensorboard_dir`) and training log files (`exp_root_path/logs/`) to MLflow under `artifacts/traces/` and `artifacts/logs/` at the end of training when MLflow tracking is enabled. New runner config options `mlflow_upload_traces` and `mlflow_upload_logs`. MLflow environment variables are passed through into the docker container; `tensorboard_dir` uses a unique timestamp-based suffix for multi-node consistency. Single-node and multi-node runs both upload artifacts correctly per the PR's MLflow run links. |

## 4. Megatron-LM Drift Overview

- Upstream: `https://github.com/NVIDIA/Megatron-LM.git` (`main`)
- Pinned in Primus `main` (`third_party/Megatron-LM`): `d3528a21301db2d12e92912b3ec025dc8a2ed4d6` — *fix(moe): fix TE general_gemm API change (#3582)*, 2026-03-06
- Upstream `main` HEAD: `ba9e0eaffd2a00ef475c5025f800a394f169a245` — *Fix: Import unwrap_model from megatron.core.utils in modelopt examples (#5045)* (2026-05-28)
- Commit gap: **upstream is 603 commits ahead of Primus pin** (+145 since the W19 snapshot `7cdf652c`).
- Submodule SHA on Primus side: unchanged in this window; last submodule SHA bump on `main` was `3bec9aa9` → `d3528a21` inside PR #654 (merged 2026-04-10).
- Recommendation: **plan sync** (unchanged from W17/W18/W19). Upstream continues to land MoE/HybridEP, Megatron-FSDP, inference perf and TE 2.15 compatibility work, growing the eventual sync diff but not changing Primus's overall sync risk profile this week.

### Notable upstream areas that have moved since the pin

- **MoE / EP / HybridEP**: Switch DSv3 on H100 to HybridEP (#5039); `Drain predecessor reduce-scatter at dispatch time` (#4940); `Release MTP assertion when EP overlap with PP=1` (#4796); thread custom process groups through MoE grad finalization (#4782); on top of W19's vLLM grouped-gemm MoE inference backend (#4566) and HybridEP DDP-hook fix (#4500).
- **Megatron-FSDP / param gather / CUDA graphs**: `[Megatron-FSDP] Add conditional param.grad dereferencing logic to support full-iteration (FWD-BWD) CUDA graphability` (#4663); `Fix mxfp8 param gather numerical issue when DP overlap is off` (#4800); `make FP4 param gather work with the mixed precisions in NVFP4 recipe` (#4358); `Also persist asymmetrical units for the MXFP8 transpose weight buffer` (#4852); `M-FSDP: Make fine_grained_param_gather configurable for MXFP8 to enable performance–memory trade-offs` (#4181); `[MXFP8/FP4-param-gather] Post processing after forced param AG in eval` (#4562); `Allow optimizer CG to share the same pool as full-iter CG` (#4698); `fix(cuda_graphs): handle TE 2.15 removal of FP8GlobalStateManager.set_skip_fp8_weight_update_tensor` (#4874).
- **Inference / KV / MTP**: `Inference Cache input + position ID views` (carries from W19, #4634); `size DynamicInferenceContext KV layer_map for non-uniform PP` (#4775); `Fix MTP recompute crash with packed sequences` (#4593); `Tests/dynamic inference functional coverage` (#4761); `add inference performance test harness for GPT 583M, hybrid 2B, …` (#4806); on top of W19's `Move inference context bookkeeping to CPU with ContextGPUView` (#4306).
- **Multimodal / MIMO**: `[mimo] Thread position_ids through MimoModel for multimodal RoPE` (#4938); `MimoOptimizer: fix distributed checkpoint save and load for non-colocated MIMO` (#4801).
- **Training utils / data / SFT**: `refactor(data): consolidate get_batch and enable PP for SFT THD` (#4103); `Various training utils` (#4872); `Fix get_batch return order to ignore BlendedDataset provenance fields` (#4952); `Allow YAML MoE configs to use model specs` (#4822); `Move bert and t5 pretrain files` (#4820); `Route non-Muon params through DistributedOptimizer` (#4771); `Use sharded_state_dict_default in MLP.sharded_state_dict` (#4693); `fix no_shard training convergency and add unittest for no_shard` (#3754); `Add a knob to throttle the max allowed inflight offload in fine grained offloading` (#4692).
- **Paged stashing / fused GEMM**: `Paged Stashing` (#4247); `Add TEFusedDenseMLP for Dense+Grouped GEMM fusion on SM100+ (#4318)` (#4786); `Fix paged stashing test submodules lookup` (#4925).
- **Resiliency / CI / release**: `Add NVSkills CI request workflow` (#5033); `Add dev-feature preservation gate and change schedule` (#4773); `test(release): skip golden comparison on intermediate resume windows` (#5040); `Use shared ModelOpt calibration loop on 0.45+ with 0.44 fallback fix` (#4881); `Move LTS dependencies from pyproject.toml to Dockerfile.ci.lts` (#4877); `fix(layer_wise): tag MTP-stage word_embeddings as is_embedding_or_output_parameter` (#5034); `DDP wrap pg size fixes` (#5006); `ci: update CI workflow conditions for integration tests` (#4658); `Perf fix` (#4996); `ci: validate release branch-rules` (#4929); `Refactor and Improve MoE Logging` (#3431).

### Megatron-LM upstream feature delta table

| Area | New Upstream Capability | Evidence (PR/Commit) | Potential Impact to Primus |
| --- | --- | --- | --- |
| MoE / EP / HybridEP | Switch DSv3 on H100 to HybridEP;<br>drain predecessor reduce-scatter at dispatch time;<br>release MTP assertion when EP overlap with PP=1;<br>thread custom process groups through MoE grad finalization;<br>(carries) vLLM grouped-gemm MoE inference backend (#4566), HybridEP DDP-hook fix (#4500) | NVIDIA/Megatron-LM #5039, #4940, #4796, #4782, #4566, #4500 | Aligns with Primus's Turbo-side MoE work on `origin/main`; once Primus syncs, DSV3 + HybridEP recipes get an upstream-blessed path. Primus's `PrimusTurboDeepEPTokenDispatcher` integration must be re-validated against the new MoE grad-finalization process-group threading. |
| Megatron-FSDP / param gather / CUDA graphs | Full-iteration (FWD-BWD) CUDA graphability in M-FSDP;<br>mxfp8 param gather numerical fix when DP overlap is off;<br>MXFP8 transpose weight buffer persistence;<br>M-FSDP `fine_grained_param_gather` configurable for MXFP8;<br>TE 2.15 `set_skip_fp8_weight_update_tensor` removal handled | NVIDIA/Megatron-LM #4663, #4800, #4852, #4181, #4874 | Primus MI300X/MI355X FP8 / MXFP8 training paths will pick up correctness and perf improvements at sync time; M-FSDP knobs become available for memory–perf trade-off tuning on Primus GPT/MoE configs. |
| Inference / KV / MTP | DynamicInferenceContext KV layer_map for non-uniform PP;<br>MTP recompute crash fix with packed sequences;<br>inference perf-test harness for GPT-583M / hybrid-2B;<br>(carries) cache input + position ID views, ContextGPUView | NVIDIA/Megatron-LM #4775, #4593, #4806, #4634, #4306 | Primus inference / post-train recipes on Megatron backend continue to benefit; expand inference validation when sync lands. |
| Multimodal / MIMO | Thread `position_ids` through `MimoModel` for multimodal RoPE;<br>MimoOptimizer dist-ckpt save/load fix for non-colocated MIMO | NVIDIA/Megatron-LM #4938, #4801 | If Primus adds multimodal recipes on the Megatron backend, these upstream changes become prerequisites; not on the critical path today. |
| Training utils / SFT / data | Consolidate `get_batch` and enable PP for SFT THD;<br>various training utils;<br>fix `get_batch` return order;<br>allow YAML MoE configs to use model specs;<br>fix `no_shard` training convergence | NVIDIA/Megatron-LM #4103, #4872, #4952, #4822, #3754 | Primus's data-path / tokenizer alignment (W19 #675) needs revalidation against the consolidated upstream `get_batch`. |
| Paged stashing / fused GEMM | Paged Stashing core;<br>`TEFusedDenseMLP` for Dense+Grouped GEMM fusion on SM100+ | NVIDIA/Megatron-LM #4247, #4786 | Paged Stashing is a new memory-mgmt subsystem that will likely interact with Primus's Megatron-FSDP and MoE paths at sync time. |
| CI / release / resiliency | NVSkills CI request workflow;<br>dev-feature preservation gate;<br>skip golden comparison on intermediate resume windows;<br>shared ModelOpt calibration loop on 0.45+ with 0.44 fallback;<br>LTS deps move to `Dockerfile.ci.lts` | NVIDIA/Megatron-LM #5033, #4773, #5040, #4881, #4877 | Sync-time CI reshuffle and dependency tracking; coordinate with Primus CI when the pin bump lands. |
| Schedule plan API (carry-over) | `post_attn` node already removed from `TransformerLayerSchedulePlan` (consumed in Primus by W17 #672) | Primus/#672, upstream schedule-plan change | Primus-side fix already shipped; treat as confirmed drift that future bumps must keep in sync. |

## 5. torchtitan Drift Overview

- Upstream: `https://github.com/pytorch/torchtitan.git` (`main`)
- Pinned in Primus `main` (`third_party/torchtitan`): `5fb7cc2e3bbb9b9dc0ab7af34ed5cc58b5f32021` — *Deepseek-V3 toml file minor fix (#1894)*, 2025-10-15
- Upstream `main` HEAD: `92abc88e7ad788404a6f51f708715ed23de019b7` — *Fix model test failure in #3395: rope refactor (#3448)* (2026-05-28)
- Commit gap: **upstream is 680 commits ahead of Primus pin** (+70 since the W19 snapshot `4ebb0895`).
- Submodule SHA on Primus side: unchanged in this window. No new Primus outer-adapter changes touching `primus/backends/torchtitan/**` landed in this window either.
- Recommendation: **urgent sync** (unchanged from W17/W18/W19). The pin is now ~7.5 months stale; the upstream MoE refactor series (`[MoE][5/n] … [MoE][8/n]`) and the RoPE refactor in this window further widen the gap.

### Notable upstream areas that have moved since the pin

- **MoE refactor series**: `[MoE][5/n] Refactor MoE to clean DTensor boundaries for shared/routed experts` (#3386); `[MoE][6/n] Extract local_reorder, split DeepEP/HybridEP dispatchers` (#3389); `[MoE][7/n] Keep 3D tensors through MoE, flatten inside GroupedExperts` (#3423); `[MoE][8/n] Use shape suffix for MoE` (#3425); `[MoE][SAC] Use deterministic ops in MoE routing` (#3146); `Remove MoE expert for-loop fallback` (#3308); `[Quantization] Use class factory for MXFP8/Float8 GroupedExperts to fix _owner` (#3251).
- **GraphTrainer / precompile / cpu-offload**: `[graph_trainer] Use separate EP process groups for overlap` (#3369); `[graph_trainer] Add DeepSeek V3 16B SDPA config` (#3361); `[graph_trainer] Rework full_inductor_compilation_pass via regional_inductor + CPU attr migration` (#3346); `[graph_trainer] Defer cudagraph compatibility check to pass execution` (#3355); `[graph_trainer] Add bucketing ops to precompile serialization filter` (#3354); `[graph_trainer] Fix fsdp_passes compat with BitsetAncestors from pytorch passes` (#3416); `[graph_trainer] Gate overlap_fsdp_ag_rs_pass behind a config flag` (#3241); `[graph_trainer] Fix eager SAC policy mm counter to reset at layer boundaries` (#3397); `[graph_trainer] Skip dense numerics tests due to upstream DTensor regression` (#3372); `Remove fsdp_reshard_after_fwd_pass` (#3370); `[cpu-offloading] Encode last consumer as dep arg in ao.wait` (#3333).
- **Attention / RoPE / Flex**: `RoPE refactor: Using model's max_sequence_length as the upper bound of Training.sequence_length` (#3395); follow-up fix `Fix model test failure in #3395: rope refactor` (#3448); `Re-enable FlexAttention bitwise tests` (#3331); `[graph_trainer] Re-enable FlexAttention tests after upstream fix` (#3394); `[graph_trainer] Fix H100 CI failure from DeepEP compilation break` (#3390); `Fix imports for latest DeepEP` (#3414).
- **Memory / FSDP / DTensor / Llama3**: `Add FSDP symmetric memory configuration and related tests` (#3105); `[Full DTensor] Config-based Full DTensor for Llama3` (#3159); `Fix optimizer state and module state coupling` (#3356); `Fix memory snapshot pickle protocol for compatibility with memory visualizers` (#3375); `[Module] Replace from_nn_module with native Module subclasses` (#3398).
- **RL / observability / batcher**: `[rl] Add Batcher in RL Loop` (#3347); `[rl] Add TITO generator and gen metrics` (#3391); `[RL] Enable experiment metrics` (#3237); `[rl] Better initial weight loading` (#3318); `RL: observability spans across trainer/generator/controller` (#3234); `Debug Numerics with DebugMode` (#3323); `[rl] switch to vllm v2 engine` (#3330); `[rl] use wandb=False in CI` (#3408); `[rl] fix CI timeout issue by properly tear down for vllm engine V2` (#3365).
- **Loss / CE / CP**: `[loss] Fix ChunkedCELoss + TP gradient placement mismatch` (#3412); `Make ChunkedCELoss support torch.autograd.grad` (#3249); `[GraphTrainer] Add Context Parallel support` (#3305); `Re-enable compile for gpt-oss integration tests` (#3373).

### torchtitan upstream feature delta table

| Area | New Upstream Capability | Evidence (PR/Commit) | Potential Impact to Primus |
| --- | --- | --- | --- |
| MoE refactor series | `[MoE][5/n]` clean DTensor boundaries for shared/routed experts;<br>`[MoE][6/n]` extract `local_reorder` + split DeepEP/HybridEP dispatchers;<br>`[MoE][7/n]` keep 3D tensors through MoE, flatten inside `GroupedExperts`;<br>`[MoE][8/n]` shape suffix for MoE;<br>`[MoE][SAC]` deterministic ops in MoE routing;<br>remove MoE expert for-loop fallback;<br>class factory for MXFP8/Float8 `GroupedExperts` | pytorch/torchtitan #3386, #3389, #3423, #3425, #3146, #3308, #3251 | Largest single moving target for Primus's torchtitan backend; the EP backend / dispatcher split touches the same surface that Primus's Qwen3 / DSV3 torchtitan paths plug into. The Primus outer-adapter under `primus/backends/torchtitan/**` will need a thorough re-validation against the new MoE-routed-vs-shared split when Primus syncs. |
| GraphTrainer / precompile | Separate EP process groups for overlap;<br>DeepSeek V3 16B SDPA config;<br>`full_inductor_compilation_pass` reworked via `regional_inductor`;<br>defer cudagraph compatibility check to pass execution;<br>bucketing ops in precompile serialization filter;<br>`fsdp_passes` `BitsetAncestors` compatibility;<br>gate `overlap_fsdp_ag_rs_pass` behind a config flag | pytorch/torchtitan #3369, #3361, #3346, #3355, #3354, #3416, #3241 | Continuing GraphTrainer infrastructure remains unavailable behind the stale pin; will require coordinated patches in `primus/backends/torchtitan/**` at sync time. |
| Attention / RoPE / Flex | RoPE refactor using model's `max_sequence_length` as upper bound;<br>follow-up fix for the rope refactor;<br>re-enable FlexAttention bitwise tests;<br>re-enable FlexAttention tests after upstream fix;<br>fix latest DeepEP imports | pytorch/torchtitan #3395, #3448, #3331, #3394, #3414 | Primus's Qwen3 attention adapter (W19 #694, `primus/backends/torchtitan/models/qwen3/`) will need to be re-validated against the upstream RoPE refactor at sync time. |
| Memory / FSDP / DTensor / Llama3 | FSDP symmetric-memory configuration and tests;<br>config-based Full DTensor for Llama3;<br>fix optimizer-state / module-state coupling;<br>fix memory snapshot pickle protocol;<br>replace `from_nn_module` with native Module subclasses | pytorch/torchtitan #3105, #3159, #3356, #3375, #3398 | Touches the same memory / FSDP surface that Primus exercises on MI300X/MI355X; revalidation needed at sync time. |
| RL / observability | Batcher in RL Loop;<br>TITO generator + gen metrics;<br>RL experiment metrics;<br>better initial weight loading;<br>observability spans across trainer/generator/controller;<br>vllm v2 engine + CI timeout fix | pytorch/torchtitan #3347, #3391, #3237, #3318, #3234, #3330, #3365 | Not on Primus's critical path today, but the structured-logging + spans pattern is directly applicable to Primus torchtitan CI. |

## 6. Primus-Turbo Monthly Drift Overview

- Drift type: **in-repo**, not upstream — compares Turbo version/SHA referenced on Primus `main` now vs the latest commit at or before `month_start_ts = 2026-05-01 00:00 Asia/Shanghai` (`2026-04-30 16:00 UTC`).
- Turbo is **not a submodule** in Primus. Canonical version source:
  - `.github/workflows/ci.yaml` → `PRIMUS_TURBO_COMMIT`, `PRIMUS_TURBO_AITER_COMMIT`, `TRITON_COMMIT` (also wired through `.github/workflows/docker/Dockerfile`)
  - `.github/workflows/benchmark.yaml` → `PRIMUS_TURBO_COMMIT`
- Reference Primus commit at month start (May) on `main`: `0a8053cdfc67dd03f69d756eb046c52101638844` (*LFM model support (#651)*, 2026-04-28 11:44 GMT+8). It is the latest `main` commit at or before 2026-05-01 00:00 GMT+8 that touched `.github/workflows/ci.yaml` / `.github/workflows/benchmark.yaml`, so the Turbo pins at that commit are the canonical month-start values.
- Current state on `origin/main` (this week):
  - `ci.yaml` `PRIMUS_TURBO_COMMIT`: `06b8d3fefd91be26d6adfb5cd43c7524ef87b825` — *Add HYBRID FP8 format support for Triton backend in gemm and grouped_gemm (#278)*, 2026-05-13
  - `ci.yaml` `PRIMUS_TURBO_AITER_COMMIT`: `b5e03ed191fca11ee423226537ef8d9435e432a6`
  - `ci.yaml` `TRITON_COMMIT`: `88b227e23f0445f3f695bad05bbf1a363b4f50e0`
  - `benchmark.yaml` `PRIMUS_TURBO_COMMIT`: `06b8d3fefd91be26d6adfb5cd43c7524ef87b825`
- Month-start (2026-05-01) state on `main`:
  - `ci.yaml` `PRIMUS_TURBO_COMMIT`: `333b68d7c81b722b21b4aad10cd250c45f15027c` — *fix sm_scale none bug (#263)*, 2026-03-27
  - `ci.yaml` `PRIMUS_TURBO_AITER_COMMIT`: `e83f9903c07001a0ec29e85d223f6e6cdbe00859`
  - `benchmark.yaml` `PRIMUS_TURBO_COMMIT`: `a4488f6cdb15cfff4383c61af7922bb50803f0ea` — *feat: update triton impl for mi300 & mi355 (#252)*, 2026-03-19
  - `ci.yaml` `TRITON_COMMIT`: not present (new env var introduced earlier in May by PR #694 in W19).
- **Primus-Turbo pins did NOT move this week.** The +39 (CI) / +44 (benchmark) commit-count drift vs month start is fully carried over from W19's PR #694 (2026-05-07, bumped to `ef5b58ea`) and W21's PR #723 (2026-05-15, re-bumped to `06b8d3fe` and reverted AITER to `b5e03ed1`).
- Recommendation: **monitor**. The two YAML pins remain in sync (CI and benchmark both `06b8d3fe`). Track the next Turbo upstream bump and validate the new Triton-backed grouped-gemm default path on Primus MoE configs.

### Notable areas changed since month start

- **HYBRID FP8 grouped-GEMM (current pin)**: Primus-Turbo #278 adds HYBRID FP8 format support for the Triton backend in `gemm` and `grouped_gemm` (this is the current pinned commit `06b8d3fe`); follow-up `fix: remove attn workaround` (#313) cleans up the previous workaround in the same window.
- **Grouped GEMM default backend → Triton**: Primus-Turbo #320 (still on the path between month start and current pin) switches the default `grouped_gemm` backend to Triton for BF16 and FP8; combined with the `TRITON_COMMIT=88b227e` source build, this changes the kernel path used by Primus-Turbo's `TEGroupedMLP` integration on the Megatron backend.
- **hipBLASLt / grouped GEMM correctness**: Primus-Turbo #333 (`hipblaslt_gemm correct workspace size for MI300X`) and #319 (`bind per-stream hipBLASLt handles to fix bf16 multi-stream stall on gfx942`) fix two MI300X-specific correctness/stall bugs that affect Primus pretraining throughput on gfx942.
- **Attention layouts and varlen**: Primus-Turbo #332 (`Add flash_attn_varlen_func with aiter backend`), #275 (sbhd) and #304 (bhsd) extend attention layout / varlen coverage; relevant for Primus Qwen3 attention adapter and any future varlen-on-AITER experiments.
- **DeepEP / EP stability**: Primus-Turbo #327 (`fix: fix deepep combine hang`) and #314 (`feat(moe): add back deepep_use_comm_stream to force use single-stream`) tighten MoE/EP correctness paths on ROCm.
- **MXFP8 / FP8 quantization fixes**: Primus-Turbo #298 (MXScalingRecipe rename + grouped-gemm FP8 API), #306 (`reduce_row_kernel` partial-tile indexing), #307 (mxfp8 int32 overflow), #308 (mxfp8 gemm WAR race) tighten correctness on FP8 paths used by Primus MoE configs.
- **AITER pin moved (carry-over)**: `PRIMUS_TURBO_AITER_COMMIT` moved from `e83f9903` (month start) to `b5e03ed1` (current, re-set by PR #723). Note this differs from the W19 snapshot value `857f4d15`, which was the intermediate pin set by PR #694 and then changed by PR #723 in week W21.
- **Triton built from source (carry-over)**: a `TRITON_COMMIT=88b227e` is wired through `ci.yaml` and the docker build, which compiles Triton from `triton-lang/triton@release/3.7.x` source inside the docker image; not present at month start.

### Primus-Turbo monthly drift table

| Component | Current Version/SHA | Month-start Version/SHA | Delta Summary | Key Changes | Evidence |
| --- | --- | --- | --- | --- | --- |
| `PRIMUS_TURBO_COMMIT` (CI build) | `06b8d3fefd91be26d6adfb5cd43c7524ef87b825` (*Add HYBRID FP8 format support for Triton backend in gemm and grouped_gemm (#278)*, 2026-05-13) | `333b68d7c81b722b21b4aad10cd250c45f15027c` (*fix sm_scale none bug (#263)*, 2026-03-27) | **+39 commits** (no movement this week) | HYBRID FP8 grouped-GEMM (#278);<br>grouped-GEMM default → Triton (#320);<br>hipblaslt workspace + per-stream handles fixes (#333, #319);<br>flash_attn_varlen aiter backend (#332);<br>DeepEP combine hang fix (#327);<br>MXFP8/FP8 quant fixes (#306, #307, #308). | [`.github/workflows/ci.yaml` L17](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/ci.yaml#L17), [Primus-Turbo compare](https://github.com/AMD-AGI/Primus-Turbo/compare/333b68d7c81b722b21b4aad10cd250c45f15027c...06b8d3fefd91be26d6adfb5cd43c7524ef87b825) |
| `PRIMUS_TURBO_AITER_COMMIT` (CI build) | `b5e03ed191fca11ee423226537ef8d9435e432a6` | `e83f9903c07001a0ec29e85d223f6e6cdbe00859` | **Pin advanced** (no movement this week) | AITER pin advanced via PR #694 then re-set to `b5e03ed1` via PR #723 (W21). Build pulls AITER at the new SHA inside the docker image. | [`.github/workflows/ci.yaml` L18](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/ci.yaml#L18) |
| `PRIMUS_TURBO_COMMIT` (benchmark) | `06b8d3fefd91be26d6adfb5cd43c7524ef87b825` (*Add HYBRID FP8 format support for Triton backend in gemm and grouped_gemm (#278)*, 2026-05-13) | `a4488f6cdb15cfff4383c61af7922bb50803f0ea` (*feat: update triton impl for mi300 & mi355 (#252)*, 2026-03-19) | **+44 commits** (no movement this week) | Same set of upstream changes as the CI pin plus the earlier interval (`a4488f6c`→`333b68d7`); benchmark and CI pins remain in sync at `06b8d3fe`. | [`.github/workflows/benchmark.yaml` L9](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/benchmark.yaml#L9), [Primus-Turbo compare](https://github.com/AMD-AGI/Primus-Turbo/compare/a4488f6cdb15cfff4383c61af7922bb50803f0ea...06b8d3fefd91be26d6adfb5cd43c7524ef87b825) |
| `TRITON_COMMIT` (CI build) | `88b227e23f0445f3f695bad05bbf1a363b4f50e0` (`triton-lang/triton@release/3.7.x`) | not present | **Carry-over** (introduced earlier in May) | Compiles Triton from source inside the docker image; wired through both the main and JAX docker builds. | [`.github/workflows/ci.yaml` L21](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/ci.yaml#L21), [`Dockerfile`](https://github.com/AMD-AGI/Primus/blob/main/.github/workflows/docker/Dockerfile) |

## 7. Source Links

- Primus main branch: https://github.com/AMD-AGI/Primus/tree/main
- Primus weekly PR listing (window): https://github.com/AMD-AGI/Primus/pulls?q=is%3Apr+is%3Amerged+base%3Amain+merged%3A%3E%3D2026-05-24T16%3A00%3A00Z
- PR #731 (feat: add turbo attn deterministic support and refine env checker): https://github.com/AMD-AGI/Primus/pull/731
- PR #727 (test(megatron): add gpt_oss_20B sink attention trainer case): https://github.com/AMD-AGI/Primus/pull/727
- PR #732 (fix(arg_utils): parse list/dict/None literals in modern CLI overrides): https://github.com/AMD-AGI/Primus/pull/732
- PR #440 (feat: Add MLflow artifact upload for traces and logs): https://github.com/AMD-AGI/Primus/pull/440
- Megatron-LM pin: https://github.com/NVIDIA/Megatron-LM/commit/d3528a21301db2d12e92912b3ec025dc8a2ed4d6
- Megatron-LM upstream HEAD (at report time): https://github.com/NVIDIA/Megatron-LM/commit/ba9e0eaffd2a00ef475c5025f800a394f169a245
- Megatron-LM compare: https://github.com/NVIDIA/Megatron-LM/compare/d3528a21301db2d12e92912b3ec025dc8a2ed4d6...main
- torchtitan pin: https://github.com/pytorch/torchtitan/commit/5fb7cc2e3bbb9b9dc0ab7af34ed5cc58b5f32021
- torchtitan upstream HEAD (at report time): https://github.com/pytorch/torchtitan/commit/92abc88e7ad788404a6f51f708715ed23de019b7
- torchtitan compare: https://github.com/pytorch/torchtitan/compare/5fb7cc2e3bbb9b9dc0ab7af34ed5cc58b5f32021...main
- Primus-Turbo current pin: https://github.com/AMD-AGI/Primus-Turbo/commit/06b8d3fefd91be26d6adfb5cd43c7524ef87b825
- Primus-Turbo CI pin (month-start → current) compare: https://github.com/AMD-AGI/Primus-Turbo/compare/333b68d7c81b722b21b4aad10cd250c45f15027c...06b8d3fefd91be26d6adfb5cd43c7524ef87b825
- Primus-Turbo benchmark pin (month-start → current) compare: https://github.com/AMD-AGI/Primus-Turbo/compare/a4488f6cdb15cfff4383c61af7922bb50803f0ea...06b8d3fefd91be26d6adfb5cd43c7524ef87b825
- Triton release branch (pin): https://github.com/triton-lang/triton/commit/88b227e23f0445f3f695bad05bbf1a363b4f50e0
- Month-start reference commit on `main` (May): https://github.com/AMD-AGI/Primus/commit/0a8053cd
- Last week's report (W19): https://github.com/AMD-AGI/Primus/blob/main/docs/weekly_reports/2026-W19-primus-weekly.md

---

*Report generated 2026-05-29 09:01 GMT+8 from `origin/main` HEAD `05ec3231`. Upstream HEADs captured at the same time. Primus-Turbo pins read from `.github/workflows/ci.yaml` and `.github/workflows/benchmark.yaml` on `origin/main`.*
