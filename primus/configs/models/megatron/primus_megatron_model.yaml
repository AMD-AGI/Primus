
disable_primus_topk_router: false
moe_router_force_load_balancing: false
use_deprecated_20241209_moe_layer: false
# Replace the MoE layer with the ROCMoE EP-engine-backed layer (EP-only, TP=1,
# PP=1, bf16). Tune per-expert capacity via the ROCMOE_OVER_PROVISION env var.
use_rocmoe: false

# softcapping for logits (for x.ai grok2)
final_logit_softcapping: null # float
attn_logit_softcapping: null # float
router_logit_softcapping: null # float

# LFM2 model configuration
# lfm_layer_types: ["conv", "conv", "full_attention", "conv", "conv", "conv", "full_attention", "conv", "conv", "conv", "full_attention", "conv", "conv", "conv", "full_attention", "conv", "conv", "conv", "full_attention", "conv", "conv", "full_attention", "conv", "conv"]
lfm_layer_types: null # list[str]
conv_L_cache: 3 # int
conv_bias: false # bool
