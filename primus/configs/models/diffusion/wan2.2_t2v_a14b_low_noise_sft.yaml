extends:
  - wan2.2_t2v_a14b.yaml

# Low-noise SFT stage, aligned with NeMo AutoModel Wan2.2-T2V-A14B:
# trains the second Diffusers transformer on sigma in [0.0, boundary_ratio].
model:
  config:
    load_from_pretrained_path: ${INIT_CHECKPOINT:/models/Wan2.2-T2V-A14B-Diffusers}
    config:
      active_transformer: transformer_2
