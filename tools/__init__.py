"""
Primus local tools package.

This file exists so imports like `tools.modeling_zebra_llama` work when the
Primus repo root is on `sys.path` (e.g. you run from the Primus root, or set
PYTHONPATH to include it).
"""

