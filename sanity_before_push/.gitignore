# Run logs (regenerated by run_sanity.sh / run_mamba_parity.sh)
logs/
mamba_parity/logs/

# Scratch benchmarking configs (local repro only: hardcoded tokenizer path + short iters)
configs/*_exact.yaml
configs/*_speed.yaml
