# Plan-4 P25 — local smoke / profile artefacts only.
#
# Same convention as P23: keep this phase folder in version control
# for the summary docs / scripts; raw logs and profile traces stay
# local (large + noisy in PR diffs).
*.log
log_*.txt
debug.log
*.tgz
*.json
# Allow markdown summaries and shell scripts inside the phase folder.
!*.md
!*.sh
