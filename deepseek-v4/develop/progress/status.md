# DeepSeek-V4 Integration Progress Tracker

> Tick `[x]` and fill in the commit hash + date when a task is done.
> Task granularity matches `[../plan/02-phase-details.md](../plan/02-phase-details.md)`.
> Any blockers / decisions go in the `> note` row right under the task.

## Phase 0 — Investigation & Preparation


|     | Task                                   | commit | date       | note                                                                        |
| --- | -------------------------------------- | ------ | ---------- | --------------------------------------------------------------------------- |
| [x] | Architecture investigation + tech blog | —      | 2026-04-28 | `deepseek-v4/develop/techblog/01-deepseek-v4-architecture-deep-dive.md`     |
| [x] | 4 architecture diagrams (PNG)          | —      | 2026-04-28 | rendered directly via Pillow; `techblog/render_diagrams.py` reproduces them |
| [x] | Development plan                       | —      | 2026-04-28 | all documents in this directory                                             |


## Phase 1 — Configs & yaml


|     | Task                                                                    | commit     | date       | note                                                                                                                                                                                                                                                                                              |
| --- | ----------------------------------------------------------------------- | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | `primus/configs/models/megatron/deepseek_v4_base.yaml`                  | `d3383c02` | 2026-04-28 | extends `llama_base`; sets `model_type=deepseek_v4` and all V4 defaults                                                                                                                                                                                                                           |
| [x] | `primus/configs/models/megatron/deepseek_v4_flash.yaml`                 | `d3383c02` | 2026-04-28 | values from `DeepSeek-V4-Flash/config.json`                                                                                                                                                                                                                                                       |
| [x] | `primus/configs/models/megatron/deepseek_v4_pro.yaml`                   | `d3383c02` | 2026-04-28 | values from `DeeSeek-v4-Pro/config.json`                                                                                                                                                                                                                                                          |
| [x] | `examples/megatron/configs/MI355X/deepseek_v4_flash-BF16-pretrain.yaml` | `d3383c02` | 2026-04-28 | scaffold; parallelism + perf knobs to be retuned in P6                                                                                                                                                                                                                                            |
| [x] | `DeepSeekV4Tokenizer` accepted by `_add_tokenizer_args`                 | `d3383c02` | 2026-04-28 | `primus/backends/megatron/training/tokenizer/tokenizer.py`                                                                                                                                                                                                                                        |
| [-] | ~~Register V4 fields into Megatron argparse~~                           | (n/a)      | 2026-04-28 | Not needed: `merge_namespace` (`train_runtime.py:_initialize_trainer`) copies yaml-only fields onto `backend_args` after `convert_config`, and `MegatronBaseTrainer._patch_parse_args` makes Megatron return `backend_args` verbatim. The V4 builder reads V4 fields directly via `args.<field>`. |
| [ ] | yaml schema test                                                        |            |            | `tests/configs/test_deepseek_v4_yaml.py` (deferred to P3)                                                                                                                                                                                                                                         |


## Phase 2 — Register `model_type=deepseek_v4`


|     | Task                                                                               | commit     | date       | note                                             |
| --- | ---------------------------------------------------------------------------------- | ---------- | ---------- | ------------------------------------------------ |
| [x] | dispatch branch in `primus/backends/megatron/megatron_pretrain_trainer.py`         | `8ae10000` | 2026-04-28 | reuses `pretrain_gpt.forward_step` for V4        |
| [x] | `deepseek_v4` branch in `primus/core/utils/import_utils.py:get_model_provider`     | `8ae10000` | 2026-04-28 | imports primus-owned V4 builder/provider         |
| [x] | `primus/backends/megatron/core/models/deepseek_v4/__init__.py` (stub)              | `8ae10000` | 2026-04-28 | re-exports `DeepseekV4Model`                     |
| [x] | `primus/backends/megatron/core/models/deepseek_v4/deepseek_v4_model.py` (stub)     | `8ae10000` | 2026-04-28 | thin subclass of `GPTModel`; replaced in P3      |
| [x] | `primus/backends/megatron/core/models/deepseek_v4/deepseek_v4_builders.py` (shell) | `8ae10000` | 2026-04-28 | bundles `model_provider` + `deepseek_v4_builder` |
| [ ] | trainer-dispatch test                                                              |            |            | (added in P3 along with model spec)              |


## Phase 3 — Layer Spec + Block scaffolding


|     | Task                                                                                 | commit     | date       | note                                                                                                                       |
| --- | ------------------------------------------------------------------------------------ | ---------- | ---------- | -------------------------------------------------------------------------------------------------------------------------- |
| [x] | `deepseek_v4_layer_specs.py` (layer / decoder-block / MTP-block specs + P4/P5 hooks) | `a5d2a561` | 2026-04-28 | delegates to GPT helpers; per-layer hooks `_resolve_attention_module_spec` / `_resolve_mlp_module_spec` reserved for P4/P5 |
| [x] | `deepseek_v4_block.py` (1-stream version, hc_mult=1 degenerate)                      | `a5d2a561` | 2026-04-28 | `DeepseekV4TransformerBlock` subclass; stashes V4 config attrs for P4 patches                                              |
| [x] | `deepseek_v4_model.py` (uses `DeepseekV4TransformerBlock` as decoder)                | `a5d2a561` | 2026-04-28 | post-`super().__init__` swap-in keeps a stable target for P4/P5 patches                                                    |
| [-] | ~~register V4 fields in `pretrain_deepseek_v4.py:extra_args_provider~~`              | (n/a)      | 2026-04-28 | superseded — yaml fields reach builder via `merge_namespace`; no argparse step required                                    |
| [x] | `deepseek_v4_builder` wired to V4 layer specs + `DeepseekV4Model`                    | `a5d2a561` | 2026-04-28 | `_resolve_layer_spec` + `_resolve_mtp_block_spec` use the V4-prefixed helpers                                              |
| [ ] | 4-layer tiny config forward + backward passes                                        |            |            | needs GPU; deferred to env validation                                                                                      |


## Phase 4 — HC + Hybrid Attention


|     | Task                                                                              | commit     | date       | note                                                                                                                            |
| --- | --------------------------------------------------------------------------------- | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------- |
| [x] | `core/transformer/hyper_connection.py` (HyperMixer + HyperHead + Sinkhorn)        | `3b7ad8c8` | 2026-04-28 | unit-tested: row/col errs ~1e-6, hc_mult=1 degenerate exact, fp32 params + fp32 sinkhorn                                        |
| [x] | `core/transformer/compressor.py` (overlap=True ratio=4 / overlap=False ratio=128) | `3b7ad8c8` | 2026-04-28 | unit-tested: HCA / CSA pool shapes correct, APE shape matches, RMSNorm + (RoPE applied externally)                              |
| [x] | `core/transformer/indexer.py`                                                     | `3b7ad8c8` | 2026-04-28 | causality verified (q=0 all -1, q=3 sees pool[0], etc.); idxs in [0,P) ∪ {-1}; backward OK                                      |
| [x] | `core/transformer/sliding_window_kv.py`                                           | `3b7ad8c8` | 2026-04-28 | causal SWA mask + per-query KV indices                                                                                          |
| [x] | `core/transformer/attn_sink.py`                                                   | `3b7ad8c8` | 2026-04-28 | sinks=0 → probs sum ≤ 1; sinks=50 → ~0; backward propagates to sinks                                                            |
| [x] | `core/transformer/dual_rope.py` (dual base + partial interleaved RoPE + YaRN)     | `3b7ad8c8` | 2026-04-28 | YaRN m_scale = 0.1·log(factor)+1 verified; partial RoPE preserves norm; nope channels untouched                                 |
| [x] | `core/transformer/deepseek_v4_attention.py` (shared Q/KV/SWA/sink/output base)    | `3b7ad8c8` | 2026-04-28 | dense attention: forward + backward + causality OK                                                                              |
| [x] | `core/transformer/csa_attention.py`                                               | `3b7ad8c8` | 2026-04-28 | overlap compressor + indexer + per-query top-K gather + joint softmax (incl. sink); causality verified                          |
| [x] | `core/transformer/hca_attention.py`                                               | `3b7ad8c8` | 2026-04-28 | non-overlap compressor + full compressed-pool concat; causal-mask verified                                                      |
| [x] | upgrade `deepseek_v4_block.py` to multi-stream HC + per-layer attention dispatch  | `3b7ad8c8` | 2026-04-28 | standalone module (does not subclass `TransformerBlock`); 8-layer mixed dense/CSA/HCA + hc_mult=4 forward/backward/causality OK |
| [x] | finalize `deepseek_v4_layer_specs.py` (placeholder for super().**init**)          | `3b7ad8c8` | 2026-04-28 | V4 block bypasses Megatron's spec mechanism; placeholder spec keeps `GPTModel.__init_`_ happy until P6 refactor                 |
| [-] | ~~`patches/hyper_connection_patches.py`~~                                         | (n/a)      | 2026-04-28 | superseded — V4 block is standalone, no patch needed; the swap-in happens inside `DeepseekV4Model.__init__`                     |
| [ ] | unit test: HyperConnection / Sinkhorn doubly-stochastic                           |            |            |                                                                                                                                 |
| [ ] | unit test: Compressor numerical alignment vs reference                            |            |            |                                                                                                                                 |
| [ ] | unit test: Indexer causality + topk                                               |            |            |                                                                                                                                 |
| [ ] | integration: 4L V4-Flash toy model 50 iter loss decreases                         |            |            |                                                                                                                                 |


## Phase 5 — MoE / Activation / RoPE / MTP


|     | Task                                                                                                              | commit     | date       | note                                                                                                                                               |
| --- | ----------------------------------------------------------------------------------------------------------------- | ---------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | `core/transformer/clamped_swiglu.py` (activation + MLP wrapper)                                                   | `5e4008dc` | 2026-04-28 | clamp tight verified, MLP forward+backward OK; placed under `core/transformer/` (not `fusions/`) since v1 is eager                                 |
| [x] | `core/transformer/moe/v4_hash_router.py` (HashRouter; static `tid2eid` table)                                     | `5e4008dc` | 2026-04-28 | per-token top-K distinct, deterministic across instantiations w/ same seed, probs sum to 1; standalone (Megatron MoE integration → P6)             |
| [x] | `core/transformer/moe/v4_topk_router.py` (V4TopKRouter; sqrtsoftplus / sigmoid / softmax)                         | `5e4008dc` | 2026-04-28 | three score functions verified, top-K renorm OK, optional noaux_tc bias; backward OK                                                               |
| [x] | `core/transformer/moe/v4_moe.py` (DeepseekV4MoE: routed + shared experts + clamped SwiGLU)                        | `5e4008dc` | 2026-04-28 | hash mode + learned mode forward+backward; same-token determinism OK                                                                               |
| [x] | layer-aware `dual_rope.py` (YaRN only on `compress_ratio != 0`)                                                   | `3b7ad8c8` | 2026-04-28 | already covered by P4: `DualRoPE.get_rope(compress_ratio)` returns the right cache; main_rope built without YaRN, compress_rope with YaRN          |
| [x] | upgrade `deepseek_v4_block.py` to use V4 MoE (replace `_SwiGLUMLP`)                                               | `5e4008dc` | 2026-04-28 | per-layer router pick (hash if `layer_idx < num_hash_layers`); token_ids threaded through forward via model-side stash on the block                |
| [x] | `models/deepseek_v4/deepseek_v4_mtp.py` (DeepseekV4MTPBlock, separate per-layer HyperHead)                        | `5e4008dc` | 2026-04-28 | shares `rope` with main decoder, separate HC head per MTP layer; loss-side wiring → P6                                                             |
| [x] | `models/deepseek_v4/deepseek_v4_model.py` instantiates MTP block (when `mtp_num_layers > 0`)                      | `5e4008dc` | 2026-04-28 | `forward()` overridden to stash `input_ids` on the V4 block for hash routers                                                                       |
| [x] | smoke test: clamped_swiglu / HashRouter / V4TopKRouter / V4MoE / V4Block w/ MoE / V4 MTP                          | `5e4008dc` | 2026-04-28 | 7-test suite (`/tmp/p5_smoke.py`); all green on the dev box container                                                                              |
| [ ] | numerical alignment: token-0 logits vs reference `inference/model.py` within 1e-2                                 |            |            | deferred to P6+ (needs reference checkpoint loaded into V4 model)                                                                                  |
| [-] | ~~`patches/moe_patches/hash_router_patches.py`~~ / ~~`sqrtsoftplus_router_patches.py`~~ / ~~`mtp_v4_patches.py`~~ | (n/a)      | 2026-04-28 | superseded — V4 block is standalone and uses these modules directly. Real Megatron-MoE / token-dispatcher / EP integration is P6's responsibility. |


## Phase 6 — Trainer end-to-end + PP / EP


|     | Task                                                              | commit     | date       | note                                                                                                                                                     |
| --- | ----------------------------------------------------------------- | ---------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | swap builder in to use real model with HC + V4 attention + V4 MoE | `97b9720d` | 2026-04-28 | `deepseek_v4_builders.py` now matches upstream provider signature (`config` / `pg_collection`) and forwards into `DeepseekV4Model`                       |
| [x] | PP layout design (HC 4-stream sent atomically)                    | `97b9720d` | 2026-04-28 | `DeepseekV4TransformerBlock` now builds per-PP local layers (`get_num_layers_to_build` + `get_transformer_layer_offset`) and supports `set_input_tensor` |
| [ ] | TP partitioning (QKV / Compressor / Indexer end-to-end)           |            |            |                                                                                                                                                          |
| [x] | EP routing (hash + sqrtsoftplus)                                  | `97b9720d` | 2026-04-28 | `DeepseekV4MoE` now shards experts by EP rank and all-reduces routed outputs across EP group                                                             |
| [ ] | smoke: 1×8 BF16 50 iter                                           |            |            | not run yet (completed 3-iter smoke for functional bring-up)                                                                                             |
| [x] | smoke: 1×8 PP=2 EP=4 BF16                                         | `97b9720d` | 2026-04-28 | passed on `uswslocpm2m-106-2371` / `dev_primus_wenx_691` with `TRAIN_ITERS=3`, no fatal error, `iteration 3/ 3` reached                                  |


## Phase 7 — Single-node bring-up (PP=2, EP=4)


|     | Task                                                                                  | commit     | date       | note                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------- | ---------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| [-] | ~~Muon optimizer phase~~                                                              | (n/a)      | 2026-04-28 | cancelled: Primus already includes distributed Muon optimizer; no dedicated integration phase needed here                                            |
| [x] | create `run_deepseek_v4.sh` (reference: `run_qwen.bak.sh`)                            | `97b9720d` | 2026-04-28 | fixed knobs `MBS=1`, `GBS=16`, `PRIMUS_PP=2`, `PRIMUS_EP=4`; added lightweight smoke overrides (`num_layers=8`, `num_experts=8`, `mtp_num_layers=0`) |
| [x] | run `run_deepseek_v4.sh` on `uswslocpm2m-106-2371` in container `dev_primus_wenx_691` | `97b9720d` | 2026-04-28 | single-node smoke passed with PP/EP groups initialized, training reached `iteration 3` and torchrun exited code 0                                    |


## Phase 8 — ~~Convergence + FP8 / FP4~~ (cancelled)


|     | Task                                 | commit | date       | note                                             |
| --- | ------------------------------------ | ------ | ---------- | ------------------------------------------------ |
| [-] | ~~convergence / FP8 / FP4 campaign~~ | (n/a)  | 2026-04-28 | cancelled for current DeepSeek-V4 bring-up scope |


## Phase 8 (v2) — ModuleSpec main-path refactor

> Replan baseline: `deepseek-v4/develop/plan-1/`.


|     | Task                                                                                         | commit     | date       | note                                                                                                                    |
| --- | -------------------------------------------------------------------------------------------- | ---------- | ---------- | ----------------------------------------------------------------------------------------------------------------------- |
| [x] | define V4 runtime `ModuleSpec` topology in `deepseek_v4_layer_specs.py`                      | `df273a45` | 2026-04-29 | landed full DeepSeek layer/submodules spec tree (attention/ffn/hc) rooted at `get_deepseek_v4_runtime_decoder_spec`     |
| [x] | make `DeepseekV4Model` runtime path spec-driven (remove decoder swap as default)             | `df273a45` | 2026-04-29 | `DeepseekV4Model` now inherits `LanguageModule` and builds decoder directly from external runtime spec (`build_module`) |
| [x] | align builder (`deepseek_v4_builders.py`) layer/mtp spec resolution with spec-driven runtime | `df273a45` | 2026-04-29 | builder now resolves/passes DeepSeek runtime decoder spec only; removed GPT placeholder/super-init spec dependence      |
| [x] | PP/VP/MTP compatibility validation for the refactored spec path                              | `df273a45` | 2026-04-29 | validated runtime instantiate/forward in container `dev_primus_wenx_691` with expected decoder + attention topology     |
| [x] | document retirement plan for legacy placeholder/swap path                                    | `df273a45` | 2026-04-29 | package/model docs updated to record LanguageModule-based path and retirement of GPT placeholder/swap strategy          |


## Phase 9 (v2) — TE provider reuse integration


|     | Task                                                          | commit     | date       | note                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------- | ---------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | revise Phase 9 plan with provider-inheritance direction       | `e5fec968` | 2026-04-30 | switched plan to `DeepSeekV4SpecProvider(PrimusTurboSpecProvider)` as the single V4 provider entry                                                                                                                                                                   |
| [x] | add `DeepSeekV4SpecProvider` class in TE spec provider module | `e5fec968` | 2026-04-30 | landed in `core/extensions/transformer_engine_spec_provider.py`; inherits `PrimusTurboSpecProvider`, adds V4 mode resolution (`local`/`te`/`turbo`) and grouped-MLP selector                                                                                         |
| [x] | wire V4 spec construction to `DeepSeekV4SpecProvider`         | `e5fec968` | 2026-04-30 | `deepseek_v4_layer_specs.py` now resolves one provider instance, injects provider mode into block/layer params, and routes norm + MoE spec payload through provider                                                                                                  |
| [x] | migrate norm + projection path to provider-driven selection   | `e5fec968` | 2026-04-30 | norm path remains provider-driven; attention projections now use `DeepseekV4AttentionSubmodules + build_module`, and dense-MLP projections route through provider `linear()` in duplicated mode (TE/Turbo) with local fallback when provider modules are unavailable |
| [x] | providerize V4 MoE expert grouped-GEMM path                   | `e5fec968` | 2026-04-30 | `v4_moe.py` now supports provider grouped-MLP instantiation and grouped forward dispatch (expert bucketing + grouped forward + scatter-add), and falls back to local expert path when runtime dependencies are missing                                               |
| [x] | deliver provider-mode A/B validation report                   | `e5fec968` | 2026-04-30 | report added in `deepseek-v4/develop/plan-1/03-phase9-provider-ab-report.md`; local forward passes, TE module-map build + CUDA forward both pass, and TE/Turbo mode host-input path now has explicit CUDA guard in decoder forward                                   |


## Phase 10 (v2) — MoE + distributed path convergence


|     | Task                                                                               | commit     | date       | note                                                                                                                                                                                                   |
| --- | ---------------------------------------------------------------------------------- | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [x] | refresh Phase 10 landing scope and task decomposition                              | `b38e83cf` | 2026-04-30 | aligned with MoE review outcomes: submodules+build_module, dispatcher reuse, and clamped-SwiGLU backend compatibility                                                                                  |
| [x] | define `DeepseekV4MoESubmodules` and wire spec-driven MoE construction             | `b38e83cf` | 2026-04-30 | `v4_moe.py` now defines `DeepseekV4MoESubmodules` and builds router/dispatcher/expert/shared-expert modules through `build_module`; `deepseek_v4_layer_specs.py` now passes MoE submodules in FFN spec |
| [x] | integrate Megatron dispatcher bridge into DeepSeek-V4 MoE forward path             | `b38e83cf` | 2026-04-30 | V4 MoE now supports dispatcher bridge flow (`dispatch_preprocess -> token_dispatch -> dispatch_postprocess -> expert_compute -> combine`) with runtime dispatcher selection and local fallback         |
| [x] | retire routed-output `all_reduce` fallback from active EP path                     | `b38e83cf` | 2026-04-30 | local fallback now disables EP all-reduce by default and only enables it with explicit debug gate `v4_enable_ep_allreduce_fallback` (subsequently retired in P14 phase-2)                              |
| [ ] | implement PP token-id propagation contract for hash-routed layers                  |            |            | explicit stage ownership, transport rules, and fail-fast assertions                                                                                                                                    |
| [x] | add clamped-SwiGLU backend compatibility checks for grouped-gemm modes             | `752b7534` | 2026-04-30 | grouped backend now requires declared clamped-SwiGLU support (`supports_clamped_swiglu` or config override) or it is downgraded to local experts with warning                                          |
| [ ] | distributed smoke: 1x8 and PP/EP combined run with deterministic routing snapshots |            |            | no hang, no dispatcher regressions, stable hash and learned routing                                                                                                                                    |


## Phase 11 (v2) — Validation + release gates


|     | Task                                                   | commit | date | note                                                 |
| --- | ------------------------------------------------------ | ------ | ---- | ---------------------------------------------------- |
| [ ] | execute full regression gate matrix (G1-G6)            |        |      | see `deepseek-v4/develop/plan-1/02-test-strategy.md` |
| [ ] | numerical alignment run and tolerance report           |        |      | fixed seed/checkpoint snapshots                      |
| [ ] | short-run convergence campaign and baseline comparison |        |      | track loss slope and stability                       |
| [ ] | TE on/off throughput + memory comparison report        |        |      | performance gate for release decision                |
| [ ] | publish release checklist and blocker disposition      |        |      | go/no-go output with risk owners                     |


## Phase 12 (v3) — Plan-2 lockdown

> Replan baseline: `deepseek-v4/develop/plan-2/`.
> Plan-1 phases 9 / 10 / 11 are paused — their tracking rows above remain
> for history but are no longer the active program of work.


|     | Task                                                                  | commit     | date       | note                                                                                                                                                                                                                                       |
| --- | --------------------------------------------------------------------- | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [x] | Architecture review of `dev/wenx/deepseek-v4` from `e194e039`..HEAD   | `636ab3de` | 2026-05-01 | findings recorded in `deepseek-v4/develop/plan-2/00-review-findings.md`                                                                                                                                                                    |
| [x] | Plan-2 documents (`README.md`, `00`-`04`)                             | `636ab3de` | 2026-05-01 | replaces plan-0 / plan-1 as the active plan                                                                                                                                                                                                |
| [x] | Phase 12+ tracking section opened in `status.md`                      | `636ab3de` | 2026-05-01 | append-only                                                                                                                                                                                                                                |
| [x] | Update tech-blog with as-built notes for plan-1 + pointer to plan-2   | `636ab3de` | 2026-05-01 | added `techblog/02-plan-1-as-built-and-plan-2-pointer.md`; techblog `README.md` now points plan-2 as active plan of record                                                                                                                 |
| [x] | Refresh `progress/timeline.html` (standard fonts + day-by-day Gantt)  | `636ab3de` | 2026-05-01 | swapped Google Fonts → system stack; Gantt switched to daily columns with May 02–05 holiday band; P13–P21 packed into May 06–09                                                                                                            |
| [x] | Refresh `progress/deepseek_v4_roadmap.pptx` (add 开发计划 schedule slide) | `636ab3de` | 2026-05-01 | new slide 7/13 with 3-row date / phase / work-content layout + holiday-gap arrow; section eyebrows renumbered                                                                                                                              |
| [ ] | **Stakeholder sign-off on plan-2 scope**                              |            |            | **external blocker — required before P13 starts (May 06)**. Sign-off owner: project lead. Material to review: `plan-2/README.md`, `00-review-findings.md`, `01-roadmap.md`, `progress/timeline.html`, `progress/deepseek_v4_roadmap.pptx`. |


> **P12 status (2026-05-01 EOD):** all engineering deliverables are done.
> The only open item is the external sign-off; plan-2 P13 starts on May 06
> (after the May 02–05 holiday) once that sign-off lands.

## Phase 13 (v3) — Faithful attention (`MLASelfAttention`-rooted)

> P13 landed in two commits inside the May 06 budget:
>
> 1. **First commit** (`cad0fb38`) — V4-faithful dense (`compress_ratio == 0`)
>   attention rooted on `MLASelfAttention`, with single-latent KV, per-head
>    `q_rms`, attn-sink, grouped low-rank O, and an inline numerical-alignment
>    test scaffold. The legacy plan-1 `_LegacyDeepseekV4Attention` kept
>    backing CSA / HCA so the smoke matrix continued to run.
> 2. **Follow-up commit (this one)** — fold Compressor / Indexer into the
>   new `DeepseekV4Attention.forward` as spec submodules so all three V4
>    layer types (dense / HCA / CSA) share one class; switch
>    `linear_q_up_proj` / `linear_o_b` projection specs from
>    `parallel_mode="duplicated"` to ColumnParallel / RowParallel; add a
>    TP=2 sharding-parity scaffold; retire `csa_attention.py` /
>    `hca_attention.py` / `_LegacyDeepseekV4Attention`.


|     | Task                                                                                   | commit     | date       | note                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------- | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | `DeepseekV4Attention(MLASelfAttention)` + V4 `DeepseekV4AttentionSubmodules` dataclass | `cad0fb38` | 2026-05-01 | dense path; CSA / HCA folded in via the follow-up                                                                                                                                                                                                                                                                                                                                                                   |
| [x] | Single-latent KV (K = V = `wkv`)                                                       | `cad0fb38` | 2026-05-01 | new attention drops `linear_k_proj` / `linear_v_proj`; `linear_kv` projects `hidden -> head_dim` and broadcasts                                                                                                                                                                                                                                                                                                     |
| [x] | Per-head `q_rms` after `linear_q_up_proj`                                              | `cad0fb38` | 2026-05-01 | parameter-less RMS — matches the released checkpoint (no `q_rms.weight` key)                                                                                                                                                                                                                                                                                                                                        |
| [x] | Reuse MLA `kv_layernorm` for V4 `kv_norm`                                              | `cad0fb38` | 2026-05-01 | provider-built RMSNorm on `head_dim`; same module name as MLA                                                                                                                                                                                                                                                                                                                                                       |
| [x] | Learnable per-head `attn_sink` parameter                                               | `cad0fb38` | 2026-05-01 | direct `nn.Parameter` on the attention (key: `attn_sink`); inline softmax-with-sink — matches released checkpoint key exactly. Spec-built `attn_sink_module` retained as forward-compat for a future TE-fused sink                                                                                                                                                                                                  |
| [x] | Grouped low-rank O projection (`linear_o_a` + `linear_o_b`, `o_groups`/`o_lora_rank`)  | `cad0fb38` | 2026-05-01 | added `o_groups` / `o_lora_rank` to `DeepSeekV4TransformerConfig`; einsum-based `wo_a` per group + `wo_b` matches `inference/model.py`. `o_lora_rank == 0` falls back to MLA-style flat `linear_proj`                                                                                                                                                                                                               |
| [x] | Compressor / Indexer as spec submodules of attention                                   | `aa9929a0` | 2026-05-01 | added `compressor` / `indexer` to `DeepseekV4AttentionSubmodules`; `DeepseekV4Attention.forward` now dispatches on `compress_ratio in {0, 4, 128}` and shares the attn-sink softmax across local + compressed branches; `_LegacyDeepseekV4Attention`, `csa_attention.py`, `hca_attention.py` deleted                                                                                                                |
| [x] | Switch projection specs from `parallel_mode="duplicated"` to TP                        | `aa9929a0` | 2026-05-01 | `linear_q_up_proj` → `provider.column_parallel_linear()` (`gather_output=True`); `linear_o_b` / `linear_proj` → `provider.row_parallel_linear()` (`input_is_parallel=False`). At TP > 1 the projection weights are sharded across TP ranks; at TP = 1 the result is bit-identical to the previous duplicated path. Grouped-O `linear_o_a` + the rest stay duplicated; full sharded grouped-O TP plan tracked in P14 |
| [x] | Inline numerical-alignment test scaffold (CPU fp32, compress_ratio=0)                  | `cad0fb38` | 2026-05-01 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_deepseek_v4_attention.py`: state-dict-key check, shape-and-finite, sink on/off forward equivalence ≤1e-3 vs inline reference, fallback path coverage                                                                                                                                                                                                        |
| [x] | Compressed branches: HCA shape + numerical alignment, CSA shape                        | `aa9929a0` | 2026-05-01 | extended the unit-test file with `test_hca_forward_shape_and_finite`, `test_hca_forward_matches_inline_reference` (compress-base RoPE + compressed-causal mask + joint softmax-with-sink, ≤1e-3 vs inline reference) and `test_csa_forward_shape_and_finite` (overlap Compressor + Indexer top-K + per-query joint softmax)                                                                                         |
| [x] | Spec wiring contract tests                                                             | `aa9929a0` | 2026-05-01 | `test_attention_spec_uses_column_and_row_parallel` asserts the q-up-proj / o-b spec uses `ColumnParallel` / `RowParallel`; `test_attention_spec_includes_compressor_and_indexer` asserts compressed-branch submodules carry `Compressor` (always) + `Indexer` (CSA only)                                                                                                                                            |
| [ ] | 1L attention forward agrees with HF reference within 1e-3 (CPU fp32)                   |            |            | **partial**: inline reference equivalence verified in `aa9929a0`; HF-reference comparison deferred to P22+ along with the state-dict adapter (per 2026-05-01 reshuffle)                                                                                                                                                                                                                                             |
| [x] | TP=2 sharding parity test (scaffold)                                                   | `aa9929a0` | 2026-05-01 | `test_tp2_sharding_parity_scaffold` skips on CPU / single-rank; runnable only under `torchrun --nproc_per_node=2`. Implementation of the bit-equality check vs a duplicated baseline is tracked in P19 (distributed re-validation)                                                                                                                                                                                  |


## Phase 14 (v3) — Faithful MoE + activation + router

> P14 landed in two commits:
>
> 1. **First commit (`1a8bf32e`)** — math + parameter-layout faithfulness:
>   pre-multiplication clamped SwiGLU, learned router rewritten with HF-aligned
>    scoring + bias-only-for-selection semantics, hash router rewritten with a
>    *learnable* gate weight (weights gathered from learned scores, expert ids
>    from `tid2eid`), `_DenseSwiGLUMLP` clamp fix, plus G3 + G4 unit tests
>    (CPU fp32 ≤1e-6 vs inline HF reference).
> 2. **Follow-up commit (this one)** — structural bring-up:
>   `DeepseekV4MoE` is now a `MegatronModule` (was `nn.Module`) and exposes
>    a `BaseMoELayer`-compatible public surface (`local_expert_indices` +
>    `set_layer_number`); the CPU local-experts path (no `pg_collection`)
>    builds a `nn.ModuleList[ClampedSwiGLUMLP]` + per-expert dispatch loop
>    that mirrors the HF reference math; provider helpers
>    `v4_grouped_mlp_spec(swiglu_limit)` + `v4_router_spec(learned)`
>    landed; G5 (1L MoE forward ≤ 1e-3 fp32 vs inline HF reference) gated by
>    `test_v4_moe.py`. Aux-loss / z-loss inheritance via `TopKRouter`
>    subclassing is intentionally deferred to P19 (the parent registers
>    CUDA buffers in `__init__`; gating that on a device check upstream is
>    out-of-scope for the CPU-clean V4 routers).
> 3. **Token-ids threading**: the router-side `(hidden, token_ids)`
>   contract is plumbed; the full `TransformerBlock → TransformerLayer →  MoE` forward-kwarg refactor (and the `decoder._v4_token_ids` stash
>    removal) co-lands with the P15 hybrid-layer rewrite.


|     | Task                                                                                       | commit     | date       | note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------ | ---------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | `clamped_swiglu_pre_mul(gate, up, alpha)` activation; separate `w1`/`w3` for the eager MLP | `1a8bf32e` | 2026-05-01 | new pre-mul clamp matches HF reference (gate clamp `max=α` one-sided, up clamp `(-α,+α)` two-sided); `ClampedSwiGLUMLP` exposes `w1.weight` / `w2.weight` / `w3.weight` keys for state-dict parity; `_DenseSwiGLUMLP` in `deepseek_v4_block.py` now applies the same pre-mul clamp; fused `[gate                                                                                                                                                                                                                                                                             |
| [x] | `DeepseekV4LearnedRouter` with sqrtsoftplus / sigmoid / softmax + bias                     | `1a8bf32e` | 2026-05-01 | renamed from `V4TopKRouter` (alias retained); gate exposed as `weight` Parameter (matches Megatron `TopKRouter` AND HF `Gate.weight`); `expert_bias` is selection-only; renormalization gated by `score_function != softmax`; honors `moe_router_topk_scaling_factor` (= HF `route_scale`). Subclassing Megatron's `TopKRouter` for full aux-loss / z-loss / dispatcher lifecycle deferred to P14 phase-2                                                                                                                                                                    |
| [x] | `DeepseekV4HashRouter` with learnable `gate_linear` + `tid2eid` lookup                     | `1a8bf32e` | 2026-05-01 | renamed from `HashRouter` (alias retained); learnable `weight` Parameter same shape as the learned router (was uniform 1/topk); `tid2eid` is a frozen `nn.Parameter(requires_grad=False, dtype=int32)` matching HF reference layout; `forward(hidden, token_ids)` gathers learned scores at expert ids prescribed by `tid2eid`; renorm + scale parity with the learned router                                                                                                                                                                                                |
| [x] | `DeepseekV4MoE` integrates with Megatron spec lifecycle                                    | `5fe8bc3c` | 2026-05-01 | parent class switched from `nn.Module` to `MegatronModule`; added `set_layer_number` (mirrors `BaseMoELayer.set_layer_number`) and `local_expert_indices`; CPU local-experts path (`pg_collection=None` -> `nn.ModuleList[ClampedSwiGLUMLP]` + per-expert dispatch loop matching HF reference). `MoELayer`-rooted aux-loss / z-loss inheritance via `TopKRouter` subclassing tracked into **P19** because the upstream `TopKRouter.__init_`_ registers CUDA buffers unconditionally; the V4 routers stay standalone `nn.Module`s for CPU-clean unit tests in the meantime    |
| [x] | Provider `v4_grouped_mlp_spec(swiglu_limit)` + `v4_router_spec(learned)`                   | `5fe8bc3c` | 2026-05-01 | added to `DeepSeekV4SpecProvider` (`extensions/transformer_engine_spec_provider.py`). `v4_grouped_mlp_spec` returns a `ModuleSpec(grouped_module, MLPSubmodules)` ready for `DeepseekV4MoESubmodules.grouped_experts`; the V4 pre-mul clamp itself flows via `config.activation_func_clamp_value` (Megatron's eager `glu()` already does the right math). `v4_router_spec(learned)` returns a bare `ModuleSpec` for either the learned or the hash router                                                                                                                    |
| [ ] | Token-ids threaded as forward kwarg (no `decoder._v4_token_ids`)                           |            |            | router-side `(hidden, token_ids)` already plumbed in phase-1; the full `TransformerBlock -> TransformerLayer -> MoE` forward-kwarg refactor (and the `decoder._v4_token_ids` stash removal) co-lands with the P15 hybrid-layer rewrite                                                                                                                                                                                                                                                                                                                                       |
| [x] | G3 unit tests: pre-mul activation matches HF reference within 1e-6 fp32                    | `1a8bf32e` | 2026-05-01 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_clamped_swiglu.py` — 7 tests cover split / fused / one-sided gate / α=0 / `w1`/`w2`/`w3` state-dict keys / fused-vs-split forward equivalence / end-to-end MLP vs HF `Expert.forward` (≤1e-6 fp32)                                                                                                                                                                                                                                                                                                                   |
| [x] | G4 unit tests: routers identical (probs, indices) to HF + gradient flow on gate            | `1a8bf32e` | 2026-05-01 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_routers.py` — 13 tests across both routers: score-function parity, learned-router HF agreement (3 score functions × 2 expert-bias modes ≤1e-6), softmax-skips-renorm contract, gradient flows to `weight`, expert-bias detached from probs graph; hash-router HF agreement (3 score functions ≤1e-6), tid2eid frozen parameter (`requires_grad=False`, dtype int32), state-dict keys, deterministic table across seeds, OOB / shape-mismatch error paths, gradient flows to `weight` while `tid2eid.grad is None` |
| [x] | 1L MoE forward within 1e-3 of HF reference (G5)                                            | `5fe8bc3c` | 2026-05-01 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_moe.py` — 11 tests: parent-class / CPU-path construction, `set_layer_number`, learned-router MoE forward vs inline HF reference (3 score functions × shared-expert on/off, ≤ 1e-3 fp32), hash-router MoE forward vs HF (3 score functions, ≤ 1e-3), `route_scale` propagation, gradient flows to `router.weight` + at least one routed expert + the shared expert, hash layer requires `token_ids` error path                                                                                                     |


## Phase 15 (v3) — Faithful layer + block + HC × PP


|     | Task                                                                     | commit     | date       | note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------ | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | `DeepseekV4HybridLayer(TransformerLayer)` with HC residual override      | `25ccdb5e` | 2026-05-01 | parent class switched from `GraphableMegatronModule` to `TransformerLayer` (`MegatronModule.__init__` bypass — V4's submodule contract differs from upstream); submodule fields renamed to upstream-canonical names (`input_layernorm` / `self_attention` / `pre_mlp_layernorm` / `mlp`). `DeepseekV4HybridLayerSubmodules` extends `TransformerLayerSubmodules` with `attn_hc` / `ffn_hc` HC-mixer hooks. Forward accepts `(hidden_states, attention_mask, *, position_ids, token_ids, **kwargs)` so the layer plugs into `MultiTokenPredictionLayer` (P16) without bespoke adapters |
| [x] | `DeepseekV4TransformerBlock(TransformerBlock)`                           | `25ccdb5e` | 2026-05-01 | parent class switched to `TransformerBlock` (`MegatronModule.__init__` bypass — V4 has its own dispatcher / lift-lower path). Type identity unlocks Megatron `isinstance` checks + sharded-state-dict integration; CPU instantiability preserved                                                                                                                                                                                                                                                                                                                                      |
| [x] | Lift / lower helpers carry `[S,B,K,D]` across PP via `[S*K,B,D]` packing | `25ccdb5e` | 2026-05-01 | `_lift_streams_in(hidden_states, pre_process, hc_mult)` and `_lower_streams_out(x, post_process, hc_mult)` extracted as module-level helpers. First PP stage lifts `[S, B, D] -> [B, S, K, D]`; intermediate stages preserve K via `[S*K, B, D]` send/recv; final stage collapses with HyperHead and transposes to `[S, B, D]`. Fixes C1                                                                                                                                                                                                                                              |
| [x] | `HyperHead` only on the post_process stage                               | `25ccdb5e` | 2026-05-01 | `DeepseekV4TransformerBlock.__init_`_ only builds `self.hyper_head` when `self.post_process and hc_mult > 1`; non-final stages keep `hyper_head = None` and forward the multi-stream form across PP P2P                                                                                                                                                                                                                                                                                                                                                                               |
| [x] | Caller-supplied `position_ids` honored                                   | `25ccdb5e` | 2026-05-01 | `DeepseekV4Model.forward` passes `position_ids=` to the decoder; `DeepseekV4TransformerBlock.forward` consumes it as a kwarg and forwards to each layer; `DeepseekV4HybridLayer.forward` falls back to `arange(S)` only when omitted (CPU smokes / unit tests). Fixes C3                                                                                                                                                                                                                                                                                                              |
| [x] | Token-ids removed from `decoder` attribute stash                         | `25ccdb5e` | 2026-05-01 | `DeepseekV4Model.forward` no longer assigns `decoder._v4_token_ids`; `input_ids` is passed to the decoder as `token_ids=` kwarg, then propagated layer -> mlp -> hash router. AST audit in `test_v4_block_pp.py::test_model_forward_does_not_set_decoder_v4_token_ids_attribute` enforces the attribute stash stays gone. Fixes C2                                                                                                                                                                                                                                                    |
| [ ] | PP=1 vs PP=2 vs PP=4 equivalence on 4L V4 toy (G6)                       |            |            | requires distributed init + multi-rank harness; tracked into **P19** distributed re-validation. CPU-only G6 sub-gate already covered: `test_v4_block_pp.py::test_lift_lower_multi_stream_intermediate_roundtrip` proves `_lift_streams_in` after `_lower_streams_out` is bit-exact, which is the math contract a real PP run depends on                                                                                                                                                                                                                                               |


## Phase 16 (v3) — MTP integration


|     | Task                                                                                         | commit                  | date       | note                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | -------------------------------------------------------------------------------------------- | ----------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | `get_v4_mtp_block_spec` builder                                                              | `6c5875d4`              | 2026-05-01 | new module `deepseek_v4_mtp_specs.py`. Returns a `ModuleSpec(MultiTokenPredictionBlock, submodules=MultiTokenPredictionBlockSubmodules(layer_specs=[mtp_layer_spec]*mtp_num_layers))`. Each per-depth spec wraps `MultiTokenPredictionLayer` with V4 RMSNorm (`provider.v4_norm_module()` for `enorm` / `hnorm` / `layer_norm`) + column-parallel `eh_proj` (from `provider.column_parallel_linear()`) + the V4 hybrid layer spec passed in by the model |
| [x] | `DeepseekV4Model.__init__` builds `MultiTokenPredictionBlock` when `mtp_num_layers > 0`      | `6c5875d4`              | 2026-05-01 | spec-based path is the default; gated on `mtp_on_this_rank` (with try/except so CPU smokes without `parallel_state` keep working). The legacy `DeepseekV4MTPBlock` path was preserved behind `v4_use_custom_mtp_block` here and removed in P17 (`e591b893`)                                                                                                                                                                                              |
| [x] | Per-MTP-layer separate `HyperHead`                                                           | `6c5875d4`              | 2026-05-01 | the V4 hybrid layer's HC mixers are baked into `DeepseekV4HybridLayer.forward`, so each MTP depth that builds a hybrid layer naturally owns its own HC math; no separate per-MTP-layer HyperHead orchestration is needed at the MTP-block level                                                                                                                                                                                                          |
| [x] | `process_mtp_loss` wired in `DeepseekV4Model.forward`                                        | `6c5875d4`              | 2026-05-01 | mirrors `GPTModel.forward`: when `self.mtp_process and self.mtp is not None`, the MTP block runs after the decoder; on `post_process` with `mtp_num_layers > 0`, `process_mtp_loss` chunks the concatenated hidden states and adds the auxiliary MTP loss to the LM-loss path                                                                                                                                                                            |
| [x] | `DeepseekV4MTPBlock` retired (deprecation banner + warning, then deletion)                   | `6c5875d4` → `e591b893` | 2026-05-01 | P16 (`6c5875d4`) annotated the legacy block as deprecated and emitted a `DeprecationWarning`; P17 (`e591b893`) deleted `deepseek_v4_mtp.py`, removed the `v4_use_custom_mtp_block` config flag, and dropped the symbol from the package surface                                                                                                                                                                                                          |
| [ ] | MTP loss appears in train log; ablation `mtp_num_layers=0` matches LM loss to 1e-6 (G7)      |                         |            | requires distributed init + `MultiTokenPredictionBlock` runtime (CP / SP plumbing); tracked into **P19** distributed re-validation alongside the PP equivalence (G6)                                                                                                                                                                                                                                                                                     |
| [+] | Plan-2 P16 follow-on — `DeepseekV4HybridLayer.forward` returns `(hidden_states, None)` tuple | `6c5875d4`              | 2026-05-01 | required by `MultiTokenPredictionLayer._proj_and_transformer_layer` which unpacks `hidden_states, _ = self.mtp_model_layer(...)`. The block's per-layer call updates to `x, _ = layer(...)`; legacy `DeepseekV4MTPBlock` likewise updated (and then deleted in P17)                                                                                                                                                                                      |
| [+] | Plan-2 P16 follow-on — V4 attention spec advertises `attn_mask_type=AttnMaskType.causal`     | `6c5875d4`              | 2026-05-01 | needed because `MultiTokenPredictionLayer.__init__` validates the inner layer's `self_attention.params['attn_mask_type']` against `{padding, causal, no_mask, padding_causal}`. `DeepseekV4Attention.__init__` accepts and ignores the kwarg (V4 manages its own SWA / sink mask)                                                                                                                                                                        |


## Phase 17 (v3) — Code cleanup (dead-code retirement)

> Plan-2 reshuffle (2026-05-01): the original Phase 17 (HF state-dict
> adapter + V4-Flash checkpoint load) is **deferred to Phase 22+** —
> see "Deferred / future work" below. Plan-2 is pre-training-only, so
> HF-weight loading is not on the release path. The slot is reused for
> the dead-code work that previously sat in Phase 21, so the cleanup
> happens *before* Phase 18's spec audit walks the tree.


|     | Task                                                                                                 | commit                     | date       | note                                                                                                                                                                                                                                                                                                                                  |
| --- | ---------------------------------------------------------------------------------------------------- | -------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | Dedup `_RMSNorm` duplicates (`block.py`, `attention.py`, `compressor.py`) into shared `LocalRMSNorm` | `e591b893`                 | 2026-05-01 | new module `primus/backends/megatron/core/transformer/local_rmsnorm.py`; three call sites import the canonical class. AST guard `test_no_v4_module_redefines_local_rmsnorm` (G14) prevents regression.                                                                                                                                |
| [-] | ~~Retire standalone `dual_rope.py`~~                                                                 | —                          | 2026-05-01 | **kept**: `DualRoPE` is load-bearing for V4's CSA / HCA branches (dual-base partial RoPE; main base for local KV + compress base for the compressed pool). Megatron's `RotaryEmbedding` only supports a single base; no equivalent exists. The plan-2 line item was over-eager. Documented here so future audits do not re-open this. |
| [x] | Retire `csa_attention.py` / `hca_attention.py`                                                       | already-gone (P13 phase-2) | 2026-05-01 | confirmed: files do not exist on disk; logic lives in `DeepseekV4Attention.forward` (compress_ratio dispatch). G14 test re-asserts.                                                                                                                                                                                                   |
| [x] | Retire legacy `DeepseekV4MTPBlock` (delete)                                                          | `e591b893`                 | 2026-05-01 | `primus/backends/megatron/core/models/deepseek_v4/deepseek_v4_mtp.py` deleted; spec-based path via `get_v4_mtp_block_spec` is the only MTP route. Package `__init`__ no longer exports the symbol.                                                                                                                                    |
| [x] | Drop `v4_use_custom_mtp_block` config flag                                                           | `e591b893`                 | 2026-05-01 | removed from `DeepSeekV4TransformerConfig`; `DeepseekV4Model.__init_`_ simplified — only the spec-based `MultiTokenPredictionBlock` branch remains. `mtp_compress_ratios` (legacy-only) also removed.                                                                                                                                 |
| [x] | Drop EP `all_reduce` fallback gate (`v4_enable_ep_allreduce_fallback`)                               | already-gone (P14 phase-2) | 2026-05-01 | the flag was retired with the dispatcher migration; only docs reference it now. The remaining "fallback" string in `v4_moe.py` is unrelated (dispatcher-type fallback warning).                                                                                                                                                       |
| [x] | AST audit: zero `_v4_token_ids` references in `primus/backends/megatron/.../deepseek_v4` (G14)       | `e591b893`                 | 2026-05-01 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p17_dead_code.py::test_no_runtime_v4_token_ids_attribute_access` walks every V4 source via `ast` and rejects any `Attribute` / `Assign` / `Name` reference. Docstring mentions are exempt (audit walks the syntax tree).                                                   |
| [x] | Fix yaml comments (4=CSA, 128=HCA) in `deepseek_v4_*.yaml`                                           | `e591b893`                 | 2026-05-01 | `flash.yaml` comment inverted (was "4=HCA, 128=CSA"); `pro.yaml` schedule list now carries the canonical comment block; `base.yaml` stub annotated. G14 test parameterises over all three yamls.                                                                                                                                      |
| [x] | Refresh `__init__.py` package surface to live classes only                                           | `e591b893`                 | 2026-05-01 | dropped `DeepseekV4MTPBlock` from imports + `__all__`; surface is now `Model / TransformerBlock / HybridLayer / get_v4_mtp_block_spec / deepseek_v4_builder / model_provider`.                                                                                                                                                        |
| [x] | Static dead-code audit gate (G14) green                                                              | `e591b893`                 | 2026-05-01 | new `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p17_dead_code.py` covers retired files / config fields / package surface / `_v4_token_ids` AST scan / `_RMSNorm` shadow scan / yaml comment mapping.                                                                                                                   |


## Phase 18 (v3) — Spec-system audit

> `_v4_token_ids` removal moved to Phase 17 so the audit walks a clean
> tree (P15 already eliminated the runtime stash; P17 removes any
> leftover references).


|     | Task                                                                      | commit     | date       | note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------------------------------------- | ---------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | Provider built once per builder call; threaded via `BuildContext`         | `b5832672` | 2026-05-01 | fixes **D1**. New module `primus/backends/megatron/core/models/deepseek_v4/build_context.py` exposes `resolve_v4_provider(config)`. Provider is cached on the config object via a private attribute, so different configs get different providers and tests stay isolated. Block (`_build_projection`, MoE shared-expert build) + layer specs + MTP specs all migrated. AST audit `test_no_direct_DeepSeekV4SpecProvider_construction_outside_build_context` (P18 spec audit) prevents regression.                                                                                                                                     |
| [x] | `provider.v4_mlp_activation_func()` returns the right thing for V4        | `b5832672` | 2026-05-01 | fixes **D2**. The base provider's `activation_func()` keeps returning `TEActivationOp` (per `BackendSpecProvider` contract — it returns a *type*). The new V4-aware helper returns `None` when `config.use_te_activation_func` is False (so Megatron MLP uses `config.activation_func` = clamped-SwiGLU, the V4 default), or `TEActivationOp` when the user opts into the TE path. Layer specs + block.py SharedExpertMLP wiring updated to call the V4 helper instead of the unconditional one. CPU-runnable contract test `test_v4_mlp_activation_func_respects_use_te_activation_func` (G1 helpers).                                |
| [x] | YAML `compress_ratios` normalized to `tuple[int, ...]` in `__post_init__` | `b5832672` | 2026-05-01 | fixes **D4**. New helper `_normalize_compress_ratios_field` converts string-list / list / tuple to `tuple[int, ...]` before any consumer reads the field. Runtime helpers (`_parse_int_sequence`, `_normalize_compress_ratios`) now see a normalized type and never need `ast.literal_eval` on the live path. Schema test `test_compress_ratios_normalized_to_tuple` parameterises over flash + pro yamls.                                                                                                                                                                                                                             |
| [x] | `tests/unit_tests/configs/test_deepseek_v4_yaml.py` (G1)                  | `b5832672` | 2026-05-01 | new file under `tests/unit_tests/configs/`. Parameterises over `deepseek_v4_{base,flash,pro}.yaml`: parses each via the standard `parse_yaml`, builds a `DeepSeekV4TransformerConfig`, asserts compress_ratios round-trips to a tuple of ints, asserts every entry is in the canonical `{0, 4, 128}` set, asserts retired fields (`v4_use_custom_mtp_block` / `mtp_compress_ratios`) are gone from both the config dataclass and the YAMLs, asserts the V4-specific runtime fields (HC / sliding-window / sink / O-LoRA / MoE extras) are present. Also covers the provider singleton (D1) and activation_func helper (D2) at runtime. |
| [x] | Audit: no spec-replaceable module instantiated outside `build_module`     | `b5832672` | 2026-05-01 | new `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p18_spec_audit.py`: AST-walks the spec helpers (`deepseek_v4_layer_specs.py`, `deepseek_v4_mtp_specs.py`) and rejects any direct `TENorm(...)` / `TEColumnParallelLinear(...)` / `TERowParallelLinear(...)` / `TELinear(...)` / `TEActivationOp(...)` `Call` node — spec builders must emit `ModuleSpec(module=...)` references. Cross-checks with the D1 + D2 + package-surface audits in the same file.                                                                                                                                                               |


## Phase 19 (v3) — Distributed re-validation

> 2026-05-07 mi355-gpu-12 smokes (10 iters, mock data, BF16, MBS=1 GBS=16,
> seq=128, num_layers=8, num_hash_layers=3, hc_mult=4): all passed and
> backed by two new patches under `primus/backends/megatron/patches/`.
> Logs live in `deepseek-v4/develop/progress/p19/`.


|     | Task                                                       | commit     | date       | note                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ---------------------------------------------------------- | ---------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | Smoke A 1×8 BF16 (TP=1 PP=1 EP=1)                          |            | 2026-05-07 | 10/10 iters, dense-PP path, no V4 patches needed (HC stays in-stage)                                                                                                                                                                                                                                                                                                                                                                                           |
| [x] | Smoke B 1×8 BF16 (TP=1 PP=2 EP=4)                          |            | 2026-05-07 | 10/10 iters; required `pp_tensor_shape` (HC × PP wire packs `[S*K, B, D]`)                                                                                                                                                                                                                                                                                                                                                                                     |
| [x] | Smoke C 1×8 BF16 (TP=1 PP=4 EP=2)                          |            | 2026-05-07 | 10/10 iters; required `pp_tensor_shape` + `pp_token_pre_broadcast` (middle PP stage owns hash-routed layer 2). Log: `p19/smokeC_pp4_ep2_v2.log`                                                                                                                                                                                                                                                                                                                |
| [x] | Smoke D 1×8 BF16 (TP=1 PP=2 EP=4 VPP=2)                    |            | 2026-05-07 | 10/10 iters; VPP-clean. `pp_tensor_shape` also wraps interleaved-1F1B `seq_length`; `pp_token_pre_broadcast` runs all PP broadcasts upfront. Log: `p19/smokeD_pp2_ep4_vpp2_v2_run3.log`                                                                                                                                                                                                                                                                        |
| [-] | ~~Routing snapshot diff = 0 across PP / EP changes (G11)~~ | (deferred) | 2026-05-07 | Snapshot dump tooling never landed (would need a hash-router + learned-router dump hook + a cross-config diff script). Defer to a follow-up plan; the P19 smokes (A/B/C/D + EP8 / PP2EP4 profile runs) are the runtime verification of the P15 / P19 patches.                                                                                                                                                                                                  |
| [x] | `c10d::allreduce` warning gone                             |            | 2026-05-07 | Verified absent (zero hits for `c10d::allreduce` / `autograd kernel`) in `p19/smokeC_pp4_ep2_v2.log`, `p19/smokeD_pp2_ep4_vpp2_v2_run3.log`, `p19/profile_ep8.log`, `p19/profile_pp2_ep4.log`. The EP routed-output `all_reduce` fallback in `v4_moe.py` was retired with the dispatcher migration (P14 phase-2) and the `v4_enable_ep_allreduce_fallback` debug gate was deleted in P17 (`e591b893`); P19 confirms no path resurrects the warning at runtime. |


### Phase 19 patches landed


| patch id                                      | file                                                                | what it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `megatron.deepseek_v4.pp_tensor_shape`        | `primus/backends/megatron/patches/deepseek_v4_pp_shape_patches.py`  | Wraps **two** Megatron entry points in `megatron.core.pipeline_parallel.schedules` so V4's mHC `K=hc_mult` packing is reflected on the PP wire: (a) `get_tensor_shapes` (used by 1F1B) gets its seq dim multiplied by `hc_mult`, and (b) `forward_backward_pipelining_with_interleaving` (used by VPP) gets its `seq_length` kwarg multiplied by `hc_mult` before its inline `tensor_shape = [seq_length, mbs, hidden]` runs. Both wrappers gate on `model_type=="deepseek_v4"` + `hc_mult>1` + `PP>1` and are strict no-ops otherwise. Without (b) VPP allocates `[S, B, D]` recv buffers while the sender emits `[S*K, B, D]`, and `_lift_streams_in` reshapes the truncated copy to `[B, S/K, K, D]` — surfaces as `DeepseekV4HashRouter: hidden=32 vs token_ids=128`.                                                                                                                                                                                                                                                                                          |
| `megatron.deepseek_v4.pp_token_pre_broadcast` | `primus/backends/megatron/patches/deepseek_v4_get_batch_patches.py` | V4's hash-routed MoE layers (the first `num_hash_layers`) need raw `input_ids` on every PP stage that owns one, but `pretrain_gpt.get_batch` returns `None` on middle PP stages. Two earlier hooks deadlocked under VPP: an in-`DeepseekV4Model.forward` broadcast and a per-call `get_batch` broadcast both raced the interleaved schedule's pre-warmup `recv_forward.wait()`. This patch instead wraps `pp_module.get_forward_backward_func` so each `train_step` first runs **all** `num_microbatches × num_chunks` PP `dist.broadcast` collectives **upfront**, before the schedule's first send/recv, and caches the resulting `(tokens, labels, loss_mask, attention_mask, position_ids, packed_seq_params)` tuples per `(vp_stage, microbatch)`. A companion wrapper around `pretrain_gpt.get_batch` consumes the cache when active and falls back to the original implementation otherwise. Cache is reset in a `finally` after each schedule call. Cost: ~~`mbs * seq * 8B` per microbatch (~~32 KiB / step on the smoke), dwarfed by the activation P2P. |


## Phase 20 (plan-3) — V4-aware TFLOPs reporting

> Plan-3 P20. See `../plan-3/02-phase-details.md#p20--v4-aware-tflops-reporting` for design.
> Megatron's stock `num_floating_point_operations` lives in
> `third_party/Megatron-LM/megatron/training/training.py:228` and falls
> on the standard-Transformer branch for V4 (since V4 is single-latent
> KV, not MLA's compressed-KV). The branch counts dense MLP per layer,
> a flat attention shape, no Compressor / Indexer / HC streams / V4
> grouped low-rank O / V4 hash router / V4 MTP — so the per-iter
> TFLOPs printed today is misleading on V4.


|     | Task                                                                                                        | commit   | date       | note                                                                                                                                                                                                                                                                                                                                      |
| --- | ----------------------------------------------------------------------------------------------------------- | -------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | Audit Megatron's `num_floating_point_operations` branches (transformer / MLA / hybrid) for V4 mismatches    | 4c27787d | 2026-05-07 | Audited `third_party/Megatron-LM/megatron/training/training.py:228`. V4 falls on the standard-Transformer branch which charges flat-attn / dense-MLP / no-Compressor / no-Indexer / no-HC / no-V4-MTP — every gap is enumerated in `02-phase-details.md#p20`.                                                                             |
| [x] | New patch `primus/backends/megatron/patches/deepseek_v4_flops_patches.py`                                   | 4c27787d | 2026-05-07 | Wraps `megatron.training.training.num_floating_point_operations` and rebinds the local copy held by `primus.modules.trainer.megatron.trainer`. `dispatch_v4` is captured at install-time so `pretrain()`'s `args.model_type → ModelType` enum overwrite (`training.py:1210`) does not silently disable V4 dispatch.                       |
| [x] | Per-layer FLOPs breakdown in V4 closed form (attn-qkv / attn-score / compressor / indexer / moe / hc / mtp) | 4c27787d | 2026-05-07 | Closed form: Q-LoRA + single-latent KV + grouped low-rank O at `S * hc_mult`; Compressor / Indexer side-paths gated on per-layer `compress_ratio`; hash router on first `num_hash_layers`; V4 MTP runs a full inner V4 layer per depth + `eh_proj`. One-shot `log_rank_0` breakdown emitted on first invocation.                          |
| [x] | Unit test: 8-layer Flash-shape config closed-form vs hand-computed reference within 1% (G16)                | 4c27787d | 2026-05-07 | `tests/unit_tests/backends/megatron/test_deepseek_v4_flops_patches.py::TestComputeV4FlopsClosedForm` (7 cases, all green; per-component byte-asserted).                                                                                                                                                                                   |
| [x] | Unit test: non-V4 model_type returns upstream value byte-for-byte (G17)                                     | 4c27787d | 2026-05-07 | `TestDispatchSwitch::test_dispatch_v4_false_falls_through_byte_for_byte` (6 parametrised non-V4 model types) + `test_dispatch_v4_true_ignores_runtime_model_type_mutation` proves the closure-captured flag survives the `pretrain()` enum overwrite. 21/21 tests pass.                                                                   |
| [x] | Re-run P19 EP8 + PP2EP4 profile smokes; per-iter TFLOPs matches reference within 1%                         | 4c27787d | 2026-05-07 | Closed-form total = **73.43 TFLOP / global-batch** for the smoke config (`bs=16, S=128, hc_mult=4, L=8, mtp=0`). Measured: EP=8 last-iter 17.9 TFLOP/s/GPU × 8 × 0.5125s = 73.4 TFLOP (Δ 0.04%, `p20/smoke_ep8_pp1_final.log`); PP=2 EP=4 last-iter 14.0 TFLOP/s/GPU × 8 × 0.6559s = 73.5 TFLOP (Δ 0.09%, `p20/smoke_pp2_ep4_final.log`). |


## Phase 21 (plan-3) — Strict spec build (no nn-fallback)

> Plan-3 P21. See `../plan-3/02-phase-details.md#p21--strict-spec-build-no-nn-fallback` for design.
> Smokes at full Flash dims surfaced hundreds of
> `DeepSeek-V4 attention projection submodule init failed (... gather_output = True ...); fallback to nn.Linear.`
> warnings. The fallback masks real spec bugs and produces an
> unsharded model (vanilla `nn.Linear` instead of column / row parallel
> shards). TP=1 happens to work because the eager forward consumes a
> full-width tensor; TP>1 would silently diverge.


|     | Task                                                                                                                                                     | commit   | date       | note                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | Delete `try / logger.warning / fallback to nn.Linear` block in `deepseek_v4_attention.py:_build_projection`                                              | a4419ac5 | 2026-05-07 | `build_module` now raises; the masked path is gone. `logger` import + module-level `logger = logging.getLogger(__name__)` retired (only consumer was the fallback warning).                                                                                                                                                                                                                                            |
| [x] | Same in `deepseek_v4_block.py:_build_projection`                                                                                                         | a4419ac5 | 2026-05-07 | Same shape: provider's TE / Turbo linear constructor failure now propagates; `logger.warning(... fallback to nn.Linear)` removed.                                                                                                                                                                                                                                                                                      |
| [x] | Drop `try / except / fallback to local Compressor` in `deepseek_v4_attention.py:_build_compressor`                                                       | a4419ac5 | 2026-05-07 | The spec passed the same `Compressor` class as the fallback; the handler was dead code. Now `_build_compressor(spec)` builds via `build_module` and lets failures propagate; `spec=None` (CPU unit tests) still falls through to a direct `Compressor(...)` call.                                                                                                                                                      |
| [x] | Drop `try / except / fallback to local Indexer` in `deepseek_v4_attention.py:_build_indexer`                                                             | a4419ac5 | 2026-05-07 | Same shape as `_build_compressor`.                                                                                                                                                                                                                                                                                                                                                                                     |
| [x] | Drop `attn_sink_module` field + its `try / except / inline softmax fallback` build branch                                                                | a4419ac5 | 2026-05-07 | `self.attn_sink: nn.Parameter` (per-head scalar) stays — checkpoint key `layers.{i}.attn.attn_sink` and the inline softmax-with-sink in `_attention_forward` are canonical. The dead `submodules.attn_sink` slot, the `provider.v4_attention_sink()` method, and `primus/backends/megatron/core/transformer/attn_sink.py` (only consumer) are retired.                                                                 |
| [x] | Root-cause `gather_output=True` rejection: add `provider.column_parallel_linear_with_gather_output()` returning upstream `ColumnParallelLinear` (non-TE) | a4419ac5 | 2026-05-07 | TE wrappers raise on `gather_output=True` (`third_party/Megatron-LM/megatron/core/extensions/transformer_engine.py:747/972`); upstream non-TE `ColumnParallelLinear` accepts the flag natively. `_build_column_parallel_spec` routes through the new helper when `gather_output=True`; the standard TE path stays for `gather_output=False`.                                                                           |
| [x] | Root-cause `input_is_parallel=False` rejection: add `provider.row_parallel_linear_with_scatter_input()` returning upstream `RowParallelLinear` (non-TE)  | a4419ac5 | 2026-05-07 | Mirror fix: TE wrappers raise on `input_is_parallel=False` (`third_party/Megatron-LM/megatron/core/extensions/transformer_engine.py:1081`); upstream non-TE `RowParallelLinear` accepts it. `_build_row_parallel_spec` routes through the new helper when `input_is_parallel=False`.                                                                                                                                   |
| [x] | Audit: AST gate G15 — no `try / except / return nn.Linear` patterns under `primus/backends/megatron/core/`                                               | a4419ac5 | 2026-05-07 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p21_strict_build.py::TestG15NoNNLinearFallback` walks every `.py` under `primus/backends/megatron/core/` and asserts no AST `try → except → return nn.Linear(...)` patterns remain; a parameterised string-grep gate also bans the retired warning strings (`"submodule init failed"`, `"fallback to nn.Linear"`, etc.).                                    |
| [x] | TP=1 build smoke G15b on a 1L V4 toy                                                                                                                     | a4419ac5 | 2026-05-07 | `test_v4_p21_strict_build.py::TestG15bTPOneBuildSmoke::test_v4_attention_strict_build_at_tp1` — initialises a 1-rank `gloo` TP group, builds full V4 attention submodules through `_build_v4_attention_submodules`, instantiates `DeepseekV4Attention`, and asserts every linear is a Megatron-parallel / TE / Turbo class (never a bare `nn.Linear`). TP=2 forward-equivalence on `mi355-gpu-12` deferred to a future perf plan.       |
| [x] | All P19 smoke logs free of `"submodule init failed"` / `"fallback to nn.Linear"`                                                                         | a4419ac5 | 2026-05-07 | `p21/smoke_ep8_pp1.log` and `p21/smoke_pp2_ep4.log` — TP=1 PP=1 EP=8 and TP=1 PP=2 EP=4 both ran 10/10 iters cleanly (lm_loss converging 11.89 → 11.62 / 11.65). `grep -cE` for the banned strings returns `0` on both logs.                                                                                                                                                                                                            |


## Phase 22 (plan-3) — `core_attention` submodule for V4

> Plan-3 P22. See `../plan-3/02-phase-details.md#p22--core_attention-submodule-for-v4-turbo--te` for design.
> V4 attention currently runs an eager-Python `[B, H, S, S]` matmul +
> softmax for every layer. At full Flash dims the logits tensor alone
> is 16 GiB / microbatch. Promote `compress_ratio == 0` (dense + SWA)
> layers to `provider.core_attention()` (Turbo flash-attn with
> `use_sink_attention=true`, or TE flash-attn). HCA + CSA stay on
> eager-Python — see analysis in plan-3 02-phase-details.


|     | Task                                                                                                                                                                                               | commit          | date       | note                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | Add `core_attention: Optional[Union[ModuleSpec, type]] = None` to `DeepseekV4AttentionSubmodules`                                                                                                  | 4c362bca        | 2026-05-07 | dataclass surface verified by `TestSubmodulesSurface::test_core_attention_field_present`                                                            |
| [x] | `_build_v4_attention_submodules` builds a `core_attention` spec (via `provider.core_attention()`) when `compress_ratio == 0`                                                                       | 4c362bca        | 2026-05-07 | provider returns `PrimusTurboAttention` when `args.use_turbo_attention=True` else `TEDotProductAttention`; HCA / CSA leave the slot `None`          |
| [x] | `DeepseekV4Attention.__init__` builds `self.core_attention`; aliases `core_attention.sinks = self.attn_sink` when sink enabled                                                                     | 4c362bca        | 2026-05-07 | alias preserves the V4-Flash checkpoint key `layers.{i}.attn.attn_sink`; gated on `attn_sink_enabled and core_attention.use_sink_attention`         |
| [x] | New `_attention_forward_via_core` helper; reshapes `[B, H, S, head_dim]` ↔ Turbo's `qkv_format="sbhd"`                                                                                             | 4c362bca        | 2026-05-07 | dispatched only when `compress_ratio == 0 and self._use_core_attention`; KV passed as `[S, B, 1, D]` (MQA broadcast inside the kernel)              |
| [x] | `compress_ratio == 128` (HCA) / `== 4` (CSA) branches: code comments document why core_attention can't be used                                                                                     | 4c362bca        | 2026-05-07 | HCA: joint softmax with shared sink across [local_keys ++ pool] needs LSE-returning kernel; CSA: per-query top-K gather is not a flash-attn pattern |
| [x] | V4 builder auto-derives `args.use_sink_attention`, `args.sink_sliding_window`, `args.sink_window_even_layers_only` from `args.attn_sink` / `args.attn_sliding_window`                              | 4c362bca        | 2026-05-07 | `_maybe_plumb_v4_sink_attention_args` in `deepseek_v4_builders.py`; runs only when `enable_primus_turbo and use_turbo_attention and attn_sink`      |
| [x] | YAML `deepseek_v4_flash-BF16-pretrain.yaml` exposes `enable_primus_turbo`, `use_turbo_attention`, `use_turbo_deepep` via `${PRIMUS_*}` env knobs; `run_deepseek_v4.sh` threads them through        | 4c362bca        | 2026-05-07 | smoke flips `USE_TURBO_ATTENTION=True` to take the Turbo path                                                                                       |
| [x] | Unit test G18 — submodules surface, spec emission gates, sink alias, sink-attention args plumbing, dense forward equivalence (Turbo-mock vs eager) within 5e-3 on CUDA                             | 4c362bca        | 2026-05-07 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p22_core_attention.py` — 11 tests, all pass                                              |
| [x] | Smoke gate — V4 P22 wiring smoke (USE_TURBO_ATTENTION=False) at PP=2 EP=4 and PP=1 EP=8; 10/10 iters clean, no `submodule init failed` / `fallback to nn.Linear`                                   | 4c362bca        | 2026-05-07 | logs `p22/smoke_eager_pp2_ep4.log` + `p22/smoke_eager_ep8_pp1.log`                                                                                  |
| [ ] | Unit test G19 — HCA + CSA branches still produce identical outputs                                                                                                                                 |                 |            | regression guard for the eager-Python paths (deferred — out of plan-3 scope)                                                                        |


## Phase 23 (plan-3) — Turbo DeepEP dispatcher in V4 specs

> Plan-3 P23. See `../plan-3/02-phase-details.md#p23--turbo-deepep-dispatcher-in-v4-specs` for design.
> The existing
> `primus/backends/megatron/patches/turbo/moe_dispatcher_patches.py`
> swaps `MoEFlexTokenDispatcher` at module attribute level, but
> `deepseek_v4_layer_specs.py` captures `MoEFlexTokenDispatcher` via
> a top-level `import` so the local symbol stays the original class.
> V4 specs build the standard dispatcher even when turbo deepep is
> enabled; the turbo path silently never reaches V4.


|     | Task                                                                                                                                                                              | commit | date | note                                                                                                                                                                                                                           |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [x] | Add `_pick_v4_dispatcher_cls(config)` helper in `deepseek_v4_layer_specs.py`                                                                                                      | 6d185003 | 2026-05-07 | gate `is_v4_turbo_deepep_active(args)` mirrors `primus/backends/megatron/patches/turbo/moe_dispatcher_patches.py:_is_turbo_deepep_enabled`: `enable_primus_turbo and use_turbo_deepep and tensor_model_parallel_size == 1` and `primus_turbo` package importable; helper imports `PrimusTurboDeepEPTokenDispatcher` lazily from `primus.backends.megatron.core.extensions.primus_turbo` so unit tests on hosts without `primus_turbo` still work; falls back to upstream `MoEFlexTokenDispatcher` when the import fails. |
| [x] | `_build_ffn_spec` calls the helper instead of the static `MoEFlexTokenDispatcher` lookup                                                                                          | 6d185003 | 2026-05-07 | `_maybe_plumb_v4_turbo_deepep_args(args)` in `deepseek_v4_builders.py` defensively sets `args.moe_enable_deepep=True` and `args.moe_token_dispatcher_type="flex"` BEFORE config build; in production this is a no-op because the existing `before_train` patch fires first, but it keeps unit tests + alternate trainer flows correct. Explicit `moe_token_dispatcher_type="allgather"` user-opt-in is preserved with a rank-0 warning. |
| [x] | `_resolve_dispatcher_type_from_spec` in `v4_moe.py` recognises `PrimusTurboDeepEPTokenDispatcher` as `"flex"` (not the `unsupported dispatcher module` warning path)              | 6d185003 | 2026-05-07 | recognised by class name (`module.__name__ == "PrimusTurboDeepEPTokenDispatcher"`) so the resolver does not require `primus_turbo` to be importable on hosts that never opt in. |
| [x] | Unit test G20 — V4 dispatcher selection: turbo on (TP=1) → `PrimusTurboDeepEPTokenDispatcher`; turbo on + TP>1 → `MoEFlexTokenDispatcher` (rank-0 warning); turbo off → unchanged | 6d185003 | 2026-05-07 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p23_turbo_deepep_dispatcher.py` — 24 tests covering gating predicate (5), args plumbing (5), class resolution (8), V4 MoE resolver (6); all pass; full V4 unit-test suite: 161 / 162 pass (1 pre-existing failure in `test_v4_mtp.py` unrelated to P23). |
| [x] | Smoke logs report `MoE layer=N dispatcher active via PrimusTurboDeepEPTokenDispatcher` for every routed-MoE V4 layer                                                              | 6d185003 | 2026-05-07 | local-only smoke log `deepseek-v4/develop/progress/p23/smoke_turbo_deepep_ep8_pp1.log` (gitignored per new rule): TP=1 PP=1 EP=8, USE_TURBO_DEEPEP=True USE_TURBO_ATTENTION=False, 10/10 iters clean, lm_loss converges 11.88 → 11.65, throughput 17–19 TFLOP/s/GPU (vs. ~12.6 on the P22 eager baseline), all 8 MoE layers emit `dispatcher active via PrimusTurboDeepEPTokenDispatcher`, zero `submodule init failed` / `fallback to nn.Linear` / `unsupported dispatcher module` / `using local Compressor|Indexer` / `fallback to alltoall` warnings. |


## Phase 24 (plan-4) — Test harness + V4 attention shape fixtures + reference op

> Plan-4 P24. See `../plan-4/02-phase-details.md#phase-24--test-harness--v4-attention-shape-fixtures--reference-op` for design.
> P24 is the foundation for the two plan-4 Triton kernels (P25 dense / HCA, P26 CSA). It extracts the math currently inlined in `DeepseekV4Attention._attention_forward` and `_csa_forward` into pure functions under a new `v4_attention_kernels/reference.py`, ships a single source-of-truth shape table for V4-Flash + V4-Pro attention, and lands the equivalence harness (forward + backward, with per-dtype tolerance budgets) every plan-4 phase reuses. The `DeepseekV4Attention` forward path is re-pointed at the extracted functions so there is exactly one definition of "the eager truth" — the existing checkpoint reproduction baseline must not move.


|     | Task                                                                                                                                                                                              | commit | date | note                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | `tests/.../v4_attention_shapes.py` exposes `V4FlashAttnShape` + `V4ProAttnShape` parametrisable shape fixtures with small/medium/large seq tiers                                                  | 8b971881 | 2026-05-08 | source-of-truth read from `primus/configs/models/megatron/deepseek_v4_{flash,pro}.yaml`; `V4AttnShape` dataclass exposes `(B, S, H, head_dim, q_pe_dim, attn_sliding_window, sink, compress_ratio, P, K)`; `v4_flash_shape` / `v4_pro_shape` builders + `v4_attention_shape_grid` Cartesian helper for pytest parametrisation; tiers `small=128 / medium=512 / large=4096`; large marked slow when used in tests. |
| [x] | `primus/backends/megatron/core/transformer/v4_attention_kernels/__init__.py` placeholder + `reference.py` exposing `eager_v4_attention(...)` + `eager_v4_csa_attention(...)`                      | 8b971881 | 2026-05-08 | pure-function refactor of math in `_attention_forward` / `_csa_forward`; signatures match the planned Triton wrappers (`v4_attention(q, k, v, *, sink, swa_window, additive_mask, attn_dropout, training, scale)` and `v4_csa_attention(q, k_local, v_local, gathered, *, sink, swa_window, sparse_mask, attn_dropout, training, scale)`) so tests can plug them in interchangeably; `_softmax_with_sink` helper extracted from `_append_sink_softmax` (bit-identical math: `sink.shape[0]` replaces `self.num_heads`). |
| [x] | `DeepseekV4Attention._attention_forward` / `_csa_forward` re-point at `eager_v4_*`                                                                                                                | 8b971881 | 2026-05-08 | refactor-only, V4 numerics preserved bit-identically: `_attention_forward` passes `swa_window=0, additive_mask=attn_mask` so the reference op uses the caller-supplied mask directly (no rebuild); `_csa_forward` keeps the compressor / indexer / per-query top-K gather and delegates the joint-softmax math to `eager_v4_csa_attention`, passing `swa_window=int(self.attn_sliding_window)` so the local SWA mask is rebuilt deterministically (same `sliding_window_causal_mask` call as `_local_mask` makes); the `local_mask` arg of `_csa_forward` is retained in the signature for back-compat (called only from `forward`) and explicitly `del`'d in the body. |
| [x] | `tests/.../v4_attention_test_utils.py` exposes `compare_fwd_bwd(reference, candidate, inputs, *, fwd_tol, bwd_tol)`                                                                               | 8b971881 | 2026-05-08 | runs reference + candidate on independent input clones (every tensor that `requires_grad` is detached + cloned + re-enabled), calls `out.sum().backward()` on each, asserts fwd output + every leaf grad within tol; structured `_diag` formatter on mismatch (shape, max abs, max rel, 8 worst entries by absolute error). Tolerance presets: `FP32_TOL=(1e-5, 1e-5)`, `BF16_FWD_TOL=(2e-2, 2e-2)`, `BF16_BWD_TOL=(5e-2, 5e-2)`; `default_tols(dtype)` helper for plan-4 P25 / P26 reuse. |
| [x] | Unit test G22 — refactor safety net: extracted `eager_v4_*` produces bit-equal (or fp32-tolerance-equal) output to pre-extraction `_attention_forward` / `_csa_forward` on the existing 1L V4 toy | 8b971881 | 2026-05-08 | `tests/.../test_v4_p24_reference_op.py` — 12 tests across 3 classes: `TestG22EagerV4Attention` (5 tests: dense + sink_on/off, HCA-style concat-KV + sink_on/off, swa_window-built mask matches explicit, `Sq != Sk` raises), `TestG22EagerV4CSAAttention` (3 tests: CSA + sink_on/off, indexer-`-1` invalid slots), `TestG22ModuleRefactorPreservesNumerics` (3 tests: dense module determinism, dense module bit-equals pre-P24 inline math, CSA module bit-equals pre-P24 inline math). All 12 pass; full V4 attention suite: 173 / 174 pass, 1 skip, 1 pre-existing `test_v4_mtp.py` failure unrelated to P24 (verified by stash + re-run on HEAD). The pre-P24 inline math is a verbatim copy in the test file (`_pre_p24_attention_forward`, `_pre_p24_csa_attention_forward`, `_pre_p24_softmax_with_sink`) that is never imported elsewhere — tamper-proof. |
| [x] | dtype contract refinement: keep every matmul / einsum on tensor cores in input dtype; only the softmax block runs in fp32                                                                         | 38ef526c | 2026-05-08 | `eager_v4_attention` and `eager_v4_csa_attention` no longer pre-`.float()` Q/K/`gathered_h`; `Q @ K^T`, `einsum("bhsd,bhskd->bhsk", q, gathered_h)`, `probs @ V`, and `einsum("bhsk,bhskd->bhsd", probs_sparse, gathered_h)` all run on tensor cores in `q.dtype` (bf16 in production; fp32 accumulator inside the matmul). The fp32 upcast is now confined to `_softmax_with_sink`, which `.float()` upcasts logits + sink internally and returns probabilities in fp32; callers downcast via `probs.to(v.dtype)` before the V-matmul. Aligns with the FlashAttention / Megatron de-facto layout (bf16 `Q @ K^T`, fp32 softmax, bf16 `probs @ V`) and removes the redundant fp32 widening of `gathered_h` (was an extra MQA-broadcast copy). For fp32 inputs (every CPU unit test) the change is bit-identical (`q.float()` is a no-op when q is already fp32); for bf16 inputs it switches to the tensor-core fast path. Full V4 attention suite still passes: 173 / 174 (the same pre-existing `test_v4_mtp.py` failure unrelated to P24). |


## Phase 25 (plan-4) — `v4_attention` Triton kernel (compress_ratio ∈ {0, 128})

> Plan-4 P25. See `../plan-4/02-phase-details.md#phase-25--v4_attention-triton-kernel-compress_ratio--0-128` for design.
> Ships the first plan-4 kernel — flash-attn-style fwd + bwd in Triton, owned by Primus, targeted at V4's exact shape envelope (`head_dim=512`, MQA single-latent KV, optional `[H]` per-head learned sink, optional SWA, optional `[Sq, Sk]` additive bias). One kernel covers `compress_ratio == 0` (dense + SWA + sink, no bias) and `compress_ratio == 128` (HCA — sink + custom additive bias for the joint-softmax mask, no in-kernel SWA). Lands the `use_v4_triton_attention` switch (default `False`); the eager-Python fallback stays in tree.


|     | Task                                                                                                                                                                                                  | commit | date | note                                                                                                                                                                                            |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | Forward kernel `_triton/v4_attention_fwd.py` — flash-attn-style block-wise softmax with optional per-head sink, optional SWA, optional `[Sq, Sk]` additive bias                                       | 7df4cfeb | 2026-05-08 | landed with `BLOCK_M=BLOCK_N=32, BLOCK_DMODEL=512, num_warps=8, num_stages=1` (head_dim=512 SMEM headroom prefers 32×32 over the spec'd 64×64; perf retune is a follow-up). Sink joined as a virtual-key column in a final step (no acc contribution because notional value is zero); LSE saved fp32 for BWD. NaN-on-fully-masked-tile fixed with a finite `NEG_INF=-1e30` sentinel for the running-max init + in-kernel SWA / causal / boundary masks (subtraction of two -1e30 gives 0; `exp(-1e30)` underflows cleanly to 0). Mask precedence in-kernel: `HAS_ADD_MASK > SWA_WINDOW > USE_CAUSAL`. |
| [x] | Backward kernel `_triton/v4_attention_bwd.py` — re-materialises softmax in fp32 from saved LSE; outputs `dq, dk, dv, dsink`                                                                          | 7df4cfeb | 2026-05-08 | two-kernel design: (1) preprocess produces per-query `D = (dout*out).sum(-1)` fp32; (2) main BWD parallelizes over m-blocks, accumulates `dQ` in registers, atomic-adds `dK`/`dV`/`dsink` into fp32 buffers (cast back to input dtype on return). Tile `BLOCK_M=BLOCK_N=32, BLOCK_DMODEL=512` keeps Q+K+V+dout = 4 × 32 × 512 × 2 = 128 KiB inside MI355's 160 KiB SMEM with register-pressure headroom. Sink contribution `-P_sink * D` accumulated per-block then atomic-added into `dsink[h]`. |
| [x] | `v4_attention_kernels/v4_attention.py` — `V4AttentionFn(torch.autograd.Function)` + functional API `v4_attention(q, k, v, *, sink, swa_window, additive_mask, attn_dropout, training, scale)`        | 7df4cfeb | 2026-05-08 | saves `(q, k, v, out, lse, sink, additive_mask)` for BWD; `additive_mask` non-leaf / non-grad as designed; explicit `NotImplementedError` raised when `attn_dropout > 0 and training` so a stray non-zero dropout config fails loud (V4 trains with `attn_dropout=0`); MQA detected via `k.shape[1] == 1` and routed through the kernel's `HEAD_K=1` constexpr branch (single shared K/V head broadcast across query heads). |
| [x] | Add `use_v4_triton_attention: bool = False` arg (config + CLI); `DeepseekV4Attention.__init__` + `forward` wire precedence `use_turbo_attention > use_v4_triton_attention > eager` for cr ∈ {0, 128} | 7df4cfeb | 2026-05-08 | new fields on `DeepSeekV4TransformerConfig` (`use_v4_triton_attention`, `use_v4_triton_csa_attention`); yaml defaults in `deepseek_v4_base.yaml` + per-variant `deepseek_v4_flash-BF16-pretrain.yaml`; env-var → CLI plumbing in `run_deepseek_v4.sh` (`USE_V4_TRITON_ATTENTION` / `USE_V4_TRITON_CSA_ATTENTION`). `DeepseekV4Attention.__init__` reads the dense/HCA flag once, auto-disables it for cr==4 layers (which use the separate CSA flag landing in P26), and adds `_attention_forward_via_v4_triton(q, k, v, attn_mask)` helper that always routes through the kernel's `additive_mask` path so dense + HCA share one dispatch. The cr==0 / cr==128 branches in `forward` consult `self._use_v4_triton_attention` after the Turbo `_use_core_attention` short-circuit. |
| [x] | Unit test G23 — fwd equivalence at every (variant, cr ∈ {0, 128}, S ∈ {small, medium}, dtype, sink) shape; MQA layout covered explicitly                                                              | 7df4cfeb | 2026-05-08 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p25_v4_attention_fwd.py` — 34 tests across `(variant ∈ {v4_flash_small, v4_pro_small}) × (path ∈ {dense, HCA-style}) × (dtype ∈ {fp32, bf16}) × (sink ∈ {on, off}) × (kv_layout ∈ {mqa, mha})` plus 2 G25 determinism / dropout-rejection tests. All 34 pass on MI355X within the plan-4 tolerance budget (`fp32 atol=1e-4`, `bf16 atol=2e-2`). The dense path test pre-builds a `[Sq, Sk]` SWA-causal mask for the eager reference and lets the kernel apply SWA in-kernel via `swa_window > 0`, so both kernel mask paths are exercised. |
| [x] | Unit test G24 — bwd equivalence (`dq, dk, dv, dsink`) at every G23 shape; sink gradient explicitly asserted on a per-head basis                                                                       | 7df4cfeb | 2026-05-08 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p25_v4_attention_bwd.py` — 33 tests across the same parameter grid. All 33 pass within `fp32 atol=1e-4 / bf16 atol=5e-2` for `dq/dk/dv` and `fp32 atol=1e-4 / bf16 atol=5e-3` for the per-head `dsink`. Independent leaf clones built per test so the two BWD passes don't interfere. The `sink_off` cases assert both reference and candidate return `dsink=None`. |
| [x] | Unit test G25 — determinism with `attn_dropout=0.0`                                                                                                                                                   | 7df4cfeb | 2026-05-08 | `test_v4_p25_v4_attention_fwd.py::test_g25_determinism_fp32_mha` — two back-to-back `v4_attention(...)` calls with the same fp32 + MHA inputs return bit-identical outputs (`torch.equal`). `test_g25_dropout_with_training_is_rejected` locks the explicit `NotImplementedError` for `attn_dropout > 0, training=True`. The BWD's atomic-add into `dk/dv/dsink` is intentionally outside the determinism contract (G24's bf16 tolerance budget absorbs reduction-order non-determinism). |
| [x] | Plan-4 P25 dispatch plumbing surface tests                                                                                                                                                            | 7df4cfeb | 2026-05-08 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p25_dispatch.py` — 5 static-plumbing tests: config exposes both new fields with `False` defaults, `__init__` reads the flag, `forward` consults `self._use_v4_triton_attention`, the new `_attention_forward_via_v4_triton` helper exists and calls `v4_attention`, cr==4 layers auto-disable the dense/HCA flag (so a stray `use_v4_triton_attention=True` doesn't accelerate cr ∈ {0, 128} only and skew apples-to-apples perf comparisons). |
| [x] | Final regression gate — full V4 attention suite + new G23 / G24 / G25 / dispatch                                                                                                                       | 7df4cfeb | 2026-05-08 | full V4 transformer suite: 245 pass / 1 skip / 1 deselected (the deselect is `test_v4_mtp.py::test_helper_pulls_norm_and_linear_from_v4_provider`, the same pre-P25 / pre-P24 failure unrelated to plan-4 — verified by stash + re-run on HEAD in P24). New plan-4 P25 tests alone: 72 pass (G23: 34, G24: 33, G25: 2 fwd-determinism + dropout-rejection embedded in fwd test file, dispatch: 5). |


## Phase 26 (plan-4) — `v4_csa_attention` Triton kernel (compress_ratio == 4)

> Plan-4 P26. See `../plan-4/02-phase-details.md#phase-26--v4_csa_attention-triton-kernel-compress_ratio--4` for design.
> Ships the second plan-4 kernel — fused local SWA + per-query top-K sparse attention with shared per-head sink and joint softmax. The wrapper does the `torch.gather` of `pool[..., topk_idxs, :]` outside the kernel (matches today's eager `_csa_forward`); the kernel takes the `[B, H, Sq, K, head_dim]` gathered tensor directly. Lands the `use_v4_triton_csa_attention` switch (default `False`).


|     | Task                                                                                                                                                                                                                                       | commit | date | note                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | Forward kernel `_triton/v4_csa_attention_fwd.py` — fused local SWA branch + sparse top-K branch + sink, single tile loop with shared running `m / l` so the joint softmax is exact                                                          | da6f48bc | 2026-05-08 | one-program-per-`(b, qhid, m)`-row design (grid `= (Sq, B*HQ)`) keeps SMEM at `head_dim=512` inside the MI355 budget — gathered tile is `[BLOCK_K, D] = 32 KiB` per program vs `[BLOCK_M, BLOCK_K, D] = 1 MiB` for a multi-row tile. Single online-softmax loop carries fp32 `m_running / l_running / acc` across local-SWA → sparse → sink in that order; uses finite-sentinel `NEG_INF = -1e30` to keep `exp(NEG_INF - NEG_INF)` numerically stable for fully-masked tiles. Reads `gathered[B, Sq, K_topk, D]` (no H dim — broadcast across heads in the eager reference); `sparse_mask[B, Sq, K_topk]` flags `topk == -1` slots. `BLOCK_N = BLOCK_K = 32`, `num_warps = 4`. |
| [x] | Backward kernel `_triton/v4_csa_attention_bwd.py` — outputs `dq, dk_local, dv_local, dgathered, dsink`                                                                                                                                     | da6f48bc | 2026-05-08 | reuses `_v4_attention_bwd_preprocess_kernel` from P25 for the `D = sum_d(dout * out)` pre-pass. Main pass mirrors the FWD's per-row design: `dq` direct-stored (one program per row, no collisions); `dk_local / dv_local / dgathered / dsink` are atomic-added into fp32 buffers (collisions across heads — `dgathered` accumulates from every H head per `(b, m, k, d)` cell, matching the eager `expand(B, H, Sq, K, D)` autograd). `dgathered = ds * scale * q + p * dout` because `gathered` appears in both `qk` and `out`. |
| [x] | `v4_attention_kernels/v4_csa_attention.py` — `V4CSAAttentionFn(torch.autograd.Function)` + functional API `v4_csa_attention(q, k_local, v_local, gathered, *, sink, swa_window, sparse_mask, attn_dropout, training, scale)`                | da6f48bc | 2026-05-08 | wrapper detects `gathered.shape[2] == 0` and short-circuits to the dense `v4_attention` kernel (CSA's local-SWA + sink path is bit-identical to dense+SWA+sink in that limit). Honors `needs_input_grad` for each leaf (cleanliness — kernel still computes; the cast to `None` happens in `backward`). `attn_dropout > 0 and training=True` raises `NotImplementedError` (V4 trains with `attn_dropout=0`). |
| [x] | Add `use_v4_triton_csa_attention: bool = False` arg (config + CLI); `DeepseekV4Attention.__init__` + `forward` wire precedence `use_v4_triton_csa_attention > eager` for cr == 4                                                                          | da6f48bc | 2026-05-08 | the dataclass field + yaml + `run_deepseek_v4.sh` plumbing already landed in P25. P26 wires it through `DeepseekV4Attention.__init__` (auto-disable for cr ≠ 4 — symmetric to the dense flag's auto-disable for cr ∉ {0, 128}) and dispatches inside `_csa_forward` — both candidate functions (`v4_csa_attention` / `eager_v4_csa_attention`) share a signature so this is a 1:1 swap. |
| [x] | Unit test G26 — fwd equivalence at every (variant, cr == 4, S ∈ {small, medium}, dtype, sink) shape                                                                                                                                          | da6f48bc | 2026-05-08 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p26_v4_csa_attention_fwd.py` — 13 tests across `(variant ∈ {v4_flash_small, v4_pro_small}) × (dtype ∈ {fp32, bf16}) × (sink ∈ {on, off})` plus `K_topk == 0` short-circuit (× 4 dtype/sink combos) and explicit dropout-with-training rejection. All 13 pass on MI355X within the plan-4 tolerance budget (`fp32 atol=1e-4`, `bf16 atol=2e-2`). 25 % of sparse slots are flagged `-inf` to exercise the `topk == -1` mask path. |
| [x] | Unit test G27 — bwd equivalence (`dq, dk_local, dv_local, dgathered, dsink`) at every G26 shape; end-to-end through the wrapper-side `torch.gather` BWD so the test exercises the full CSA backward path                                  | da6f48bc | 2026-05-08 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p26_v4_csa_attention_bwd.py` — 10 tests across the same 8-cell parameter grid plus 2 finite-grad sanity checks (90% sparse mask). All 10 pass within `fp32 atol=1e-4 / bf16 atol=5e-2` for `dq/dk_local/dv_local/dgathered` and `fp32 atol=1e-4 / bf16 atol=5e-3` for the per-head `dsink`. Independent leaf clones built per test so the two BWD passes don't interfere with each other's autograd graphs. The `sink_off` cases assert both reference and candidate return `dsink=None`. |
| [x] | Plan-4 P26 dispatch plumbing surface tests                                                                                                                                                                                                  | da6f48bc | 2026-05-08 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p26_dispatch.py` — 4 static-plumbing tests: config exposes `use_v4_triton_csa_attention` with `False` default, `__init__` reads the flag, `__init__` auto-disables for cr ≠ 4 (the integer `4` constexpr appears in the init's `co_consts`), `_csa_forward` consults `self._use_v4_triton_csa_attention` and references `v4_csa_attention` from the bytecode. |
| [x] | Final regression gate — full V4 attention suite + new G26 / G27 / dispatch                                                                                                                                                                  | da6f48bc | 2026-05-08 | full V4 transformer suite (`tests/unit_tests/megatron/transformer/deepseek_v4/`): 272 pass / 1 skip / 1 deselected (the same pre-P26 / pre-P25 / pre-P24 deselect for `test_v4_mtp.py::test_helper_pulls_norm_and_linear_from_v4_provider` — unrelated to plan-4). New plan-4 P26 tests alone: 27 pass (G26: 13, G27: 10, dispatch: 4). Verified on `mi355-gpu-14` inside `dev_primus_wenx_693`. |


## Phase 27 (plan-4) — Release-tier shape gate + dispatch wiring + smoke

> Plan-4 P27. See `../plan-4/02-phase-details.md#phase-27--release-tier-shape-gate--dispatch-wiring--smoke` for design.
> P27 is split into three layered checks — (1) **G28 release-tier kernel correctness** at production V4 dims (`head_dim=512`, real `H`, real `swa_window`, real `K_topk`) — locks kernel-numerics in independently of the pipeline; (2) dispatch wiring + run-script docs (the flag plumbing already landed in P25 / P26, P27 only documents precedence + adds a startup log line); (3) TP=1 PP=1 EP=8 10-iter smoke with both kernels engaged. Order matters: G28 lands BEFORE the dispatch + smoke layers so any kernel-numerics regression at `head_dim=512` is localised before the pipeline is touched. Smoke logs gitignored under `progress/p27/` per the plan-3 directive.


|     | Task                                                                                                                                                                                                                                                                                                                                                                              | commit | date | note                                                                                                                                                                                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x] | **G28 release-tier shape gate** — extend `_BASE_SHAPES` in `test_v4_p25_v4_attention_fwd.py`, `test_v4_p25_v4_attention_bwd.py`, `test_v4_p26_v4_csa_attention_fwd.py`, `test_v4_p26_v4_csa_attention_bwd.py` with V4-Flash (`H=64, head_dim=512, swa_window=128, K_topk=512`) and V4-Pro (`H=128, head_dim=512, swa_window=128, K_topk=512`) entries; `S` calibrated so the eager fp32 reference fits MI355X memory (V4-Flash @ S=1024, V4-Pro @ S=512). Guarded with `pytest.mark.slow` so default CI still runs the fast tier only. Lands FIRST in P27 per user directive — the kernels' real-shape correctness is locked in BEFORE the dispatch + smoke layers are added on top. | e19663f7 | 2026-05-08 | `tests/unit_tests/megatron/transformer/deepseek_v4/conftest.py` adds a function-scoped autouse `torch.cuda.empty_cache()` fixture (the eager CSA reference's `torch.einsum("bhsd,bhskd->bhsk", q, gathered_h)` materialises a `[B, H, Sq, K, D]` intermediate up to ~64 GiB at fp32 — without per-test cache release the second / third release-tier test in a pytest session would OOM well before exhausting MI355's 287 GiB HBM). `tests/unit_tests/conftest.py` registers `pytest.mark.slow` and adds a `--run-slow` opt-in (also accepts `-m slow`). V4-Pro release `K_topk` was downscaled from production `1024` to `512` to keep the eager broadcast peak at 64 GiB instead of 128 GiB; the kernel's per-row sparse-tile loop is still exercised at full production `K_topk=512` via the V4-Flash release row. Release-tier bf16 tolerances bumped to absorb `head_dim=512` matmul-noise + `tl.atomic_add` jitter on `dk / dv`: FWD bf16 `atol=5e-2` (vs 2e-2 fast); BWD bf16 `dq/dk/dv/dgathered atol=2e-1`, `dsink atol=5e-2`. **80 / 80 release-tier tests pass** on `mi355-gpu-14` inside `dev_primus_wenx_693` in 60.2 s (`pytest --run-slow -m slow`); fast-tier suite remains 272 pass / 81 skipped (the 80 new release-tier tests are now in the skipped count alongside the pre-existing skip; the lone `test_v4_mtp.py::test_helper_pulls_norm_and_linear_from_v4_provider` deselect predates plan-4). |
| [x] | Dispatch precedence in `DeepseekV4Attention.forward`: `use_turbo_attention > use_v4_triton_attention > eager` for dense / HCA; `use_v4_triton_csa_attention > eager` for CSA                                                                                                                                                                                                       | e19663f7 | 2026-05-08 | rank-0 startup log line landed via new `DeepseekV4Attention._log_kernel_choice` (called once at end of `__init__`, logs `[V4-attn] Layer N: cr=R, kernel = ...` through the module's `logging.getLogger(__name__)`). Precedence is now documented in a dedicated section of the `DeepseekV4Attention` class docstring covering the cr-by-cr table (cr ∈ {0, 128, 4}), the auto-disable rules for the two flags, and the "core_attention only built for cr == 0" caveat. Smoke log confirmed 8/8 layers emit the expected line (`v4_attention (Triton, dense path)` for cr=0 layers 0/1/7, `v4_csa_attention (Triton)` for cr=4 layers 2..6). |
| [x] | `run_deepseek_v4.sh` exposes `USE_V4_TRITON_ATTENTION` / `USE_V4_TRITON_CSA_ATTENTION` env vars + matching CLI flags; both default `False`                                                                                                                                                                                                                                          | e19663f7 | 2026-05-08 | env-var → CLI plumbing was already in place from P25 (lines 85-86 + 133-134 of `run_deepseek_v4.sh`); P27 added a soft `[WARN]` echo when either flag is on AND `PRIMUS_TP > 1`, since plan-4 unit tests / smoke gates only ran TP=1 (kernels are MQA-centric and operate per-rank on the local `H/TP` head slice — TP > 1 should work but stays uncovered). The warning is informational only (not a hard abort) so existing TP=1 smokes are unaffected. |
| [x] | Unit test G29 — dispatch precedence (turbo > v4 > eager for dense; v4_csa > eager for CSA) covered by a parametrised test that mocks each kernel and asserts the right one fired                                                                                                                                                                                                  | e19663f7 | 2026-05-08 | `tests/unit_tests/megatron/transformer/deepseek_v4/test_v4_p27_dispatch_precedence.py` — 16 tests grouped into four sub-gates: (G29.A) 7 parametrised log-line tests covering each (cr, flag) → expected-kernel combo + format / once-per-call / layer-number assertions; (G29.B) 1 runtime-mock test patching `v4_attention` in the module namespace and asserting `_attention_forward_via_v4_triton` actually invokes it with the right kwargs (`additive_mask`, `swa_window=0`); (G29.C) 3 runtime-mock tests on `_csa_forward` covering precedence (csa_triton wins / eager wins) + the kwargs contract to both candidate kernels; (G29.D) 2 auto-disable runtime tests for the two cross-layer-kind contracts. All 16 pass on `mi355-gpu-14` inside `dev_primus_wenx_693`. |
| [x] | Smoke run on a free MI355X (`mi355-gpu-14` or peers): TP=1 PP=1 EP=8, 10 iters, `USE_V4_TRITON_ATTENTION=True USE_V4_TRITON_CSA_ATTENTION=True USE_TURBO_ATTENTION=False USE_TURBO_DEEPEP=True PRIMUS_SEQ_LENGTH=128`                                                                                                                                                                | e19663f7 | 2026-05-08 | `deepseek-v4/develop/progress/p27/run_smoke_v4_kernels_ep8_pp1.sh` invokes `run_deepseek_v4.sh` with both flags + Turbo DeepEP on; tee'd log lands at `progress/p27/log_smoke_v4_kernels_ep8_pp1.txt`. Phase folder ships `.gitignore` excluding `*.log` / `log_*.txt` / `debug.log` / `*.tgz` / `*.json` (matches the P25 / P23 convention) so the smoke artefacts MUST NOT land in git. Smoke log NOT uploaded to GitHub per the plan-3 directive — the script + .gitignore are the only files committed for this phase. |
| [x] | Smoke gate G30 — 10-iter loss curve stable, no NaN / Inf, no banned warnings (`v4_attention SMEM exceeded` / `v4_attention NaN observed` / `v4_csa_attention NaN observed`)                                                                                                                                                                                                        | e19663f7 | 2026-05-08 | green on `mi355-gpu-14` inside `dev_primus_wenx_693`. 10/10 iters clean; lm_loss converged stably (`11.85 → 11.84 → 11.78 → 11.68 → 11.69 → 11.72 → 11.65`); grad norm steady at 6.6 → 7.7 (no explosion); 0 nan iterations across all 10 steps; throughput **17.3 TFLOP/s/GPU** steady-state (vs ~12.6 on the P22 eager baseline → +37 % uplift) at ~500 ms / iter. All 8 layers emitted the expected `[V4-attn]` kernel-choice log line (`v4_attention (Triton, dense path)` × 3 for cr=0 layers, `v4_csa_attention (Triton)` × 5 for cr=4 layers). Banned-warning grep returns 0 hits for `submodule init failed` / `c10d::allreduce_` / `fallback to nn.Linear` / `unsupported dispatcher module`. The `Layer 0` placeholder in every log line is a Megatron-spec ergonomic detail (the V4 spec does not plumb `layer_number` through to `DeepseekV4Attention.__init__` at construction time — Megatron sets it later via the spec walker); kernel selection itself works correctly because dispatch only depends on `compress_ratio` (visible in the log line) and the runtime flags. Tracked as a follow-up. |
| [x] | Hand-off note in plan-4 `02-phase-details.md` — commit SHAs P24..P27, G28 release-tier matrix size + pass count, smoke iter / TFLOP/s / ms-per-iter numbers vs eager baseline, follow-ups (HCA LSE-merge, CSA in-kernel gather, FP8)                                                                                                                                                | e19663f7 | 2026-05-08 | new `### P27 hand-off (status)` block appended at the end of Phase 27 in `plan-4/02-phase-details.md` summarising: (1) commit chain P24 → P27 (`8b971881 / 38ef526c → 7df4cfeb → da6f48bc → e19663f7`); (2) test totals — fast tier 272 / 81 skipped (P25 G23 + G24 + G25 + dispatch + P26 G26 + G27 + dispatch + P27 G29 + 80 release-tier deselected without `--run-slow`), release tier 80/80 with `--run-slow`; (3) G30 smoke perf delta vs eager / DeepEP baseline; (4) follow-up list (Megatron-side `layer_number` plumbing, full-S=4096 smoke, HCA LSE-merge for Turbo, CSA in-kernel gather, FP8). |


## Blockers / Risks Log


| date       | description                                                                                                                                                                                          | status                                                  | decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-04-28 | PyTorch warns `c10d::allreduce`_ autograd kernel is not registered for the EP routed-output allreduce path in `v4_moe.py`                                                                            | resolved (P14 phase-2 + P17 + P19 runtime verification) | flag removed during P14 phase-2 dispatcher migration; `v4_enable_ep_allreduce_fallback` debug gate deleted in P17 (`e591b893`); P19 smokes (A/B/C/D + EP8 / PP2EP4 profile runs on `mi355-gpu-12`) confirm zero `c10d::allreduce` warnings in stderr — the EP routed-output reduction now flows entirely through `MoEAlltoAllTokenDispatcher` / `MoEFlexTokenDispatcher`.                                                                                                                                                                                                                                                                                             |
|            | (example) HC 4-stream PP send/recv interface does not directly support 4D tensor                                                                                                                     | tracked in plan-2                                       | Plan-2 P15: lift `[S,B,K,D]` to `[S*K,B,D]` for PP P2P; revisit a 4D PP send path in P21                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-05-01 | Current attention does not match real V4 (single-latent KV / q_norm / kv_norm / grouped O all missing)                                                                                               | open                                                    | Plan-2 P13 rebases on `MLASelfAttention` and lands the missing pieces                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-05-01 | HashRouter has no learnable gate weight; clamped SwiGLU clamps post-mul instead of pre-mul; `w1`/`w3` fused                                                                                          | resolved (P14 phase-1)                                  | both routers now share a learnable `weight` Parameter; activation rewritten as pre-multiplication clamp; `ClampedSwiGLUMLP` uses separate `w1` / `w3` Linears (state-dict parity with HF)                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-05-01 | Custom V4 block / layer / MoE bypass `TransformerBlock` / `TransformerLayer` / `MoELayer`                                                                                                            | resolved (P14 phase-2 + P15)                            | `DeepseekV4MoE` is a `MegatronModule` (P14 phase-2); `DeepseekV4HybridLayer` is a `TransformerLayer` and `DeepseekV4TransformerBlock` is a `TransformerBlock` (P15). Aux-loss / z-loss inheritance via `TopKRouter` subclassing remains tracked into P19                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-05-01 | Token-IDs propagation via `decoder._v4_token_ids` attribute                                                                                                                                          | resolved (P15)                                          | `DeepseekV4Model.forward` now passes `token_ids=input_ids` directly to the decoder; AST audit gate prevents regressions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-05-01 | No state-dict adapter / V4-Flash safetensors cannot be loaded into the Primus model                                                                                                                  | deferred                                                | Pre-training-only release; adapter + numerical-alignment moved to **Phase 22+** (deferred follow-up) per 2026-05-01 user-driven reshuffle. Activate when SFT / evaluation needs HF weights                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-05-01 | Plan-2 reshuffle: P17 → code cleanup; P21 → docs only; HF state-dict adapter / V4-Flash numerical alignment deferred to Phase 22+                                                                    | resolved (docs-only)                                    | Pre-training is the release path; HF-checkpoint load is not. Dead-code (`_RMSNorm` duplicates / `dual_rope.py` / `csa_attention.py` / `hca_attention.py` / legacy `DeepseekV4MTPBlock` / EP `all_reduce` fallback gate / `_v4_token_ids` residue / yaml comment fixes) front-loaded into P17 so the spec audit (P18) and release gates (P19 / P20) walk a clean tree. See plan-2 `01-roadmap.md` + `03-phase-details.md`                                                                                                                                                                                                                                              |
| 2026-05-07 | Phase 19 smoke B/C: HC × PP wire-shape mismatch (recv `[S, B, D]` vs send `[S*K, B, D]`)                                                                                                             | resolved (P19)                                          | `megatron.deepseek_v4.pp_tensor_shape` patch wraps `schedules.get_tensor_shapes` (multiplies seq dim by `hc_mult`) for the 1F1B path. Surfaces as `dual_rope` shape error or hash-router flatten mismatch when `pp_tensor_shape` is missing.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-05-07 | Phase 19 smoke C: hash-routed layer on middle PP stage has `input_ids = None` (Megatron `pretrain_gpt.get_batch` short-circuits)                                                                     | resolved (P19)                                          | `megatron.deepseek_v4.pp_token_pre_broadcast` patch broadcasts source-of-truth `input_ids` from PP rank 0 across the PP group **upfront** in the `forward_backward` schedule wrapper, before any send/recv runs. Two earlier in-loop variants (in-forward and in-`get_batch`) both deadlocked the interleaved-1F1B / VPP schedule because PP rank 0's broadcast wait blocks before its first `send_forward`, while PP rank > 0 is already parked in `recv_forward.wait()`.                                                                                                                                                                                            |
| 2026-05-07 | Phase 19 smoke D (VPP=2): inline `tensor_shape` in `forward_backward_pipelining_with_interleaving` ignores `get_tensor_shapes`                                                                       | resolved (P19)                                          | Same `pp_tensor_shape` patch additionally wraps the interleaved schedule and scales its `seq_length` kwarg by `hc_mult`. Inside the schedule `seq_length` is only used for the inline `tensor_shape` (`schedules.py:1001-1004`), so scaling is shape-only and side-effect-free.                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-05-07 | Megatron `num_floating_point_operations` does not know about V4 (HC × K, single-latent KV, grouped low-rank O, Compressor / Indexer / hash router / V4 MTP)                                          | resolved (plan-3 P20)                                   | New patch `primus/backends/megatron/patches/deepseek_v4_flops_patches.py` wraps the upstream function with a V4 closed form. Two non-obvious invariants surfaced & fixed: (1) `primus.modules.trainer.megatron.trainer` imports `num_floating_point_operations` at module load → must rebind the local copy after monkey-patching the upstream module; (2) `pretrain()` rewrites `args.model_type` from the YAML string to a `ModelType` enum (`training.py:1210`) before `train()` runs → V4 dispatch flag is captured at *install time* via the closure, not re-checked per call. Smokes verify within 1% of the closed form on EP=8 and PP=2 EP=4 (P20 row above). |
| 2026-05-07 | V4 attention + dense-MLP projection helpers `logger.warning` and silently fall back to `nn.Linear` when `build_module(spec)` raises (e.g. `gather_output=True` rejected by `TEColumnParallelLinear`) | resolved (plan-3 P21)                                   | Two new provider helpers (`column_parallel_linear_with_gather_output` / `row_parallel_linear_with_scatter_input`) return upstream non-TE `ColumnParallelLinear` / `RowParallelLinear` for the gather / scatter cases TE explicitly rejects. `_build_column_parallel_spec` and `_build_row_parallel_spec` route through them automatically; every `try/except/return nn.Linear` and `try/except/return local Compressor                                                                                                                                                                                                                                                |
| 2026-05-07 | V4 attention runs an eager-Python `[B, H, S, S] fp32` matmul + softmax on every layer; at full Flash dims the logits tensor alone is 16 GiB / microbatch                                             | wired (plan-3 P22, 4c362bca)                            | Wiring landed: new `core_attention` submodule slot, spec emission for `compress_ratio == 0` only, `_attention_forward_via_core` reshape to Turbo's `qkv_format="sbhd"`, sink alias to preserve `attn_sink` checkpoint key. HCA / CSA explicitly stay on eager-Python (joint softmax / sparse top-K can't ride a stock flash kernel). End-to-end Turbo on V4-Flash dims is gated on two aiter Triton kernel upgrades: (1) SMEM tuning for `head_dim=512` (the kernel falls into the `BLOCK_M=128 BLOCK_N=64` config with 256 KiB SMEM, which exceeds MI355's 160 KiB limit); (2) SWA support so `attn_sliding_window=128` is honored (`aiter/ops/triton/attention/mha.py:68` raises `ValueError: Sliding Window is not supported yet in the Triton Backend`). Until those land, the production V4-Flash path stays on eager-Python; smoke baseline with the new wiring is green at PP=2 EP=4 + EP=8 PP=1. |
| 2026-05-07 | aiter Triton flash-attn cannot run V4-Flash dims (SMEM blow-up at `head_dim=512`; explicit raise on `window_size != (-1, -1)`)                                                                       | tracked into plan-4 (in-tree Triton kernel)             | (1) Padding V4 Q/K with a phantom 64-channel "pe" axis would push the kernel into the smaller-block tuning bucket — math-equivalent because the extra channels contribute 0 to QK^T. (2) Land aiter SWA support so `attn_sliding_window` is honored at the kernel level. Both are kernel-engineering follow-ups in aiter. Plan-4 takes the parallel route: Primus owns its own Triton kernels (`v4_attention` for cr ∈ {0, 128}; `v4_csa_attention` for cr == 4), tuned for V4's exact shape envelope (BWD `BLOCK_M=BLOCK_N=64` to fit MI355's 160 KiB SMEM at `head_dim=512`; in-kernel SWA + `[Sq, Sk]` additive bias). The plan-3 P22 Turbo wiring stays in place so a future aiter upgrade can flip back to it on a YAML / env knob. |
| 2026-05-07 | `deepseek_v4_layer_specs.py` captures `MoEFlexTokenDispatcher` at top-level import, so the Primus turbo-deepep monkey-patch never reaches V4 specs                                                   | wired (plan-3 P23, 6d185003) | New `_pick_v4_dispatcher_cls` helper probes `args.use_turbo_deepep` (gated by `enable_primus_turbo` + `tp_size==1` + `primus_turbo` importable) and imports `PrimusTurboDeepEPTokenDispatcher` directly. `_resolve_dispatcher_type_from_spec` in `v4_moe.py` recognises the turbo class as `"flex"` instead of falling through to `"alltoall"`. Smoke at TP=1 PP=1 EP=8 (USE_TURBO_DEEPEP=True): all 8 V4 MoE layers report `dispatcher active via PrimusTurboDeepEPTokenDispatcher`, 10/10 iters clean, throughput climbs from ~12.6 → 17–19 TFLOP/s/GPU vs. the P22 eager baseline. In production the `before_train` patch fires before V4 builder anyway, so the V4-side plumbing helper (`_maybe_plumb_v4_turbo_deepep_args`) is a defensive idempotent no-op; it only mutates args when the patch hasn't run yet (alternative trainer flows / unit tests). |
